# Install only as an instance-specific drop-in for an approved offline test.
[Service]
ExecStart=
ExecStart=/data/tt-post-gpu/runtime/bin/python /data/tt-post-gpu/ops/verify_offline.py --output-root /data/tt-post-gpu/validation/%i --lane direct_outro
