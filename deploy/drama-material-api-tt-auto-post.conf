[Service]
# Optional so main API remains healthy before the independent sidecar is
# installed. TT automatic-publish routes fail closed until the bearer exists.
EnvironmentFile=-/etc/tt-auto-post-app.env
