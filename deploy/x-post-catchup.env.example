# Root-owned systemd EnvironmentFile for one explicitly approved catch-up.
# Copy to /etc/x-post-catchup.env with mode 0400; never enable a timer for it.
X_POST_CATCHUP_RUN_DATE=2026-07-27
X_POST_CATCHUP_EXPECTED_MISSING_COUNT=6
X_POST_CATCHUP_REASON=scope_expansion_v1
