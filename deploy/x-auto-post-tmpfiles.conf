# Install as /etc/tmpfiles.d/x-auto-post.conf, then run systemd-tmpfiles --create.
# The independent SQLite ledger is private to the existing x-post-daily user.
d /mnt/data-disk/x-auto-post-publisher 0700 x-post-daily x-post-daily -
