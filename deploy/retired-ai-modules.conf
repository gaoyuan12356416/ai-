# Server-scope return runs before the existing location/proxy dispatch.
# Keep the separately owned playable preview API and published material files.
if ($uri ~ "^/(api/ad-control(/|$)|api/ad-material(/(?!playable-preview$)|$)|api/ad-material-(vision|generation)(/|$)|ad-material-test(-api)?(/|$)|ad-control(-[a-z-]+)?\.(html|js|css)$|ad-material(s|tasks|-tasks|-task-optimization-test)\.html$)") {
    return 410 "This feature has been retired.\n";
}
