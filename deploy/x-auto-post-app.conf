[Service]
# Optional so the main API remains healthy before the independent sidecar is
# installed. X automatic-template routes fail closed until this file exists
# with the dedicated bearer.
EnvironmentFile=-/etc/x-auto-post-app.env
