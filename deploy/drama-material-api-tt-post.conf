[Service]
# Optional only so an older environment without the TT feature keeps the main
# API healthy. TT routes themselves remain fail-closed until this file exists
# with a valid dedicated bearer.
EnvironmentFile=-/etc/tt-post-app.env
