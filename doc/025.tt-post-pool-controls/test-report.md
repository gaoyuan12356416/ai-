# 测试报告

## 结论

本地与生产候选回归共 `281/281` 通过，生产部署和登录态只读验收通过。

## 自动化

| 测试组 | 结果 |
| --- | --- |
| TT core | 59/59 通过 |
| TT service | 96/96 通过 |
| TT 发布池 UI contract | 32/32 通过 |
| TT app contract | 12/12 通过 |
| TT GPU worker 回归 | 57/57 通过 |
| TT prepare runner | 14/14 通过 |
| TT 个号设置 UI 回归 | 11/11 通过 |
| Python 编译与 diff check | 通过 |

共 281/281 个 TT 自动化用例通过。

关键结果：

- 停用自动发布不依赖新的发布确认、账号仓库、发布设置、creator-info、发布时间或正式门禁。
- 停用使用 SQLite 原子版本比较；保留原发布时间和历史 consent，无记录时不创建伪造行。
- 停用后新的自动 due 不建队、不消费素材；已领取或已建队列任务不被隐式取消。
- 制作中素材不能手动发布；状态转为 ready 后页面刷新可用数量，并保留同一手动请求幂等键。
- 排期草稿不会被门禁、账号或制作状态的后台刷新覆盖。
- 账号失效或账号源整体故障时，只返回匿名本地排期占位用于安全停用，重新启用继续 fail closed。
- 版本冲突、保存期账号切换、旧 GET 覆盖和制作状态刷新失败重试的竞态均已覆盖。

独立代码审查结论：GO，本次范围内无剩余 P0/P1。

## 生产验收

- 生产功能提交：`2c8c5428474b907155d22f6b4733f9a7240eaf8e`；上线时间：`2026-07-31 18:32:45 CST`。
- 三份公开页面 SHA-256 一致：`99c71e8316b184b02566e6cb0b74fb8d6cb3d219be7f71272db75a50ecf8cc85`。
- 页面采用与 X 素材池一致的独立设置交互：显示“自动发布设置”“启用素材池自动发布”“保存发布设置”，停用草稿时按钮变为“关闭自动发布”。
- 取消自动发布勾选不要求新的发布确认；刷新制作状态后草稿仍保持未勾选。整页刷新后服务端仍为 `enabled=1`、每天 `11:00`、版本 `1`，证明验收没有保存开关。
- 上线时数据库 `integrity_check=ok`；排期 `1`、素材池 `5`、队列 `3`、schedule run `3`、素材 intake `4`。验收前后 queue、run 和 publish 数量未增加。
- 账号设置只读展示为所有人可见，允许评论、Duet、Stitch，并开启自有品牌披露。
- 验收未点击“立即发布一条”，没有制造真实 TikTok 请求。

### 2026-08-03 跟踪复验

- 生产仍使用同一功能 release 和页面哈希；CPU sidecar、runner/prepare、GPU 隧道及 SocialKit 同步健康。
- 账号 `640` 可确认；排期仍为每天 `11:00`、版本 `1`，关闭草稿与后台刷新隔离继续通过。
- 当前可立即发布 `0` 条、制作中 `0` 条，因此手动按钮按预期禁用并显示“该账号素材池没有可立即发布素材”。
- 自然排期在 `2026-08-01 11:00` 公开发布了素材 `5801636`，允许评论、Duet、Stitch；当前任务共 `4` 条，其中已发布 `3`、失败 `1`。跟踪复验未创建任务或修改排期。
