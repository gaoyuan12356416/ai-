# SA 评审意见

## 结论

有条件通过。以下问题均已在 `requirements.md` v2 中关闭，可进入开发。

## 问题清单

| 编号 | 严重级别 | 位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-001 | 高 | 数据合并 | 渠道行与转化行粒度不一致，原始 Join 会重复花费 | 两个事实独立聚合，仅在共享维度合并 | 已采纳 |
| SA-002 | 高 | 游戏归属 | Google 一个来源键可能对应多个游戏 | 仅唯一映射；多归属/未归属单列，禁止分摊 | 已采纳 |
| SA-003 | 高 | 国家维度 | 渠道 `country` 含 `AIG-WW` 等分组，不能等同转化国家 | 拆成“渠道国家/分组”和“转化国家” | 已采纳 |
| SA-004 | 中 | Schema | 设计记忆中的 `avg_play_duration_seconds` 与现网 `play_duration_seconds` 不同 | 运行时探测两个允许字段；均缺失则失败并保留旧快照 | 已采纳 |
| SA-005 | 中 | 发布安全 | 大表刷新或写 JSON 中断可能发布半套文件 | SQLite 事务 + 版本目录 + 最后原子切换 `latest.json/index.html` | 已采纳 |
| SA-006 | 中 | 来源覆盖 | Unity 不在统一渠道事实中，直接显示 0 会误导 | 使用手工 `cost` 兜底并在所有出口显式标注 | 已采纳 |
| SA-007 | 低 | 金额口径 | 手工表没有币种字段 | 页面固定展示依赖条件和数据质量提示 | 已采纳 |
| SA-008 | 高 | 渠道视图覆盖 | 原渠道明细只加载 delivery 日文件，按渠道会漏掉仅存在于手工转化表的 Unity/organic/restricted/Facebook | 同时读取 delivery/conversion 日文件，以指标互斥的并列事实聚合；禁止原始行 Join | 已采纳，BUG-006 |
| SA-009 | 中 | 展示精简 | 直接删除底层行计数可能削弱无双计回归，保留展示又不符合验收反馈 | 仅移除卡片/提示/表格/CSV 出口；内部计数、六渠道聚合与测试守恒继续保留 | 已采纳，v6 |

## 决策记录

- 复用 TT 报表现有飞书鉴权子请求，不改主 API。
- 新报表独立目录、SQLite、systemd timer 和 Nginx location，不改 TT 报表业务逻辑。
- 默认昨日，今日保留但标记未完结。
- 普通请求静态化；MySQL 只在 oneshot 刷新中访问只读端点。

## PM 修订确认

PM 已在 2026-08-25 将全部评审项写入 `requirements.md` v2，状态均为已采纳。

2026-08-25 验收反馈后，PM 将 SA-008 写入 `requirements.md` v5；复用现有版本文件，不增加 MySQL 查询或重复发布转化日文件。

2026-08-25 第二轮验收反馈后，PM 将 SA-009 写入 `requirements.md` v6；本次只调整前端呈现，不修改数据文件、SQLite 或刷新口径。

## v7 增量评审（2026-08-26）

结论：需求口径有条件通过，代码与生产发布尚未验收。

| 编号 | 严重级别 | 位置 | 评审结论 | 状态 |
| --- | --- | --- | --- | --- |
| SA-010 | 高 | Unity 查询 | 仅允许 `product='Neonarcade' AND category=0`，固定 `idx_date`；禁止与 category 1 求和 | 已采纳，待只读对账 |
| SA-011 | 高 | 指标语义 | 曝光必须取 `starts`，不得误用 completed `views` | 已采纳，待回归 |
| SA-012 | 高 | 事实合并 | Unity 渠道指标与手工转化继续分离；不接入 Unity raw spend，有效花费仍为手工兜底 | 已采纳，待回归 |
| SA-013 | 高 | 游戏归属 | 映射键必须包含日期与 `campaign_id + creative_pack_id/ad_id`，只丰富游戏维度 | 已修复并通过本地回归，待只读对账 |
| SA-014 | 中 | SQLite 兼容 | 负 source ID 隔离来源；`source_game_id` 只能增量迁移，不重建/清空旧缓存 | 已采纳，本地回归通过 |

v7 不改变公开 JSON 列集合；改变的是 delivery 文件中 Unity 行的覆盖范围，渠道 CPI 仍保持既有 source-spend 口径。当前本地门禁与独立代码评审已绿；生产发布前仍须补齐阴影对账、备份与回滚证据。
