# 测试报告

## 测试结论

CTB-000101 已完成实现、真实只读数据阴影、生产部署、BUG-006/BUG-007 修复和自然刷新终验，可提交用户验收。34 个标准用例中 33 个通过、0 个失败、1 个保留为用户手点验收：Chrome 扩展不能暴露 Blob 下载事件，但 CSV 点击无页面报错，BOM/CRLF、字段范围、内部行计数不可导出和延迟释放契约均已通过。

## 测试范围

- 只读 MySQL Schema/字段、双事实口径和包含 Unity 的渠道游戏映射；
- SQLite 日期事务、全量/增量刷新、原子清单和发布审计；
- 游戏总览、渠道明细、转化明细、日期/游戏/渠道/国家/关键词/最低花费/维度/分页；
- Nginx 飞书鉴权、静态私有缓存、systemd timer、共享锁和回滚边界；
- 桌面 Chrome 与 390×844 手机布局；
- 现有 TT 报表及 AI 主 API 进程回归。

## 执行统计

| 类型 | 数量 | 通过 | 失败 | 待用户确认 |
| --- | ---: | ---: | ---: | ---: |
| Python 单元/部署契约 | 27 | 27 | 0 | 0 |
| 前端静态/Node 语法契约 | 1 | 1 | 0 | 0 |
| 标准测试用例 | 34 | 33 | 0 | 1 |
| 生产 MySQL/SQLite/JSON 对账组 | 3 | 3 | 0 | 0 |
| 生产浏览器布局/交互组 | 10 | 9 | 0 | 1 |

## 缺陷情况

| 缺陷 | 严重级别 | 发现阶段 | 结论 |
| --- | --- | --- | --- |
| BUG-001 测试精度/文案假失败 | 低 | 本地 QA | 已修复 |
| BUG-002 release 无法隐式导入只读 MySQL 模块 | 高 | SA 代码评审 | 已修复并经全量刷新验证 |
| BUG-003 MySQL 日期格式返回字面量 | 高 | 首次生产阴影 | 已修复并增加 SQL 回归 |
| BUG-004 总播放时长重复乘安装 | 高 | 真实指标对账 | 已修复；平均时长与源表精确一致 |
| BUG-005 CSV Blob URL 生命周期过短 | 低 | 生产浏览器 | 已加固；下载文件由用户手点确认 |
| BUG-006 渠道明细遗漏仅转化侧渠道 | 高 | 用户验收 | 已修复；自然 timer、六渠道与无双计终验通过 |
| BUG-007 Unity 渠道安装/曝光/点击缺失 | 高 | 用户验收 | 已修复；阴影、生产对账、浏览器与自然 timer 通过 |

## 验证证据

### 不可变代码与构建

- 运行提交：`28cefbb0c6439bea53b243de2595e789002dfa64`；
- release：`/opt/ai-game-performance/releases/28cefbb0c6439bea53b243de2595e789002dfa64`；
- `current` 指向该 release，worktree clean；
- 本地及服务器均为 27/27 PASS，`node --check`、前端契约和 Unity 并列事实行为测试 PASS。

### 全量容量

- 日期：2026-08-10 至 2026-08-26；
- v7 独立全量阴影：487,895 条转化事实、21,568 条投放事实、2,329 条总览日组合；
- 全量阴影 `quick_check=ok`、`integrity_check=ok`，峰值 RSS 321,976 KiB；
- 全量命令墙钟 3 分 18.63 秒；先等待现有 TT 共享锁自然释放，未并发扫库；
- 在线完整刷新与阴影合计一致，发布前 SQLite 在线备份为 298,778,624 bytes 且 `quick_check=ok`。

### 2026-08-24 精确对账

转化表 MySQL 与 SQLite 完全一致：

- 行数 48,088；成本 54,894.469168；安装 165,854；D1 留存 28,306；
- 总播放时长 177,524,774.967 秒；平均时长 1,070.367763 秒；
- D0/D1 收入当前均为 0，页面如实展示，不伪造 ROAS。

投放表 MySQL 与 SQLite 完全一致：

- Google：625 行，花费 50,754.22，安装 168,687，曝光 83,241,630，点击 1,590,203；
- TikTok：213 行，花费 875.02，安装 3,333，曝光 1,297,036，点击 55,819。

两类事实均按规范化字段逐行比较，源库与 SQLite `exact_equal=true`；转化侧包含 `Facebook Ads`、`googleadwords_int`、`organic`、`restricted`、`tiktokglobal_int`、`unityads_int` 六个渠道。

### 发布、鉴权和定时器

- 最终验证公开版本：`20260826T174301241241+0800`，由 v7 自然 timer 生成；
- 匿名访问报表与 JSON 均 302 到 `/api/auth/feishu/login?next=/reports/ai-game-performance/`；
- 登录态 access log：HTML、清单、总览/渠道/转化日文件均 200；
- BUG-006 上线后 2026-08-25 17:12:10 自然触发，17:13:19 成功，`Result=success`、`ExecMainStatus=0`；
- timer 为 enabled/active，固定每小时 `:12`、`:42` 触发并附加不超过 30 秒随机延迟；
- `nginx -t`、Nginx、`drama-material-api` 均正常；现有 TT 报表匿名鉴权仍为预期 302。
- v6 只从既有 SQLite 执行 `--publish` 后原子切换 current；未查询 MySQL、未 reload Nginx、未重启主 API 或 TT/X/Meta 服务。

### v6 历史登录态浏览器

- v6 版本刷新后默认总览约 0.67 秒完成（已命中私有日快照缓存）；
- 近 3 天、最低花费聚合后过滤、关键词、分页、三视图与 TikTok 渠道筛选通过；
- 自然刷新后“渠道明细｜渠道”显示“渠道事实 838 行 + 转化事实 48,088 行｜按渠道聚合 6 行”；Google、TikTok、Unity、Facebook、organic、restricted 均完整出现；
- 渠道卡片为渠道花费 51,629.24、手工成本 54,894.47、渠道安装 172,020、测转安装 165,854；Google/TikTok 有效花费未叠加手工成本，Unity 兜底 3,547.86；
- 页面卡片平均时长 1,070.37 秒，与源表/SQLite 一致；
- 390×844：页面无横向溢出，明细表可独立横向滚动；
- v6：游戏总览、渠道明细、转化明细的指标卡、状态、质量提示和表头均不再出现“渠道行/转化行”；仅渠道维度仍为 6 行，内部 14,002/443,363 行守恒未变；
- CSV：点击无页面/控制台业务错误；扩展未暴露 Blob 下载事件，待用户确认下载文件。

## 遗留风险

1. CSV 文件下载成功与内容打开需用户在生产页手点确认；这是任务验收的一部分。
2. 源表没有币种字段，页面按现有上传与渠道源共同币种显示 `$`，需运营保证上传币种一致。
3. 当前 D0/D1 收入为 0，因此 ROAS 为 0；后续源数据出现收入后 timer 会自然刷新。
4. 最终全范围仍有 82 条、22,606.51 花费未归属；页面保留为“未归属”，未做猜测分摊。

## 发布建议

维持当前生产版本并提交用户验收。用户重点检查报表口径、默认 2026-08-24 数字、未归属提示和 CSV 文件；若 v6 展示精简验收不通过，使用部署文档中的非破坏回滚恢复上一运行提交 `98045976290b92ce3d69d030ae45eab45f386760`。

## 平均时长分钟单位热修复测试（2026-08-26）

- 运行提交：`a63293b323f3a24afb5835d89d58e02d63b4139e`；公开版本：`20260826T165257950954+0800`。
- 本地与服务器 20/20 PASS；前端契约、`node --check`、`git diff --check` PASS。
- 行为样例：内部 `1098.31` 秒不变；页面 `18.31 min`；CSV 表头 `平均游戏时长(min)`、值 `18.31`。
- 登录态生产页：指标卡 `18.13 min`；表格样本 `18.31 min`、`20.39 min`、`21.36 min`；可见秒单位计数 0。
- 清单仍记录 `avg_play_duration_seconds=1260.936077`；三视图各 17 个日文件完整，SQLite `quick_check=ok`、`integrity_check=ok`。
- 匿名页面/清单、正确 TT 报表入口均返回预期飞书登录 302；Nginx、`drama-material-api`、timer 正常。
- CSV 实际下载文件仍保留为用户手点验收项；页面和导出代码契约已通过。

## Unity 补数发布测试报告（2026-08-26）

用户发现 BUG-007：Unity 渠道安装/曝光/点击缺失。本节记录从失败优先测试到生产发布、浏览器和自然 timer 的完整证据。

### 已执行

- 新增 8 组后端测试，覆盖 `idx_date/product/date/category=0`、starts 曝光映射、varchar 数值失败关闭、负 ID 共存、同日/跨日维度隔离、overview 守恒和旧缓存 additive migration。
- 扩展 Node 渠道并列聚合，加入 Unity delivery + conversion，验证渠道指标/手工指标各计一次、有效花费仍为手工兜底、CPI 保持 source spend 除渠道安装且 Unity 为 0，以及 `source_country` 分桶不伪 Join。
- 开发中首次运行后端测试共 21 项：20 通过、1 失败。失败项为跨日相同广告键的游戏映射被误判 ambiguous，符合失败优先预期，已交回实现修复；这不是发布 PASS。
- 同轮前端 Node 2/2 通过；仍需在最终实现落定后重跑完整 discovery 与静态契约。

### 本地修复后门禁

1. 同日映射键已修复；Python discovery 27/27、前端 Node 2/2、`py_compile`、前端静态契约与 `git diff --check` 全部通过。
2. 独立 SA 代码评审已确认后端与前端 CPI 公式一致、Unity raw spend 未进入有效花费，并关闭两项阴影发布 P1。
3. 精确 release 独立阴影完成 2026-08-10 至 2026-08-26 全量刷新；Unity category 0 为 6,530 行、安装 98,719、曝光 21,123,305、点击 11,095,086，MySQL→SQLite→JSON 对账一致，category 1 未并入。
4. 生产前完整备份、不可变 release、在线全量刷新和原子发布完成；运行提交 `28cefbb0c6439bea53b243de2595e789002dfa64`，首次版本 `20260826T174015329035+0800`。

### 生产回归

- 2026-08-25 稳定日：Unity 1,188 行、渠道安装 11,847、曝光 2,034,406、点击 1,017,135；源库、生产 SQLite 与版本 JSON 完全一致。
- 登录态 Chrome 按渠道筛选 `unityads_int`：有效花费 `$3,562.51`、渠道花费 `$0`、手工成本 `$3,562.51`、渠道安装 `11,847`、测转安装 `11,655`、曝光 `2,034,406`、点击 `1,017,135`、CTR `50.00%`、CPI `$0`。
- 表头与来源说明包含“渠道安装/曝光/点击”和 `Starts=曝光`；桌面布局通过，390×844 页面无整体横向溢出，明细表仅在自身容器内横向滚动。
- 17:42:25 自然 timer 启动、17:43:13 成功，`Result=success`、`ExecMainStatus=0`，公开版本更新为 `20260826T174301241241+0800`；Unity 合计未漂移。
- SQLite quick/integrity、`nginx -t`、匿名鉴权、TT 报表、主 API 回归通过；未写 MySQL，未 reload Nginx，未重启 AI 主 API 或 TT/X/Meta 服务。

### 当前结论

PASS。Unity 渠道安装、曝光和点击已上线并完成源库/缓存/JSON、生产 UI 与自然 timer 验证；无遗留 P0/P1。CSV 实际下载文件仍按既有验收边界保留为用户手点检查项。
