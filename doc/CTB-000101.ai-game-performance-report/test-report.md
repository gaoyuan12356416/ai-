# 测试报告

## 测试结论

CTB-000101 已完成实现、真实只读数据阴影、生产部署和登录态浏览器回归，可提交用户验收。22 个用例中 21 个通过、0 个失败、1 个保留为用户手点验收：Chrome 扩展不能暴露 Blob 下载事件，但 CSV 点击无页面报错，BOM/CRLF、字段范围和延迟释放契约均已通过。

## 测试范围

- 只读 MySQL Schema/字段、双事实口径和三渠道游戏映射；
- SQLite 日期事务、全量/增量刷新、原子清单和发布审计；
- 游戏总览、渠道明细、转化明细、日期/游戏/渠道/国家/关键词/最低花费/维度/分页；
- Nginx 飞书鉴权、静态私有缓存、systemd timer、共享锁和回滚边界；
- 桌面 Chrome 与 390×844 手机布局；
- 现有 TT 报表及 AI 主 API 进程回归。

## 执行统计

| 类型 | 数量 | 通过 | 失败 | 待用户确认 |
| --- | ---: | ---: | ---: | ---: |
| Python 单元/部署契约 | 18 | 18 | 0 | 0 |
| 前端静态/Node 语法契约 | 1 | 1 | 0 | 0 |
| 标准测试用例 | 22 | 21 | 0 | 1 |
| 生产 MySQL/SQLite 对账组 | 2 | 2 | 0 | 0 |
| 浏览器布局/交互组 | 7 | 6 | 0 | 1 |

## 缺陷情况

| 缺陷 | 严重级别 | 发现阶段 | 结论 |
| --- | --- | --- | --- |
| BUG-001 测试精度/文案假失败 | 低 | 本地 QA | 已修复 |
| BUG-002 release 无法隐式导入只读 MySQL 模块 | 高 | SA 代码评审 | 已修复并经全量刷新验证 |
| BUG-003 MySQL 日期格式返回字面量 | 高 | 首次生产阴影 | 已修复并增加 SQL 回归 |
| BUG-004 总播放时长重复乘安装 | 高 | 真实指标对账 | 已修复；平均时长与源表精确一致 |
| BUG-005 CSV Blob URL 生命周期过短 | 低 | 生产浏览器 | 已加固；下载文件由用户手点确认 |

## 验证证据

### 不可变代码与构建

- 运行提交：`479398bed4a2656a431d571ec3b58d0efd452a88`；
- release：`/opt/ai-game-performance/releases/479398bed4a2656a431d571ec3b58d0efd452a88`；
- `current` 指向该 release，worktree clean；
- 本地及服务器均为 18/18 PASS，`node --check` 与前端契约 PASS。

### 全量容量

- 日期：2026-08-10 至 2026-08-25；
- 首次全量：433,060 条转化事实、13,983 条投放事实、1,959 条总览日组合；
- 全量 SQLite：约 255 MiB，`quick_check=ok`，峰值 RSS 310,036 KiB；
- 全量命令墙钟 9 分 18 秒，其中包含等待现有 TT 共享锁；未并发扫库；
- 最终增量后：435,309 条转化事实、13,990 条投放事实，`quick_check=ok`、`integrity_check=ok`。

### 2026-08-24 精确对账

转化表 MySQL 与 SQLite 完全一致：

- 行数 48,086；成本 54,880.309168；安装 165,849；D1 留存 25,834；
- 总播放时长 177,521,085.491 秒；平均时长 1,070.377786 秒；
- D0/D1 收入当前均为 0，页面如实展示，不伪造 ROAS。

投放表 MySQL 与 SQLite 完全一致：

- Google：625 行，花费 50,740.10，安装 168,687，曝光 83,240,705，点击 1,589,636；
- TikTok：213 行，花费 875.02，安装 3,329，曝光 1,297,036，点击 55,819。

### 发布、鉴权和定时器

- 最终公开版本：`20260825T162241764976+0800`；
- 匿名访问报表与 JSON 均 302 到 `/api/auth/feishu/login?next=/reports/ai-game-performance/`；
- 登录态 access log：HTML、清单、总览/渠道/转化日文件均 200；
- 2026-08-25 16:12:25 自然触发，16:13:17 成功，`Result=success`、`ExecMainStatus=0`；
- 下一次计划 16:42:28，timer 为 enabled/active；
- `nginx -t`、Nginx、`drama-material-api` 均正常；现有 TT 报表匿名鉴权仍为预期 302。

### 登录态浏览器

- 最终版本刷新后默认总览约 0.67 秒完成（已命中私有日快照缓存）；
- 近 3 天、最低花费聚合后过滤、关键词、分页、三视图与 TikTok 渠道筛选通过；
- 登录态回归时的 16:02 快照单日转化明细加载 48,085 条源行并聚合为 8,602 行；最终 16:22 快照已对账为 48,086 条；
- 页面卡片平均时长 1,070.38 秒，与源表/SQLite 一致；
- 390×844：页面无横向溢出，明细表可独立横向滚动；
- CSV：点击无页面/控制台业务错误；扩展未暴露 Blob 下载事件，待用户确认下载文件。

## 遗留风险

1. CSV 文件下载成功与内容打开需用户在生产页手点确认；这是任务验收的一部分。
2. 源表没有币种字段，页面按现有上传与渠道源共同币种显示 `$`，需运营保证上传币种一致。
3. 当前 D0/D1 收入为 0，因此 ROAS 为 0；后续源数据出现收入后 timer 会自然刷新。
4. 最终全范围仍有 82 条、22,569.55 花费未归属；页面保留为“未归属”，未做猜测分摊。

## 发布建议

维持当前生产版本并提交用户验收。用户重点检查报表口径、默认 2026-08-24 数字、未归属提示和 CSV 文件；若不通过，使用部署文档中的非破坏回滚恢复 `2655aaf` 版本。
