# BUG-006 渠道明细按渠道遗漏转化侧渠道

## 发现阶段

用户验收生产报表；日期为 2026-08-24，进入“渠道明细”后仅保留“渠道”维度。

## 现象

页面显示渠道事实 838 行并聚合为 2 行，仅有 `googleadwords_int` 与 `tiktokglobal_int`。同一天手工转化表实际还有 `unityads_int`、`organic`、`restricted`、`Facebook Ads`，运营按渠道查看时误以为大量数据丢失。

## 根因分析

原“渠道明细”严格只加载 `delivery` 日文件，即 `ads_custom_source_insight product='Neonarcade'` 的渠道事实。聚合本身没有丢失 838 条记录，但视图没有同时纳入 `conversion` 日文件，因此仅存在于手工转化侧的渠道不会进入渠道筛选和渠道汇总。

2026-08-24 用户反馈版本的生产只读基线：

- 渠道事实：Google 625 行、TikTok 213 行，共 838 行 / 2 个渠道；
- 转化事实：48,086 行 / 6 个渠道；其中 Google 46,088、Unity 1,299、TikTok 427、organic 175、restricted 59、Facebook 38 行。

## 修复说明

- “渠道明细”同时读取同日 `delivery` 和 `conversion` 两个既有版本文件，不重复发布数据；
- 两类记录转换为指标互斥的并列事实后，再由浏览器按当前维度聚合，绝不做原始行 Join；
- 渠道事实只贡献渠道花费/安装/曝光/点击；转化事实只贡献手工成本/测转安装/D1/时长/收入；
- Google/Meta/TikTok 有效花费仍仅来自渠道源，Unity 仅取手工成本兜底，organic/restricted 为 0；
- 状态区分别展示渠道事实与转化事实行数，表格/卡片增加有效花费、手工成本、测转安装和两类行数。

## 影响范围

仅修改静态前端的渠道视图装载、聚合、提示和 CSV 字段；MySQL 查询、SQLite Schema、定时刷新、Nginx、鉴权及其他两视图不变。普通页面仍不查询 MySQL。

## 验证命令与当前结果

- `python -m unittest discover -s ops/ai-game-performance -p "test_*.py" -v`：19/19 PASS；
- `python ops/ai-game-performance/validate_frontend_contract.py`：PASS；
- 新增 Node 行为测试断言 6 个渠道、Google 不双计、Unity 兜底和两类行数守恒：PASS；
- `python -m py_compile ...`、`git diff --check`：PASS。

## 回归结论

修复已通过 GitHub-first 流程上线：

- GitHub/生产提交：`98045976290b92ce3d69d030ae45eab45f386760`；
- release：`/opt/ai-game-performance/releases/98045976290b92ce3d69d030ae45eab45f386760`；
- 回滚备份：`/mnt/data-disk/ai-game-performance/backups/20260825T170107+0800-pre-9804597`，备份清单校验全部通过；
- 本地与服务器 19/19、前端契约、`node --check`、`nginx -t`、SQLite quick/integrity：PASS；
- 登录态 Chrome：修复发布后已先以反馈快照验证“渠道事实 838 行 + 转化事实 48,086 行｜按渠道聚合 6 行”；六个渠道、各列指标、Unity 单渠道筛选、总览/转化回归通过；
- 390×844：页面宽度无横向溢出，渠道表在自身容器内横向滚动。

17:12 自然 timer 终验通过：

- 17:12:10 启动、17:13:19 成功结束，`Result=success`、`ExecMainStatus=0`；公开版本为 `20260825T171308367560+0800`；
- 2026-08-24 源库与 SQLite 逐字段完全一致：渠道事实 838 行、转化事实 48,088 行；`quick_check=ok`、`integrity_check=ok`；
- 生产页面刷新后状态为“渠道事实 838 行 + 转化事实 48,088 行｜按渠道聚合 6 行”，六个渠道均完整出现；
- Google 渠道花费 50,754.22、TikTok 渠道花费 875.02、Unity 有效花费兜底 3,547.859168；渠道总花费 51,629.24、测转总成本 54,894.469168，未发生双计；
- 当前全量 440,857 条转化事实、13,996 条渠道事实；页面仅在整次刷新成功后替换公开清单。
