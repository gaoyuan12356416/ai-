# 测试用例

## 测试范围

CPU 状态机、账号安全边界、GPU 成片、发布门禁、主后台权限、页面交互、部署单元和线上关闭态健康检查。

## 测试数据

- 临时 SQLite。
- 伪造的安全账号行和占位 Token；测试不得使用真实 Token。
- 本地短视频与固定新版片尾测试副本。
- Mock TikTok creator info/init/status 响应。
- 北京时间边界、并发 claim 和 unknown outcome 数据。

## 用例列表

| 编号 | 场景 | 前置条件 | 步骤 | 预期结果 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-001 | 账号列表安全 DTO | 快照含 Token | 查询列表 | 响应/日志不含 Token | P0 | 待执行 |
| TC-002 | 不合格账号 | Token 过期或禁发 | 创建任务 | 返回 `account_not_eligible` | P0 | 待执行 |
| TC-003 | creator info 无 scope | Mock `scope_not_authorized` | 预检 | fail-close，不显示可发 | P0 | 待执行 |
| TC-004 | 素材解析 | 有效素材 ID | 预览 | 返回真实 `content_id` 和媒体 | P0 | 待执行 |
| TC-005 | 默认可编辑描述模板 | 首次打开页面 | 检查并编辑描述框 | 首屏显示当前模板和 `{{contect_id}}`；无 readonly；读取/切换素材不覆盖编辑内容 | P0 | 待执行 |
| TC-006 | 每素材动态描述 | 两素材映射不同 `content_id` | 用同一编辑模板分别建队 | 两条分别冻结相同模板和各自真实 Drama ID 的最终描述 | P0 | 待执行 |
| TC-007 | 账号设置必需 | 账号未在 TT 个号管理保存设置 | 打开页面并尝试排期 | 发布池只读提示未配置，不能提交 | P0 | 待执行 |
| TC-008 | 账号设置只读冻结 | 账号已有隐私/互动/披露/AIGC 设置 | 打开页面并建队 | 发布池只读展示，服务端忽略旧页面覆盖字段并冻结已保存设置 | P0 | 待执行 |
| TC-009 | 显式同意 | 未勾同意 | 创建任务 | `consent_required` | P0 | 待执行 |
| TC-010 | 时间转换 | 上海时间 | 创建任务 | UTC 落库正确 | P0 | 待执行 |
| TC-011 | 素材全局去重 | 素材已有有效任务 | 并发创建 | 仅一个成功 | P0 | 待执行 |
| TC-012 | 账号时间冲突 | 同账号同一时点 | 并发创建 | 仅一个成功 | P0 | 待执行 |
| TC-013 | 原子 claim | 多 runner | 同时领取 | 单任务只被一个 runner 领取 | P0 | 待执行 |
| TC-014 | 超时不补发 | 超过宽限期 | 执行 runner | 标记 `missed` | P0 | 待执行 |
| TC-015 | init 前取消 | scheduled | 取消 | 成功释放素材 | P0 | 待执行 |
| TC-016 | init 后取消 | 已有 publish_id | 取消 | 拒绝，只允许对账 | P0 | 待执行 |
| TC-017 | unknown 禁重发 | init 结果不明 | runner 重跑 | 不再 init，状态 `needs_review` | P0 | 待执行 |
| TC-018 | 三重门禁关闭 | 任一 gate=0 | 到点执行 | 不调用 init，`blocked_compliance` | P0 | 待执行 |
| TC-019 | GPU 数据盘 | GPU worker | prepare | 只在 `/data/tt-post-publisher` 写工作/成片 | P0 | 待执行 |
| TC-020 | GPU 成片 | 正片+新版片尾 | prepare | 音视频连续，SHA/大小/时长正确 | P0 | 待执行 |
| TC-021 | 动态 Drama ID | 固定片尾含示例 ID | prepare | 真实 ID 清晰，示例被标注教程 | P1 | 待执行 |
| TC-022 | GPU 仅 loopback | 服务启动 | 检查监听 | 仅 `127.0.0.1` | P0 | 待执行 |
| TC-023 | Token 不落盘 | creator/publish mock | 全流程搜索产物和日志 | 无 Token/Authorization | P0 | 待执行 |
| TC-024 | 已有 publish_id 对账 | status mock | reconcile | 只调用 status，不再 init | P0 | 待执行 |
| TC-025 | 页面权限 | 无权限用户 | 打开页面/API | 403 或权限提示 | P0 | 待执行 |
| TC-026 | X 回归 | 现有 X 测试 | 执行回归 | X 发布池无回归 | P0 | 待执行 |
| TC-027 | 去除原 CTA | 39.1 秒样例素材 | 默认 prepare | 去尾 4.333333 秒，旧 CTA 不进入成片 | P0 | 待执行 |
| TC-028 | Logo 和 phone-match | Logo/片尾文件有效 | prepare | 正片 Logo 正确，0.9 秒缩屏叠底过渡，无硬切 | P0 | 待执行 |
| TC-029 | GPU 自行冻结源指纹 | CPU 仅提供 URL | prepare | 下载、SHA和大小均在 `/data` 产生，CPU 不重复下载 | P0 | 待执行 |
| TC-030 | 批量分隔与规范化 | 混合空格、换行、中英文逗号/分号及前导零 | 批量读取 | 1–100 个 ID 按首次出现顺序规范化；重复项只处理一次 | P0 | 待执行 |
| TC-031 | 批量非法输入前置拒绝 | 一项为非数字、0 或超过允许格式 | 点击批量读取 | 整体在首个后台请求前拒绝，输入和模板保留 | P0 | 待执行 |
| TC-032 | 批量数量边界 | 分别输入 1、100、101 个唯一有效 ID | 批量读取 | 1/100 可执行；101 在后台请求前拒绝 | P0 | 待执行 |
| TC-033 | preview 部分失败隔离 | 有效、无效、有效三项 | 批量读取 | 第 1/3 项成功，第 2 项显示安全错误；第 3 项仍被调用和展示 | P0 | 待执行 |
| TC-034 | material-pool 部分失败隔离 | 三项预览成功，第 2 项已有历史 | 批量入池 | 第 1/3 项成功，第 2 项失败；已成功项不回滚，后续项不停止 | P0 | 待执行 |
| TC-035 | 每日时间保存 | 账号已配置，输入 10:00 | 保存设置 | 只保存严格上海 `HH:MM`，返回版本与下一次执行 | P0 | 待执行 |
| TC-036 | 每日自然执行 | 账号 10:00、池中 A/B | 连续两个自然日跑 timer | 第一天消费 A、第二天消费 B；各日只生成一个 run | P0 | 待执行 |
| TC-037 | 每日时间边界 | 00:00、23:59、24:00、带秒或非法值 | 保存设置 | 前两者有效并正确换算 UTC，其余拒绝 | P0 | 待执行 |
| TC-038 | FIFO 与账号隔离 | 两账号交错入池多条素材 | 到点执行 | 每账号独立按 `created_at,id` 顺序消费 | P0 | 待执行 |
| TC-039 | 重复 tick 与并发 | 同时运行两个 runner/重复 minute tick | 领取同一时点 | 唯一 run/queue，仅一条素材变为 reserved | P0 | 待执行 |
| TC-040 | 描述占位符校验 | 分别缺少占位符、使用未知占位符、使用正确拼写别名 | 保存 | 前两者在 GPU 前拒绝；`{{content_id}}` 正常渲染 | P0 | 待执行 |
| TC-041 | UTF-16 描述边界 | 含 emoji 的模板分别渲染为 2200/2201 UTF-16 单位 | 建队 | 2200 成功，2201 在 GPU 前拒绝 | P0 | 待执行 |
| TC-042 | 模板和最终描述不一致 | 同时提交 `caption_template` 与错误 `caption_text` | 建队 | `tt_caption_template_render_mismatch`，不制作、不建队 | P0 | 待执行 |
| TC-043 | 多任务精确幂等重试 | 首次三项均成功，模拟响应丢失；时钟推进至原排期之后 | 保持本批幂等键和确认时间，重放相同逐项请求 | 时间新鲜度校验前返回原三个 queue ID，不增加队列、不重复 GPU prepare | P0 | 待执行 |
| TC-044 | 同幂等键改变模板 | 已存在任务 | 同 key 改模板或渲染文案重试 | Service/Core 都返回 `tt_post_idempotency_conflict` | P0 | 待执行 |
| TC-045 | 历史请求重放 | 历史固定描述、历史自定义 `caption_text`、缺省默认模板任务 | 以原 key/原请求重放 | 原任务逐字返回，不改写模板/文案，不调用 GPU | P0 | 待执行 |
| TC-046 | preview/入池 prepare 复用 | 同素材身份先 preview 再入池 | 比较 GPU job 与调用 | 两阶段命中相同确定性 job/产物；源 URL 或 profile 变化时 job 改变 | P0 | 待执行 |
| TC-047 | 七表增量迁移 | 旧四表真实副本 | 启动新版本两次 | 新增 schedule/pool/run 三表，旧 queue/事件/设置不变且 integrity ok | P1 | 待执行 |
| TC-048 | 批量页面进度与汇总 | 100 项含多种单项失败 | 观察执行全过程 | 持续显示当前项/总数；结束后成功失败数量及逐项结果准确，不泄露凭据 | P1 | 待执行 |
| TC-049 | TT 长素材 | 素材 4665764，2087 秒 | preview | 通过 TT 3600 秒预校验并进入 GPU；X 仍拒绝 141 秒素材 | P0 | 待执行 |
| TC-050 | 目标账号最终时长 | 同一长素材，账号上限 3600/600 | 入池与生成 queue | 3600 账号通过；600 账号明确拒绝且不消费素材 | P0 | 待执行 |
| TC-051 | 手动幂等 | 同一手动 request key 双击/重试 | run-now | 返回同一 run/queue，只消费一条 | P0 | 待执行 |
| TC-052 | 手动额外一条 | 今日自动时点尚未到，池中 A/B | 手动后等待自动时点 | 手动消费 A，自动仍消费 B；每日配置不变 | P0 | 待执行 |
| TC-053 | 手动与定时并发 | run-now 与 minute tick 同时发生 | 执行 | 同账号不会并发发布；两个独立 run 按锁顺序安全消费，不重复素材 | P0 | 待执行 |
| TC-054 | 门禁关闭不消费 | 任一 gate=0，池中有素材 | 自动 tick 与 run-now | 不创建可执行 queue、不改变素材状态、TikTok init=0 | P0 | 待执行 |
| TC-055 | unknown 账号阻断 | 账号已有 unknown queue | 自动 tick 与 run-now | 不领取下一条；其他账号不受影响 | P0 | 待执行 |
| TC-056 | path 唤醒兜底 | run-now 后 path 正常/故障 | 观察 runner | 正常立即唤醒；故障时一分钟 timer 兜底，均不重复 | P1 | 待执行 |

## 回归范围

- X 素材池、短剧池、多时点、账号权限和日志。
- AI 平台登录、导航、素材状态和其他现有 API。
- CPU `socialkit-tiktok-account-sync` 保持只读消费边界。
- GPU 既有 `x-post-media-repair` 服务和 18820 隧道。
- X 素材池原有批量分隔、规范化和去重行为。
- 历史 TT 固定描述任务的查询、取消、执行和幂等重放。

## 2026-07-30 增量测试矩阵（仅本地自动化）

本节用于验收“TT 长素材 + 每日素材池 + 手动立即发布”增量。状态“本地自动化通过”仅表示仓库内隔离测试已通过，不代表生产部署、线上页面或真实 TikTok 发布已经验收。

| 编号 | 场景 | 本地自动化步骤 | 预期结果 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- |
| TC-057 | 素材 4665764 长度合同隔离 | 使用 4665764 的 2087 秒属性构造 TT resolver fixture，同时执行 X 素材选择器回归 | TT 接受 `1..3600` 秒并保留既有安全检查；X 查询参数仍固定 `1..140`，未被 TT 放宽 | P0 | 本地自动化通过 |
| TC-058 | 每日排期与账号级 FIFO | 为账号保存单一上海 `HH:MM`，按顺序加入 A/B，并重复领取同一自然日时点 | 排期版本与下一次时间稳定；同一时点 run 幂等；每个账号独立按 `created_at,id` 消费下一条 | P0 | 本地自动化通过 |
| TC-059 | 固定 600 秒宽限 | 校验 Runner 配置、宽限内重试及超窗任务 | 仅接受 `grace_seconds=600`；宽限内可恢复同一 run，超窗标记 `missed` 且不补发 | P0 | 本地自动化通过 |
| TC-060 | `claim → freeze` 崩溃恢复 | run 已 claim/素材已 reserved 后模拟 `freeze_queue` 进程中断，下一分钟重跑 | 找回同一 claimed run 和同一素材，创建唯一 queue；不生成第二个 run，不重复消费 | P0 | 本地自动化通过 |
| TC-061 | `freeze → bind` 崩溃恢复 | queue 已按 run key 冻结后模拟 bind 中断，并以同一手动请求重试 | 通过 queue 幂等键找回既有 queue 并完成 bind；queue 数量保持 1，素材保持同一归属 | P0 | 本地自动化通过 |
| TC-062 | 手动发布跨刷新、多账号幂等 | 为多个账号分别产生手动意图，写入并重载固定 `sessionStorage` 映射；覆盖成功、明确未发布、`unknown` 和未确认响应 | key 按 `account_id` 隔离；成功/明确未发布只删除对应账号；`unknown`/未确认保留；非法账号、key、状态和超长存储被拒绝 | P0 | 本地自动化通过 |
| TC-063 | Runner 旧队列优先与总领取预算 | 同一 tick 同时提供既有到期 queue 和新 daily due queue | 先 claim/publish 旧队列，再冻结 daily due；仅用剩余 `claim_limit` 二次 claim，单 tick 总领取不超过配置上限 | P0 | 本地自动化通过 |
| TC-064 | 门禁关闭不消费 | 任一 Direct Post gate 关闭时执行自动 due 与手动 run-now | fail-close；不创建可执行 queue、不改变 recurring pool 的 `available` 状态、不调用 TikTok publish init | P0 | 本地自动化通过 |
| TC-065 | 同一 run 并发错误释放与孤儿 queue 防护 | 两个执行者竞争同一 run，覆盖“旧 owner 先释放、后冻结”和“先冻结 queue、后尝试释放”两种时序，并在 120 秒后模拟新 owner 接管 | 每个 run 同时只有一个 120 秒 execution lease；每次接管签发不同 fencing token；`freeze/release/bind` 在事务内核验 run、pool、lease 与 token 身份；过期 owner 不能再冻结、释放或绑定；最终只能是安全释放且无 queue，或唯一 queue 成功绑定，不产生孤儿 queue | P0 | 本地自动化通过 |
| TC-066 | 切换未配置账号不继承上一账号时间 | 先加载已配置且非默认时间的账号，再切换到未配置或仍在加载的账号 | 时间立即恢复默认 `11:00`，不得显示、保存或继承上一账号的时间；加载/未配置状态按页面合同处理 | P1 | 本地自动化通过 |
| TC-067 | 公共兼容 `/queue` 禁止写入 | 在三道门禁关闭状态下，通过主应用公共兼容入口尝试 `POST /api/admin/tt-posts/queue`，并检查查询、取消和手动调和入口 | 主应用精确 `/queue` 白名单仅允许 `GET`，POST 不转发且不能 reserve 素材；既有 GET 查询及动态 `cancel/reconcile` 路由保留 | P1 | 本地自动化通过 |

### 生产验收待填写

- 代码提交与不可变 release：`待填写`
- CPU/GPU 切换前备份与回滚目标：`待填写`
- 生产 SQLite 迁移前后 `integrity_check`、七表清单及行数：`待填写`
- Runner timer/path、固定 600 秒宽限和单 tick 领取预算实测：`待填写`
- 三项 Direct Post 门禁值及“门禁关闭不消费”生产证据：`待填写`
- 素材 4665764 在目标 3600 秒账号上的真实 preview/入池结果：`待填写`
- 公网页面、登录态交互、跨刷新/多账号手动幂等验收：`待填写`
- 本轮验收前后 queue/run/pool 数量与 TikTok 外部请求计数：`待填写`
