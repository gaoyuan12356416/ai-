# SA 代码评审

## 结论

通过。固定多时间与随机日计划共用原有自动发布账本、素材 FIFO、10 分钟安全窗口和认领幂等机制，没有新增绕过发布门禁的执行路径。桌面布局修复仅调整 CSS Grid 与响应式断点，不改变发布行为。

## 评审范围

- `features/tt_posts/core.py`：加法迁移、计划生成、持久化、配置镜像及认领二次校验。
- `features/tt_posts/service.py`：API 校验、固定/随机槽位扫描。
- `static/tt-post-pool.html`：固定多时间编辑、随机次数、计划展示和配置版本冲突保护。
- `static/tt-post-pool.html`：桌面外层单列流、内部配置卡跨列及 1680px 响应式收敛。
- 核心、服务、页面及既有 TT 回归测试。

## 问题清单

| 编号 | 严重级别 | 文件/位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| CR-01 | 高 | 随机计划生成 | 重启或并发 runner 可能得到不同计划 | 使用账号+日期主键、`BEGIN IMMEDIATE` 和持久化快照 | 已关闭 |
| CR-02 | 高 | 自动运行认领 | 仅依赖当前配置版本会误拒绝当天旧计划或接受伪造槽位 | 随机模式按日计划版本和精确时间二次校验 | 已关闭 |
| CR-03 | 中 | 旧数据库迁移 | 加列后历史单时间配置可能失去模式 | 新字段默认 `fixed/0/空日期`，增加旧 schema 重建回归 | 已关闭 |
| CR-04 | 中 | 24 次边界 | 无界重试可能卡住 runner | 使用有界间隔变换、512 次上限及 24 次真实生成测试 | 已关闭 |
| CR-05 | 中 | 页面草稿 | 多时间/模式切换可能绕过原版本冲突保护 | 将数组、模式、次数纳入草稿比较和单次原子保存 | 已关闭 |
| CR-06 | 中 | 页面桌面布局 | `.direct-test-panel` 在无显式列的外层 Grid 上跨两列，形成隐式列并压缩随机配置区 | 取消外层跨列，并在 1680px 及以下将配置区收敛为单列 | 已关闭 |

## 编译 / 验证结果

- Python 编译通过，页面内联 JavaScript 语法检查通过。
- 核心 75、服务 125、页面 36、其他 TT Python 122 项全部通过。
- 2560 至 820px 多宽度、100% 缩放浏览器检查通过；未修改 DOM、事件、API 或随机发布逻辑。
- TT bridge 53 项断言通过；额外生成 4,000 份随机计划验证边界属性。
- `git diff --check` 通过；未调用真实 Creator Info、GPU 发布或 TikTok 发布接口。
