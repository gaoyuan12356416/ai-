# API 文档

本需求不新增或修改 API。

详情继续读取 `GET /api/drama-material/jobs/{job_id}` 的 `youtube_publish_tasks`；前端仅消费既有安全字段：`id`、`title`、`source_kind`、`status`、`video_state`、`comment_status`、`sync_status`、`video_id`、`unknown_outcome`、`error_message`、`created_at_utc`、`updated_at_utc`。

## 接口列表

## 请求/响应

## 错误码

## 兼容性说明
