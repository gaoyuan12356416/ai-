# 测试报告

## 测试结论

通过。发布入队后可见记录、中文状态和自动刷新均符合需求，建议发布。

## 测试范围

两份静态入口、YouTube 弹窗与入队返回详情、状态轮询、封面任务按钮和配方 XSS 回归。

## 执行统计

| 类型 | 数量 | 通过 | 失败 | 阻塞 |
| --- | --- | --- | --- | --- |
| Python 核心回归 | 85 | 85 | 0 | 0 |
| Playwright 浏览器回归 | 4 | 4 | 0 | 0 |
| 静态一致性/差异检查 | 2 | 2 | 0 | 0 |

## 缺陷情况

BUG-001 已修复并关闭。

## 验证证据

- 入队成功后显示 `本次发布 #42` 和 `排队中`。
- 3 秒后模拟接口推进，原记录更新为 `发布成功`。
- 85 项既有合成/YouTube 安全回归全部通过。
- 全程拦截 API，未创建真实发布。

## 遗留风险

生产只做无真实发布的静态文件、服务状态和空队列验证；最终真实任务表现由用户下一次正常发布确认。

## 发布建议

允许发布静态页面；无需数据库迁移或 API 重启。

## 生产验证

- 公网 `drama-synthesis.html` 返回 HTTP 200。
- 公网源码包含 `jobDetailYoutubePublishes` 与发布状态组件。
- 公网响应 SHA-256 与 GitHub release、服务器四个静态文件一致。
- API 与 YouTube Worker 均保持 `active`，队列为 0，未发生测试发布。
