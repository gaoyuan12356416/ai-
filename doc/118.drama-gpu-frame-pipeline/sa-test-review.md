# SA 测试评审

测试覆盖性能、输入像素、时间轴、alpha、故障恢复和实际运行权限。固定输入与输出契约必须同时通过。

修正两类不可靠参考：旧 OpenCL 混合分辨率输出有时间偏移；PyAV 12.3 RGB 转换在本环境未按指定 BT.709 矩阵输出，不能拿它作为颜色基准。硬解像素在 NV12 阶段比较，颜色以固定 FFmpeg 成片和视觉抽帧验证。

不将同时运行生产任务期间的系统 swap 变化归因于单个测试；报告保留原始结果、子进程 RSS 和实际媒体结果。

评审为执行者自审。最终结果见 test-report.md。
