# Test review

Accepted coverage includes actual storage/file boundaries instead of only SQL/string mocks. HTTP checks execute extracted production handler AST, preserving the real Cookie/module/navigation/same-origin logic. Browser mocks isolate writes and include network-loss behavior. Deployment rollback is rehearsed on temporary files and confirms data retention. Production catalog checks validate actual schema/SQL feasibility, separately from offline fixtures.
