# SA 需求评审

## 结论

通过。采用“移除系统 600 秒业务上限、保留 X API 4 小时边界”的可执行口径。

## 评审决策

| 级别 | 问题 | 决策 |
| --- | --- | --- |
| P0 | 仅修改候选 SQL会在 GPU 修复时仍裁成 599 秒 | 同步升级 Premium 修复上限和 profile |
| P0 | 不能从蓝标或页面标签推断会员资格 | 继续以当前账号 Token 的 `subscription_type` 为唯一依据 |
| P1 | 放宽正式素材池可能误放宽自动模板 | 自动模板显式保留 600 秒参数和回归测试 |
| P1 | CPU/GPU策略不一致可能错误裁剪 | profile 从 v3 升到 v4，部署时要求两端同版本 |

## PM 修订确认

以上决策均已写入需求与测试用例。
