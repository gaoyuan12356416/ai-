# SA 代码评审

## 结论

阶段性通过，待完整回归与生产副本验证后签署发布结论。

## 评审范围

- 调度器候选选择与重入。
- 短剧选择器账号范围。
- SQLite 原子计划、FIFO、归属和完成计数。
- 内部 Sidecar 请求/响应兼容性。

## 问题清单

| 编号 | 严重级别 | 文件/位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| CR-01 | 高 | schedule runner | 计划请求仍发送完整账号会与候选数量冲突 | 发送实际候选账号子集 | 已解决 |
| CR-02 | 高 | service | 部分容量错误码会破坏 stale recovery 条件 | 仅用计数和说明审计 | 已解决 |
| CR-03 | 高 | drama store | 子集校验不能禁止未绑定短剧既有合法账号置换 | 仅固定已绑定剧，未绑定剧保留自由账号集合 | 已解决 |
| CR-04 | 中 | material FIFO | 跳过证据必须是当前预检产生 | 保留 `validation_cutoff` 校验 | 已解决 |
| CR-05 | 高 | material runner | `candidate_pool_limit` 被当作总上限，正常素材可能藏在第 101 条以后 | 冻结 `scan_limit` 快照并逐页处理 | 已解决 |
| CR-06 | 高 | material store | 历史可复检错误当前已正常仍被 FIFO 事务拒绝 | 只允许本次 prepared 候选在同一事务清错建队 | 已解决 |

## 编译 / 验证结果

- `py_compile`：通过。
- 修复专项单元/集成：106/106 通过。
- 完整 X 回归：718/718 通过，2 条条件跳过。
- `git diff --check`：通过。
- 独立 QA 额外覆盖前 100 条语言不匹配、前 100 条无 relay、三种可复检错误和跨页确定性，全部通过。
- 新修复的生产副本与自然 timer 证据：待补。
