# 测试报告

## 测试结论

专项、完整 X 回归、生产副本及部署后自然 timer 冒烟均通过。

## 测试范围

Drama selector、schedule runner、material relay、multi-schedule store，以及后续全部 X 发布测试。

## 执行统计

| 类型 | 数量 | 通过 | 失败 | 阻塞 |
| --- | --- | --- | --- | --- |
| 专项单元/集成 | 138 | 138 | 0 | 0 |
| 完整 X 回归 | 715 | 715 | 0 | 0（2 条条件跳过） |
| 生产 DB 副本 | 2 个部分容量场景 | 2 | 0 | 0 |
| 服务器部署前专项 | 138 | 138 | 0 | 0 |
| 服务器部署后专项 | 138 | 138 | 0 | 0 |
| 自然 timer 冒烟 | 11:08、11:10 两轮 | 2 | 0 | 0 |

## 缺陷情况

首轮发现短剧未绑定候选合法账号置换被误收紧，已修正并回归通过。旧的五条“容量不足必须整批失败”断言已按新需求改为部分发布断言。

## 验证证据

- `python -m py_compile ...`：通过。
- 四个专项模块：`Ran 138 tests ... OK`。
- 全部 X 测试：`Ran 715 tests ... OK (skipped=2)`。
- 生产主库备份副本：短剧配置 17 个账号、实际 16 个可用，成功创建 16 条队列；素材配置 19 个账号、仅提供 1 个可用素材，成功创建 1 条队列。两条 run 均保留完整配置范围，`expected_count` 等于实际队列数，`error_code=''`，并写入中文部分容量说明。
- 生产副本 `PRAGMA quick_check=ok`、外键异常 0；未调用真实 X 发布接口。
- 部署前、部署后服务器专项均为 `Ran 138 tests ... OK`。Linux 测试进程只适配单测临时工作目录，生产固定数据盘路径保护未修改；部署后自然 timer 已通过真实生产路径校验。
- 新 release 自然执行：11:08、11:10 schedule 返回 `no_due`，claim 正常、无执行错误。
- 在线主 X Post/X Auto SQLite 均 `quick_check=ok`、外键异常 0、unknown 0；三个常驻服务、六个 timer 均 active。

## 遗留风险

本次未制造计划外真实 Post。部分容量行为已在生产副本和部署后真实 timer 路径验证；下一自然物料/短剧发布点仍由生产账本持续留痕。任何未知结果继续失败关闭。

## 发布建议

已发布，可以保持 timer 正常运行并按自然计划验收实际 X 回执。
