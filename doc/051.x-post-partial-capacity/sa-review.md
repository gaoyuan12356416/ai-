# SA 评审意见

## 结论

通过。采用“完整冻结范围 + 实际队列子集”的模型，无需数据库迁移，且不破坏幂等、去重和未知结果保护。

## 问题清单

| 编号 | 严重级别 | 位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-01 | 高 | 冻结批次 | 直接缩小 `account_ids_json` 会丢失原排程审计范围 | 保留完整范围，仅缩小 `expected_count` 和队列 | 已解决 |
| SA-02 | 高 | 短剧分配 | 首个账号无候选时 `break` 会隐藏后续绑定剧 | 改为 `continue` 并验证有序子集 | 已解决 |
| SA-03 | 高 | 素材 FIFO | 长素材无 relay 时旧规则阻塞后续正常素材 | 记录本轮能力检查并允许继续扫描 | 已解决 |
| SA-04 | 中 | 批次状态 | 将部分容量写成错误码会干扰跨日 stale recovery | 错误码保持空值，用计数和中文说明审计 | 已解决 |

## 决策记录

- 数量不足不再是整批失败条件；零可用候选仍失败。
- 候选必须为配置账号的非空有序子集。
- 存储层按实际队列数完成批次，配置范围不改写。

## PM 修订确认

需求已明确接受部分容量，且要求池内正常内容不得等待。
