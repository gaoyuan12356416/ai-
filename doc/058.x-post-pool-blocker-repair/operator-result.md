# X发布故障处理记录（2026-08-28）

## 363为什么失败

363并没有发布后丢失结果：生产账本为0条队列、0次X写入。真正未确认的是360的队列/日志726，账号19（@timothyjames455）于8月28日约04:06创建帖子时收到X HTTP503，没有取得Post ID，无法判断是否已发出。

旧排期逻辑将配置中的全部账号一并检查，被账号19的历史待核对状态挡住；业务拒绝又被包装为“计划创建结果未确认”，让363看起来像新请求结果丢失。修复后保留真实拒绝原因，已明确阻塞的账号单独隔离，不影响其他健康账号。363历史记录不重建、不伪改成功。

## “需要人工确认”具体是什么

360的19条中，17条成功、1条明确失败、1条结果未知。

- **账号8（@NaughtyLovm57c）**：X明确返回HTTP403临时锁定。q719与更早短剧q533的源帖已经成功，只是目标账号转发失败。需登录X完成平台解锁，再按原源帖核对和恢复转发；不能重发源帖。源帖ID分别为2093063204410683662、2092506119834865718。
- **账号19（@timothyjames455）**：q726是HTTP503后的未知创建结果。核对8月28日约04:06、剧名“小さな魔女ポピー”、素材5890278对应的真实X帖子；如已成功，应以真实Post ID核对落账。无法确认时继续保持待确认，不能清掉unknown直接重发。

普通账号资料接口查询成功，不代表X已解除发布/转发限制。本次没有清除上述三条保护记录，也没有通过重新发帖来试探状态。

## 短剧池为什么被阻塞

17个账号都已绑定未完结短剧。其中16条“下一集”在真正调用X之前因视频尺寸不合规而失败，原绑定和去重占用仍然有效，不能继续推进；另1条是账号8的q533转发锁定。因此356找不到可用的下一集。池中另有11部待发新剧，但不能为绕过失败而中断原剧、换账号或换集。

已上线的修复包括：逐条短剧在X写入前允许一次受控GPU修复，完整核对输出指纹/大小/视频规格，再检查当前账号凭据；下载结束核对实际长度，仅对明确少字节的静默截断最多重新GET三次，不重试X写入；新下载错误可进入既有素材重查流程。

## 16条原队列恢复进度

目标为Run348的q635—647和Run350的q667—669。恢复保留原queue/log、账号、正文、剧集、绑定和转发路径；只替换有完整验证证据的媒体URL，追加不可变审计。

目前正在副本上逐项实际媒体验证，尚未生产恢复。完成后需先副本apply、逐字段验收，再全程持原调度锁进行生产恢复，最后核对原定时器的自然发布结果。恢复入队不等于已发布。

## 已验证版本与证据

- CPU：e300542887fb89314bef145b752c3ad8aa6c5c9c，17:59健康检查通过；Sidecar PID1840134。Linux856/856通过（38.484秒）；Windows856项通过、2项POSIX跳过。
- 香港GPU：170e3b1325b71a72fcd6de913982ce92bb77fa40，保持/data运行路径与PID1780296；CPU18820经香港隧道健康200。未修改美国worker/tunnel的masked状态。
- CPU备份及证据目录：`/mnt/data-disk/x-post-automation/backups/20260828-pool-blockers-155312`，包含各阶段在线备份、deployment-result、Linux测试日志及cpu-verified逐项证明。私有Token不写入报告或Git。
- 部署、回滚和详细用例分别见deploy.md、test-report.md、test-cases.md；开始真实发布后不得整库回滚。
