# 代码评审

## 结论

独立只读评审未发现可证实的P1/P2。复核新增两个helper、媒体准备/二次verify/credentials顺序、实际队列fence、FIFO/短剧归属与relay锁归属。

## 已关闭问题

- 只读available_drama_pool_items错误曾携带unknown标志：已改为精确409，保留全池needs_review保护，真实本地HTTP用例通过。
- schedule内部请求timeout原上限900不足以覆盖GPU准备：独立上限7200，默认及Daily不变，配置测试通过。
- 新错误码未登记：已补中文说明并将新增媒体模块纳入catalog扫描。
- 历史恢复source切换缺少明确拒绝：增加素材ID+URL与原队列完全一致校验和故障用例。

## 首次部署前的生产兼容性基线

Sidecar为18d9cfb；主API共享service.py较旧，差异仅为尚未同步的恢复审计、租约、容量证明等代码，未发现独立新功能。发布前保留其备份，仅同步共享service.py和新增两个helper，不改主API selector/OAuth/app或其他功能。备份副本先执行初始化并确认业务数量不变，再切换代码；历史媒体恢复独立进行，live apply之前必须先在副本演练通过。

后续实际生产版本及验证见deploy.md：CPU现行e300542，香港GPU维持170e3。恢复用运维命令单独从GitHub取出，不再切换服务版本。

## 恢复工具追加评审

- 完整CPU证据wrapper经独立审查和26项相关回归，跨时间复用和先恢复348再验证350均通过；缺失/过期/冲突证据拒绝，reuse-only禁止媒体/GPU回退。
- 独立operator评审关闭audit只验数量的P2，补27个非id字段的完整对应关系；关闭timer部分停止、恢复或查询异常时报告可能丢失的问题，进入apply后异常保留暂停状态，禁止把不确定结果当未应用重跑。
- 真实副本演练发现2.8MB账本超过2MiB证据上限，恢复前即安全停止、0 audit写入；仅用无损压缩封装解决，不提高helper上限或改冻结证据。离线大快照还原和超限拒绝通过。
- 最终operator 386d447（文件SHA d8b2b9b04e0640250d7940172382830a781e4280a92318d14c3a97b808b1a41a）独立审查无剩余可证实P1/P2；copy/live和自然16/16发布均由主代理另行实际核验，不以离线审查替代生产结果。
