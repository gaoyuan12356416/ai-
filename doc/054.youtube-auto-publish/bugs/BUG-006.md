# BUG-006 并发加载覆盖审核详情

工作台加载修复的独立审查发现：详情和列表并发请求时，详情若先返回且不在最近200条列表中，会被之后的列表全量替换移除；较慢列表也可能覆盖较新的审核结果。

修复范围：static/youtube-publish.js 的任务缓存/列表合并与请求顺序隔离。列表与详情需要各自保存，较早发起的请求不得覆盖较新的任务更新。浏览器回归覆盖“详情先返回、任务不在列表”及“旧列表晚于审核成功返回”。该问题在本次上线前处理。
