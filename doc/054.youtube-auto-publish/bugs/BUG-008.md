# BUG-008：自动刷新重建弹窗导致闪屏

2026-09-10 用户提供21.53秒录屏，页面已加载，详情/素材弹窗仍重复淡入。原因是每6秒轮询重新执行列表/详情render，通过innerHTML替换列表和所有弹窗，overlay-in/modal-in重新播放，图片重建。素材搜索、频道迟到回包及成功分支/finally连续render也触发。此前加载和原剧参考图修复未处理这一独立渲染问题。

修复：同模板跳过DOM写入，变化内容采用局部同步，按弹窗key、任务ID、控件身份、媒体URL保留节点；保留滚动、焦点/选区和展开状态。busy状态纳入渲染维度以正确恢复控件。保留6秒轮询、真实状态自动更新、请求版本和abort防覆盖逻辑。

旧版两轮6秒轮询使弹窗/封面/列表节点被替换并触发4次动画，新版节点全部保留、动画0次、320px滚动位置不变。真实进度变化继续展示，素材查询保留选区，迟到回包不重建嵌套视频节点。专项29+既有发布34+参考图43+并发10，共116项浏览器检查通过；独立代码复核通过。部署记录见flicker-fix.md。
