# 验证报告

2026-09-10 离线检查通过：

- 上传及旧流程回归252项（新引擎43、原功能209）。
- 工作流/素材/宏/通知/runtime服务41项。
- HTTP鉴权及真实服务空态16项。
- 真实浏览器mock API交互34项（1366×768、1920×1080）。
- Python编译、Node语法通过；live feature guard 5功能/22文件规则通过。差异格式检查通过。

独立复核的上传未知结果、管理员替代资产归属、校验前短链副作用、素材仅搜索首100条问题均已修复并回归。封面预览、三审核分支、历史深链与错误草稿保留有浏览器截图。

未运行真实YouTube上传、公开、首评、生图或Feishu发送。生产SQL为空，线上验收限真实路由权限/页面/零素材与服务健康；实际外部链路将在用户正常配置素材并提交时执行。部署结果后续追加。


## 生产验证结果

生产Python3.9从GitHub发布目录运行以下命令，全部通过：

- `python3 scripts/test_youtube_auto_engine.py`：43项，2.262秒。
- `python3 scripts/test_youtube_auto_service.py`：41项，2.445秒。
- `python3 scripts/test_youtube_auto_http.py`：16项，0.556秒。

主服务/worker状态、Nginx配置、公网静态摘要和API401、零素材/零新增任务均通过。外部登录浏览器停留在正常飞书登录入口，未代用户扫码；没有把mock交互结果标成真实外部发布成功。

发布结论：开发及SQL预留空态上线通过。待最终SQL后按正常用户创建/审核流程验收真实外部链路。回滚点和命令见deploy.md。

## 2026-09-10 工作台加载修复

素材SQL已按用户最新条件启用，当前状态以material-sql-activation.md为准，前文“SQL为空”仅是首次发布记录。

- service 43项、HTTP 17项通过；新增接口权限、轻量初始化不调用频道/素材SQL、创建时实时校验频道通过。
- 原浏览器34项交互及慢请求专项23项通过。隔离mock路由下，导航永久悬挂时工作台247 ms可见、新建弹窗157 ms；频道12.35秒超时可重试，POST45.44秒超时保留草稿/operation_id，没有自动重复提交。这些是本机受控测试数据，不是生产端到端速度。
- 独立审查发现并修正详情被列表覆盖及自动刷新错误提示丢失，分别记录BUG-006/007；针对性并发回归10项通过，浏览器合计67项。生产数据在loading-performance.md中追加。
- Python编译、Node语法、live feature guard 5功能/22文件规则、diff格式检查通过。

本次没有真实生图、飞书发送、YouTube上传或评论；仅验证页面加载、接口合同、离线业务流程及生产只读结果。

## 2026-09-10 原剧封面参考图

- 后端 service43、HTTP17、剧集关联21、参考工作流9、参考下载及运行适配器25，共115项在CPU Python3.9通过。Linux符号链接验证通过；本地Windows同项因平台原因跳过。
- 浏览器77项通过（既有34+参考图43），包括原剧封面预览、缺图提示/禁止AI提交、本地上传不受影响、详情及审核对照。截图位于`output/playwright/youtube-cover-reference/`，使用mock素材，不能当成真实发布证据。
- 独立代码复核通过；下载总截止覆盖TLS握手，已有宏错误优先级保持兼容。原有加载修复及素材五条件SQL保持不变。
- 隔离实际生图使用素材6617770的原剧封面，耗时141.266秒，输出1672×941 PNG；同一生产图片入库方法在隔离SQLite/资产目录中成功转换为608220字节JPEG。原图SHA保持不变。任务创建、发布引擎与通知回调禁用；没有测试视频/首评/飞书发送。
- 实际参考图调用、生产切换及回读证据见`reference-cover.md`最终记录。仅凭输入提示或生成图存在不足以证明生图工具接收原图。
- 18:17生产切换后，既有真实会话只读GET验证3条素材原剧封面均available，旧任务published/complete，lite bootstrap/materials/tasks/detail均200；公网静态匹配GitHub字节、匿名API401、四服务active/0重启。SQL和6条账本/短链保持不变。CLI内部生图参数未被简化trace记录，具体证据边界见上述文档。

## 2026-09-10 闪屏修复

BUG-008定位为轮询重建DOM重复播放动画。旧版两轮6秒轮询触发4次动画，修复后0次且保留媒体/滚动节点，真实进度变化继续显示。专项29+发布34+原剧参考43+并发10，共116项浏览器检查通过。静态补丁不改后端/SQL，不重启服务；部署证据见flicker-fix.md。
