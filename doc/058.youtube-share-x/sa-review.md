# 方案评审

通过：独立链接分享台账，复用账号鉴权/刷新/锁；不改动 YouTube 上传、首评和自动排期。任务权限先于外部请求，URL 由服务端生成，未知发送永久保留重复围栏。

生产基线包含退休模块、素材上传者筛选、精简列表等组合改动。基线同步与功能提交分开记录，部署精确哈希防止覆盖其他任务改动。

特殊 emoji/URL 计数保守回退，平台响应为最终判断；卡片效果由平台控制。对这些边界在产品上给出简短说明。

## 2026-09-17 标记方案评审

通过：使用独立只读后台探测和有界内存快照，避免列表 N 次同步网络调用。权限过滤先于入队；缓存键包含视频与授权频道身份。过期/失败不沿用绿色，地区或年龄限制单独标记。网络异常只影响标记，不影响上传/首评或原任务状态。

检查结论只影响展示，不能替代入队时重新核验。没有永久数据库标签或固定名单回填，避免视频隐私/卡片变化后仍被误认为可用。
