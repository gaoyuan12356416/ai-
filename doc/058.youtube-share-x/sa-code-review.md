# 代码评审

独立复核覆盖Cookie/双模块/归属、真实YouTube字段和X客户端适配、宏、Sidecar并发/发送/恢复、UI请求生命周期。

BUG-001非法ZWJ计数、BUG-002异常status属性、BUG-003终态存储错误仅重试台账、BUG-004首次明确409恢复编辑均已修复并通过最终回归。部署脚本另修正故障版本可回滚、空触发器列表、停止/恢复异常清理以及先关闭API入口再检查在途分享的切换顺序。

真实桥接→临时Sidecar SQLite→模拟X双账号流程仅发送2次；重放维持同一run/2次尝试。无真实凭据/外部发帖。

2026-09-17 标记增量复核：权限过滤先于缓存 get；get 仅锁内快照/排队，2 个后台线程执行外部读。缓存键包含完整频道/视频身份、待处理项单飞，淘汰不移除在途项；过期不会复用正结果。内部 token 有界且短时复用，不进入 DTO/异常文本，HTTP 不跟随重定向。地区/年龄受限单独标识，原发布/首评/分享台账和 enqueue fresh-check 未改动。部署只覆盖 9 个明确目标，基线与回滚均校验完整哈希，只有主 API 重启。
