# SA 评审意见

## 结论

通过。采用“组合浅复制”代替 Meta deep copy，写入顺序满足 PAUSED → ledger → ACTIVE；V2 与 V3、读库与写库边界清晰。

## 问题清单

| 编号 | 严重级别 | 位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-01 | 高 | Meta copy | deep copy 对象数限制会导致生产失败 | 逐层浅复制并逐个回读 source_* | 已关闭 |
| SA-02 | 高 | 幂等 | POST 超时可能已创建对象 | 持久化 intent，禁止自动重试 | 已关闭 |
| SA-03 | 高 | 落表 | 两库自增 ID 可碰撞 | lineage 使用来源库 + 行 ID | 已关闭 |
| SA-04 | 中 | 调度 | 服务器时间不能代表账号时间 | 候选加载账号时区后逐候选判断 | 已关闭 |

## 决策记录

- V3 独立模块和 runner，不复用 V2 SQLite 状态。
- 复制来源可来自 `kunlunads_dev` 或 `ads_ai`；永不更新来源库 created_data。
- `same_adset` 不静默忽略预算，明确失败。

## PM 修订确认

需求已补充真实执行确认、上线 Canary 和数据库回滚边界。
