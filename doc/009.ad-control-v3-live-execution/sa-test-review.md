# SA 测试用例评审

## 结论

覆盖正常、幂等、隔离、时区、权限和回归边界；生产只允许 PAUSED Canary。

## 覆盖性问题

| 编号 | 场景/用例 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- |
| TR-01 | POST 超时 | 不能证明 Meta 未创建 | 验证不重试并隔离 intent | 已纳入 |
| TR-02 | 多层复制中断 | 需隔离部分对象 | 故障注入每一层并检查 PAUSED | 已纳入 |
| TR-03 | 账号时区 | 跨日窗口与半小时时区 | 固定时间/间隔均测试 | 已纳入 |
| TR-04 | ads_ai 失败 | 不得激活 | 注入事务失败并检查状态 | 已纳入 |

## QA 修订确认

测试报告需记录 commit、备份目录、DDL hash、systemd 状态、Meta Canary ID 和回滚命令。
