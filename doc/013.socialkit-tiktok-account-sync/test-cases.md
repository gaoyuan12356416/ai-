# 测试用例

## 测试范围

覆盖源行校验、固定端点、参数化写入、Token 脱敏、幂等同步、软停用、DDL 与 systemd 定时状态。

## 测试数据

- 单元测试使用虚构账号与虚构 Token。
- 生产 dry-run 仅输出计数，不输出账号名或 Token。
- 生产首次同步预期 24 个活动 TikTok 个号、23 个指标快照、1 个缺指标账号（以执行时实时数据为准）。

## 用例列表

| 编号 | 场景 | 前置条件 | 步骤 | 预期结果 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-001 | 正常源行解析 | 虚构单行 | 调用 normalize | 字段类型正确 | P0 | 通过（单元） |
| TC-002 | 空源保护 | 空列表 | 调用 normalize | 报错且不写目标 | P0 | 通过（单元） |
| TC-003 | 重复主键保护 | 两行同 ID | 调用 normalize | 报错且不写目标 | P0 | 通过（单元） |
| TC-004 | 行数上限 | 超过配置上限 | 调用 normalize | fail closed | P0 | 通过（单元） |
| TC-005 | 写端点保护 | 目标端口 63350 | 校验配置 | 拒绝连接 | P0 | 通过（单元） |
| TC-006 | 目标库保护 | 目标库非 ads_ai | 校验配置 | 拒绝连接 | P0 | 通过（单元） |
| TC-007 | 日志脱敏 | 虚构 Token | 生成 summary | 不含 Token/账号名 | P0 | 通过（单元+生产扫描） |
| TC-008 | 参数化 SQL | 虚构 Token | 检查 SQL 与参数 | SQL 模板不含 Token 原文 | P0 | 通过（单元） |
| TC-009 | 首次生产同步 | 表已创建、密钥已配 | 手工启动 service | 目标活动行与源一致 | P0 | 通过（生产） |
| TC-010 | 幂等重跑 | 首次同步成功 | 再次启动 service | 行数不增加，任务成功 | P0 | 通过（生产） |
| TC-011 | 消失账号停用 | 测试/模拟旧 run_id 行 | 执行同步 | inactive 且 Token 为 NULL | P0 | 通过（单元事务验证） |
| TC-012 | Timer | 单元安装 | `systemctl list-timers` | enabled/active，小时 :05 | P1 | 通过（生产） |
| TC-013 | 密钥权限 | env 已安装 | `stat` | root:root 0600 | P0 | 通过（生产） |

## 生产执行摘要

2026-07-22 完成 dry-run、首次同步、第二次幂等同步、63350 读回、源目标逐字段对账、日志敏感值扫描和主服务/crontab 回归。缺指标账号 ID `122` 按约定保留一行并将七项指标写 0，`has_metric_snapshot=0` 明确标识，不伪装成已获取指标。

## 回归范围

- `drama-material-api.service` 不重启、不改代码，保持 active。
- 现有 crontab 不变。
- 63350 继续只用于读验证，63353 仅执行本表 DDL/同步写。
