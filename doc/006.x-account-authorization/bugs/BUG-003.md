# BUG-003 Token属主与必需权限校验不足

- 严重级别：P1
- 状态：已修复
- 发现阶段：代码评审

## 现象

初版主动校验未要求 `/2/users/me` 返回的用户 ID与记录一致，且环境变量可意外删掉五项必需 scope后仍显示配置完整。

## 修复

- `/2/users/me` 必须返回非空且与记录严格一致的 `x_user_id`，否则标记 `x_identity_mismatch`。
- 将五项权限定义为代码内 `REQUIRED_SCOPES`，环境变量只能保持或追加。
- 增加空/错误用户 ID和缺失必需 scope测试。
