# SA 评审意见

## 结论

通过，可以开发。规则明确且不需要数据库迁移；必须同步修改统计、快照、名称
与执行凭证四个查询位置，避免口径漂移。

## 问题清单

| 编号 | 严重级别 | 位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-001 | P1 | 业务规则 | `status=2` 表注释为“关闭”，纳入发布存在业务风险 | 记录为用户明确决策，并在验收中单独覆盖 | 已接受 |
| SA-002 | P1 | 资格查询 | 只改运行冻结会导致执行时仍取不到 Token | 同时修改 `eligible_credentials()` | 已接受 |
| SA-003 | P2 | 历史运行 | 重写已冻结任务会破坏审计 | 仅对新查询生效，不回填历史 run/task | 已接受 |

## 决策记录

- 唯一硬排除状态为整数 `1`。
- Token 非空、`fb_user_id` 非空、去重和 Graph 失败轮换合同保持不变。
- 不用真实发帖验收。

## PM 修订确认

需求已补充 `status=2`、历史运行、吞吐和无真实发帖边界，可以进入开发。
