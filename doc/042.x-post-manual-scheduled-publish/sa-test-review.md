# SA 测试用例评审

## 结论

通过。用例覆盖到期前零执行、时间/幂等、reservation 并发、失败释放、自动模板兼容和无真实 X 写入。

## 覆盖性问题

| 编号 | 场景/用例 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- |
| TR-001 | TC-004 | 只测 store claim 不能证明 runner 不触发预检 | 增加 execute tick 的 no_pending collaborator 调用计数 | 已补入回归要求 |
| TR-002 | TC-007 | future 校验容易破坏过期幂等重放 | 固定时间推进并断言返回同一 ID | 已覆盖 |
| TR-003 | TC-009/010 | 仅应用层检查不能覆盖跨进程竞争 | 同时测试 partial unique index 和 pool/queue trigger | 已覆盖 |
| TR-004 | TC-014 | 仅看新列不足以证明旧数据不变 | 比较迁移前后旧行字段快照并跑 integrity_check | 已覆盖 |
| TR-005 | TC-019 | 浏览器本地时区可能掩盖 +08 语义 | 静态契约断言与浏览器 smoke 同时验证 | 已覆盖 |

## QA 修订确认

2026-08-12：已将以上建议并入用例与回归范围。
