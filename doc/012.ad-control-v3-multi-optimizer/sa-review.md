# SA 评审意见

## 结论

通过。采用多对多关联表和强身份层白名单，不修改旧主字段，不以 JSON 代替可索引关系，也不放宽姓名冲突。

## 问题清单

| 编号 | 严重级别 | 位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-01 | 高 | 身份解析 | 任意多命中会把同名不同人合并 | 仅精确 user_id/邮箱允许多别名，姓名多命中继续失败 | 已落实 |
| SA-02 | 高 | 执行链路 | 两个别名可能让同一对象执行两次 | 以渠道+账号+层级+对象 ID 去重，跨别名重复阻断 | 已落实 |
| SA-03 | 中 | 持久化 | 仅改主 optimizer_id 无法表达多范围 | 新增关系表，旧字段保留为主优化师 | 已落实 |
| SA-04 | 中 | 兼容上线 | 代码先于建表会导致所有查询失败 | 先建表回填校验，再原子部署代码 | 已落实 |
| SA-05 | 高 | 临时管理员身份 | 管理员路径跳过本人别名，测试期间只会生效一个账号 | 保留管理员全量单选；仅当选中本人别名时扩展完整集合，选他人不扩展 | 已落实 |

## 决策记录

- 主优化师按升序取第一项；王鹏为 387。
- 普通用户自动绑定完整自有别名集合。管理员保持全量单选能力；选中本人任一别名时绑定完整自有集合，选择其他人时仍为单优化师。
- 管理员本人身份解析采用 best-effort，未解析、姓名冲突或超过上限时返回空本人集合，但不影响管理员原有操作。
- 旧规则不自动扩展到新识别别名；仅回填其原主优化师，避免无审计的行为扩大。普通用户保存规则时按当前身份集合更新。
- 生产验收不执行真实暂停/复制。

## PM 修订确认

2026-07-17 用户明确：“命中两个优化师的话这两个都生效。686也是王鹏，是他的小号。”随后要求检查凌云柯，并说明两人的 admin 都是为了测试临时增加。
