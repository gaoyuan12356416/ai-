# SA 代码评审

## 结论

通过。204 项本地自动化检查全部成功，待线上最终只读验证。

## 评审范围

身份可信边界、授权、SQL 参数化与事务、候选去重、执行审计、动态 UI、迁移/回滚顺序。

## 问题清单

| 编号 | 严重级别 | 文件/位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| CR-01 | 高 | service._scan_discover | 跨别名同对象可能执行两次 | 合并键去重并 blocked | 已修复 |
| CR-02 | 高 | repository | 新代码依赖关系表 | 部署强制先迁移，事务内写父子范围 | 已修复 |
| CR-03 | 中 | live_execution | intent 可能只记主优化师 | 改为优先 target.optimizer_id | 已修复 |
| CR-04 | 中 | rollback SQL | 新鲜安装回滚遗漏新表 | 计数并按外键顺序删除 | 已修复 |
| CR-05 | 低 | 普通用户显示名 | 仅整数测试映射会显示 ID | 缺名称时回退登录姓名 | 已修复 |
| CR-06 | 中 | 生产备份 | 超长 SSH 内联命令容易被跨 shell 转义破坏 | 使用已提交、可审查且限制数据盘路径的 checkpoint 脚本 | 已修复 |
| CR-07 | 高 | service 管理员分支 | 临时 admin 不会解析本人别名，选择小号只生效一个范围 | 增加 best-effort 本人别名，并仅在选中本人时扩展 | 已修复 |

## 编译 / 验证结果

- Python py_compile：通过。
- Node `--check app.js`：通过。
- `git diff --check`：通过。
- checkpoint shell `bash -n` 与越界路径拒绝：通过。
- 全部 V3 自动化 204/204，其中核心服务 67/67、动态 UI 契约 32/32：通过；最终统计见测试报告。
