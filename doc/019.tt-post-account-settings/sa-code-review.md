# SA 代码评审

## 评审结论

代码复核与生产关闭态验收通过。当前实现符合设计主路径，无需修改 GPU 代码或既有队列表结构。

## 已核对项

- 新表仅保存非敏感账号设置。
- Core 对字段集合、布尔值、隐私枚举和版本做严格校验。
- Service 保存与入池均实时读取 `creator_info`。
- 入池策略只由已保存账号配置生成。
- 幂等重放不因当前账号配置变化而冲突或重制。
- AI 后台按独立导航项鉴权，并记录不含凭据的审计摘要。
- 发布池不再呈现账号设置输入控件。
- 个号管理页不使用 HTML 注入，不出现 Token 字段。

## 最终检查清单

- [x] 全部自动化测试通过（229/229）
- [x] Python 与 JavaScript 语法检查通过
- [x] `git diff --check` 通过
- [x] 分支基于最新发布池提交
- [x] 生产备份、自动回滚演练、健康检查与最终 release 指针核对完成
- [x] 浏览器只读联动验收完成
- [x] 账号设置与任务队列均维持 0，发布门禁全关闭，未发送 TikTok 发布请求

## 生产复核补充

- 首次切换后健康请求早于 `Type=simple` 服务就绪，部署脚本按预案自动回滚到旧 release；日志未发现启动错误。随后改为最长 30 秒的就绪轮询重新部署成功，证明回滚路径可用。
- Nginx 使用精简导航配置，部署时只原子新增 `ttAccountSettings` 并调整 `ttPostPool` 顺序，没有覆盖其他导航项。
- 页面和服务静态文件哈希一致；未授权调用新 API 返回 401，路由存在且受保护。
- 个号管理页只保存 AI 后台的账号发布策略，不反向修改 TikTok App 设置；保存与入池都重新校验实时 `creator_info`。
