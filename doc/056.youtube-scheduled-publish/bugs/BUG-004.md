# BUG-004：历史剧集关联测试 Mock 缺少数据库路径

- 级别：测试 fixture 问题；生产代码不变。
- 复现：聚合与独立 scripts/test_youtube_drama_association.py 均在 test_runtime_short_link_adapter_accepts_standalone_and_synthesis 报 Path(Mock) TypeError。
- 根因：旧 Mock app 只给 PUBLIC_BASE_URL/DB_NAME，没有提供后来 ChannelFailures 初始化需要的 JOB_DB_PATH。runtime/channels 相关实现未在本次修改。
- 修复：Mock 显式 JOB_DB_PATH=str(self.root / 'jobs.sqlite3')。
- 验证：独立剧集关联 21/21；冻结后完整回归 507 次执行、506 通过、1 OS 能力跳过，无失败。
- 状态：已修复，仅测试文件变更。
