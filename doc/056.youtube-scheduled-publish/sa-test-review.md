# SA 测试用例评审

2026-09-11：已独立检查 test-cases.md。覆盖留空/未来值、审核与处理过期、幂等、原生预约确认、修改/取消的租约与版本竞争、未知视频保护、时区、提醒和首评依赖。

执行要求：以请求计数和冻结的视频 ID 验证无重复写入；用固定时钟制造“创建时未来、写入时已过期”；用两个独立数据库连接/线程覆盖真实 CAS，而不只模拟布尔返回值。平台超时与 items=[] 必须分开测试。浏览器用例须检查 payload 的 UTC 值及实际 UI，不能只查源码关键词。

真实平台调度与取消无法由离线假平台证明，部署验收应明确区分静态/服务健康、只读已有任务核验与真实预约发布；本次不得为验收创建生产视频或发送消息。

## 结论

用例设计通过；实际执行结果单独记录，不能据此宣称功能已通过。

## 覆盖性问题

| 编号 | 场景/用例 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- |
| STR-01 | TC-06/09 | 网络请求期间可能跨过预约时间。 | 写入前校验考虑实际 HTTP 超时；增加安全窗口内不会调用平台 PUT 的断言。 | 纳入 |
| STR-02 | TC-15 | 单线程 mock 不证明真实 SQLite 竞争。 | 至少覆盖版本过期与持有有效 worker lease 的拒绝路径；有条件时用两个真实连接竞争。 | 纳入 |
| STR-03 | TC-18 | 只测试准备过期可能遗漏已入账本任务。 | 准备与 ledger 两种错过时间都进入同一可靠异常提醒路径，重复巡检不新建事件。 | 纳入 |

## QA 修订确认

22 个用例已涵盖上述要求；待实现完成执行并标注实际证据和剩余限制。
