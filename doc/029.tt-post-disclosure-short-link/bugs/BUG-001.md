# BUG-001 自动发布继承品牌披露且短链不可读

## 现象

自动任务可能显示“自有品牌披露”，短链使用 19 位数字。

## 根因

自动 queue 直接冻结账号披露设置；TT 为避开 X 纯数字路径使用大整数命名空间，但没有采用独立短路径前缀。

## 修复

自动 queue 冻结和最终发布双重关闭披露；新链接改为 `/s2l/tt/{queue_id}.html`，保留旧 TT/X 路由。

## 状态

本地修复完成，347 个 Python 测试和 53 个 Node 断言通过，待生产部署验证。
