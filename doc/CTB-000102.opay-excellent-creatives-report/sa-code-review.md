# SA 代码评审

## 2026-08-27 Google CPC 增量独立评审结果

运行代码 `7cea6648fe58e9f24c6c1c2fd00ddc91e7242bed` 的选优公式、平台分母、跨线及并列、严格CPC/CTR边界、完整USD覆盖、B的全量PIC/VID基准经非实现者复核通过。映射路径另由非映射实现者复核，27项桥接测试通过，无阻断项。Meta/TT仍走原规则。

独立变异测试发现验收器字段覆盖不足与零平台金额边界差异，已记BUG-GCP-002并修复，新增17项测试；148后端/46前端通过。正式放行仍须真实七个月缓存、独立July源库核验和公开页面验收，详见后续发布记录。此次未修改个人技能或长期记忆。

## 结论

2026-08-26 最终代码评审通过。2026-07 数据回归、2026-01 至 2026-07 历史回填、生产发布和回滚演练均已完成，无未关闭的 P0/P1 缺陷。

## 评审范围

- Python 数据读取、缓存、选优、媒体和发布提交点。
- 关键词配置/覆盖、静态前端、Nginx、systemd 和安全边界。
- 单元、契约、故障注入和生产回填证据。

## 问题清单

| 编号 | 严重级别 | 文件/位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| CR-001 | 高 | systemd service | 初稿误复用旧 TT 报表 SQL 锁，可能造成无关任务互相跳过 | 改为独立 `/tmp/opay-excellent-creatives.lock`，增加契约测试 | 已修复 |
| CR-002 | 高 | Nginx location | 若父级 server 存在鉴权指令，新 location 可能继承并破坏匿名访问 | 五个 location 均显式 `auth_request off`、`auth_basic off` | 已修复 |
| CR-003 | 中 | 素材维度缓存 | 初稿仅加载缓存中不存在的素材维度，初版到终版之间修正的制作者、封面或 URL 不会更新 | 每次月度刷新对当月出现的维度各重载一次，事实缓存仍按日增量 | 已修复 |
| CR-004 | 中 | 冻结返回与 SQLite | 开发自测发现冻结状态字段合并顺序错误，Windows 测试中文件句柄未及时关闭 | 调整返回字段顺序并使用显式 closing | 已修复 |
| CR-005 | 低 | 前端验证/图标 | 初稿契约验证包含无必要字面量且浏览器请求缺失 favicon | 修正契约断言并内嵌 data favicon | 已修复 |
| CR-006 | 高 | MySQL 只读护栏 | 影子运行发现 63350 代理后的 `@@port` 为后端端口 25437，初稿把后端端口误当入口端口并拒绝了合法只读连接 | 从脱敏后的 MySQL CLI 参数验证入口 `-P63350`，并独立要求 `@@read_only=1` | 已修复 |
| CR-007 | 中 | 视频代表帧 | 生产机没有 FFmpeg，部分无 cover 视频只能显示异常状态 | 增加有超时上限的 OpenCV 子进程回退，失败继续按单行降级 | 已修复 |

## 编译 / 验证结果

- `python -m py_compile ...`：通过。
- `python -m unittest discover ...`：25/25 通过（含固化 2026-07 回归签名和视频回退契约）。
- `validate_frontend_contract.py`：通过。
- Playwright 桌面与 390×844：筛选、证据、视频预览、CSV 均通过；控制台 0 error / 0 warning。
- 本地预览夹具已删除，不进入 Git 提交。
- 生产影子：2026-07 生成 26 条，复合键 26 个，歧义广告日 0；Meta/TikTok 产出，Google 0 估算。
- 独立只读 SQL：六个渠道/App 平台消耗与 AF D0 汇总均与 JSON 一致；素材 4566082、4759574 的严格渠道粒度指标与 AF D0 一致。
- 生产回填：2026-01 至 2026-07 共 186 行，95 个唯一素材 ID；源文件和缩略图均为 186/186 可用。
- 生产回滚演练：两个新 timer 可停止/禁用并恢复为 enabled/active；演练后页面 200，`nginx -t` 通过。

## V2 代码评审记录（2026-08-27）

上述结论均属 V1 历史记录。V2 范围为 GG 素材级事实、历史 USD、六项指标和冻结缓存升级，不扩大其他报表或调度范围。

| 编号 | 级别 | 问题/风险 | 修正与验证 |
| --- | --- | --- | --- |
| V2-CR-001 | 中 | APM 初稿仅显示两位小数，0.0585 被舍入为 0.06 | 主表/详情固定四位，CSV保留原始数值；新增 0.0585、0.05856789、真实0的行为测试 |
| V2-CR-002 | 高 | 缺 Campaign 账户日时不能使用不完整平台 CTR 评优 | 基准账户日完整性门禁，缺失则平台指标 null、规则 B 暂停；独立测试覆盖 |
| V2-CR-003 | 高 | 不可就地升级 V1，或由 GG 回填改变 Meta/TT | 原库只读在线 clone、原子无覆盖提交；冻结签名门禁、旧行/基准/审计保留，仅增 metrics |
| V2-CR-004 | 中 | 仅平台历史 FX 缺失时，原审计文案会显示“0条素材日缺失”而无法说明平台留空 | 分开披露素材与 Campaign 日缺口，新增平台原币缺口审计；平台USD/CPA/coverage留空但完整CTR仍可用于B |
| V2-CR-005 | 高 | 独立评审发现一条合法链会掩盖同资产另一条非法 source 链 | 所有候选链必须合法且指向同一真实素材；任一不符即排除，原始四字段链存入缓存审计；新增混合链测试 |
| V2-CR-006 | 中 | 独立评审发现某历史 FX 候选为空时直接报错，掩盖另一可核验候选 | 缺失/无效候选跳过；没有唯一可核验候选则账户日降级；缺失历史USD也进入缺口，非整月异常 |
| V2-CR-007 | 中 | 独立评审发现平台 conversions 的整数限制会拒绝小数 | 接受有限非负小数，缓存支持REAL，Decimal月汇总后输出有限JSON number；不进入安装/AF或评优 |

- 本地及服务器后端 53 项测试通过；前端 36 项行为测试通过。
- 服务器 7 月隔离试算运行提交 `308d712e9163f3d5d662ad10776f1490708f46fe`：原 26 行不变，新增 GG NG 2 行；两个素材只读 SQL、唯一映射与历史汇率逐日核验通过。
- 旧 `platform_daily/af_daily/material_daily/daily_audit/ads_source_dim` 与 V2 副本逐行哈希一致；旧缓存仍为 schema 1。
- 独立评审、全历史独立验证与正式发布结果在本节后续记录；本条不代表 V2 已上线。
- 针对独立评审修复后，本地全量 90/90（含独立产物验收器32项）及前端36/36通过；服务器将按修正后的精确GitHub提交重新读取GG并验证，原试算产物不直接推广。
- 独立复测已关闭 V2-CR-005/006/007：8组混合映射端到端变体均排除；缺失FX候选和历史USD按约定降级；小数conversions在新REAL/旧INTEGER-affinity缓存关闭重开及月度汇总后保持有限数值。独立评审未发现剩余P0/P1/P2阻断项，可进入隔离回填和发布验收。

### V2 最终刷新维度复测

- V2-CR-008（中）：常规3日/5日刷新需更新GG独占素材的制作者/封面。新增 `refresh_dimensions`，由主入口仅在非 `google_only` 时开启，与Meta/TT共享本次已刷新ID集合；历史升级仍仅补缺失维度。
- 本地全量93/93；独立复测5/5及真实SQLite主入口验证通过：Google-only不修改已有维度且跳过Meta/TT/AF，常规刷新每个素材一次，下一次可继续同步后续修正。
- 修订后的全历史影子版本 `20260827T115804942528+0800` 独立七个月验收通过：原186条Meta/TT原有字段、基准和审计完全一致，仅增加metrics；GG新增6条月度记录。最后维度修正不改变Google-only路径及上述历史快照。

## Google CPC / 图片视频 CTR 前端自查交接（2026-08-27）

本节仅为实现者自查，不冒充独立SA/QA评审；上述V1/V2通过结论保持历史效力。本执行者只改report.html、validate_frontend_contract.py和本目录Markdown，未修改其他协作者Python、未提交/部署。

- 页面消费 `material_cpc/platform_cpc/rule_a_metric/platform_ctr_scope/rule_a_unavailable_reason`，平台CTR原列保持但Google含义明确为PIC+VID；Campaign原CTR在独立参考项保留。CPC显式null不从旧值/消耗反推，素材CPC缺失留空；仅平台CPC缺键可读同App Google benchmark.cpc。
- Google A不可用原因按原文安全 `textContent` 展示；A/B/A+B不在浏览器重算。Meta/TT仍AF CPA，原25列表格、六项metrics及CSV前31列不变，新3列追加末尾；原始精度/BOM/引号/空值/预览路径保留。
- 新HTML读取旧manifest/月数据时按历史规则说明，不把旧B-only数据标成CPC政策；版本提交仍以latest为准。
- 自查并修复验证器编辑过程中的括号语法错误，修正后完整前端46/46通过；未遗留该错误。独立后端/映射代码评审、真实原缓存全候选重算、所有Meta/TT字段/旧表哈希检查和浏览器测试均待后续独立QA追加结果。
