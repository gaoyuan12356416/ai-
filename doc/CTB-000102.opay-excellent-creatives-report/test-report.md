# 测试报告

## 测试结论

2026-08-26 验收通过，可发布。32/32 计划用例通过，25/25 自动测试通过，无未关闭的 P0/P1 缺陷。

## 测试范围

覆盖 `test-cases.md` 中的规则边界、产品范围、平台映射、AF 严格 D0、关键词、素材媒体、制作者、缓存与原子发布、初终版冻结、前端、定时器、历史回填和旧系统回归。

## 执行统计

| 类型 | 数量 | 通过 | 失败 | 阻塞 |
| --- | ---: | ---: | ---: | ---: |
| 计划验收用例 | 32 | 32 | 0 | 0 |
| Python 单元/契约测试 | 25 | 25 | 0 | 0 |
| 前端静态契约 | 1 | 1 | 0 | 0 |
| 2026-07 冻结快照回归 | 1 | 1 | 0 | 0 |
| 生产浏览器桌面/移动端 | 2 | 2 | 0 | 0 |

## 生产版本与数据

- 运行提交：`0cba014b56f1c6394a9d0d3be5d735a370f83659`。
- Release：`/opt/opay-excellent-creatives/releases/0cba014b56f1c6394a9d0d3be5d735a370f83659`。
- 数据版本：`20260826T184515557171+0800`；`latest.json` SHA-256 为 `7b272cb2ea01e8a1ac9da0361d124ff517022bea0867a29cbf07cf3decb4d0dc`。
- 已回填 2026-01 至 2026-07，月行数依次为 34、26、18、23、32、27、26，共 186 行、95 个唯一 `custom_source.id`。
- 186/186 源文件可用，186/186 缩略图可用；素材制作者“未登记”为 0。
- Google 14 个“月份 × App”审计卡均为 0 入选且明确展示缺口，没有估算行；Meta/TikTok 均有严格映射结果。

## 数据对账

- 2026-07 生产结果 26 行：Meta NG 12、Meta PK 4、TikTok NG 10；多素材歧义广告日为 0。
- 只读 SQL 对账通过：六个“渠道 × App”的平台消耗、曝光、点击、安装及 AF D0 汇总与发布 JSON 一致。
- 严格素材抽样通过：Meta NG 素材 `4566082` 消耗 `$8,152.44`、曝光 `9,685,932`、点击 `828,503`、安装 `12,365`、AF D0 `3,910`；TikTok NG 素材 `4759574` 消耗 `$15,501.48`、曝光 `22,663,199`、点击 `132,572`、安装 `6,337`、AF D0 `6,479`。
- 所有 186 行均回查为 OPay/Opay 素材产品；OperaNews 等无关产品计数为 0。

## 页面与运行验收

- [公开报表](https://ai.yingliangads.com/reports/opay-excellent-creatives/) 匿名 HTTP/2 200，无飞书跳转；HTML/JSON 均返回 `X-Robots-Tag: noindex,nofollow`。
- `latest.json` 为 `no-store`，版本月文件为 immutable；无斜杠路径 301 到规范路径。
- 月份切换、渠道/App/类型/关键词/制作者/规则筛选、证据详情、图片/视频弹窗和源文件链接均通过。
- 生产视频预览返回 HTTP 206 Partial Content；CSV 导出为 27 行（1 行表头 + 26 行当前筛选数据），字段及 UTF-8 中文正确。
- 1440×1000 桌面和 390×844 移动端视觉检查通过；浏览器控制台 0 error / 0 warning。
- 两个 timer 均 enabled/active；停止/禁用后再启用的可逆演练通过，页面保持 200，`nginx -t` 通过。
- 旧 AI Game Performance URL 仍按发布前行为 302 到飞书登录；主服务和 8787 监听未受影响。

## 缺陷情况

- `BUG-0001`（P0）：只读代理入口端口护栏误判。已修复并完成本地、服务器和生产影子回归。
- 无其他未关闭缺陷。

## 遗留风险

- Google 素材级精确数据契约仍不足，V1 按批准口径只展示覆盖缺口、不生成估算素材行。
- 69 行关键词状态为“待补关键词”，不影响绩效入选；可通过版本化关键词或 `custom_source.id` 人工覆盖配置补齐。
- 生产事实未提供更早的成功发布时间时，首次上线日期按最早实际投放日回退，详情字段标记为 `first_delivery`。
- 报表公开，`noindex` 不是访问控制，任何获得链接的人均可查看素材和制作者。

## 发布建议

通过。保持终版冻结和显式 `--rebuild` 约束，按北京时间每月 3 日初版、5 日终版运行。

## V2 验证记录（2026-08-27）

上文为 V1 发布证据，以下单独记录 V2；未完成的全历史、浏览器和正式发布步骤不计为通过。

- 本地后端 53/53，前端行为 36/36；包括 APM 固定四位与 CSV 原始数值一致、null/0/∞、平台USD缺失但CTR完整、平台账户日缺失停B。
- 服务器 Python 3.9 后端 53/53；只读 MySQL入口63350、`@@read_only=1`。
- 7月隔离 canary 单元 `opay-google-v2-canary-308d712.service` 成功退出0；28行=原26行+Google NG2行。原线上current和latest未切换。
- 原事实表逐行一致：platform_daily 1,272；af_daily 299,206；material_daily 373,324；daily_audit 1,272；ads_source_dim 4,137。旧cache保持schema1。

### GG 只读 SQL 抽样

从 `ads_google_resource_mapping` + `ads_source` 独立复核唯一 custom_source，再查询 `ads_google_insights type=3`；分账户日核验历史汇率候选确能复现 `spend/spend_usd`，不以缓存计算函数自证。

| custom_source.id | 资产资源数 | 账户日数 | USD消耗 | 曝光 | 点击 | 平台转化数 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 2072578 | 1 | 31 | 16643.90 | 3554565 | 544644 | 19830 |
| 3393516 | 4 | 106 | 5056.98 | 1397087 | 143438 | 6204 |

两行均仅按B入选；安装、AF D0首交、CPA/APM/CVR/安装首交转化率保持null。平台转化数只在详情出现。

7月NG平台另有账号9969655472的10条Campaign日记录（7月2—11日）缺少可核验历史FX，原币NGN合计13802269.035107，不能使用其他账户或当前汇率补齐。因此平台USD总额/CPA/覆盖率null；完整type0曝光点击仍构成可用CTR基准。
