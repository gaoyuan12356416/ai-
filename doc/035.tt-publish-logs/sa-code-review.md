# SA 代码评审

## 结论

通过。未发现阻塞合并的问题；旧发布 sidecar、发布状态机、定时器和数据库结构均无变更。

## 评审范围

- 两个 SQLite 账本的只读投影与跨来源排序、统计、分页。
- 主 API 白名单、导航权限和浏览器安全字段。
- 新日志页、旧发布池日志移除及原操作入口保留。
- 新增测试、既有 TT Post/自动发布回归和需求文档。

## 问题清单

| 编号 | 严重级别 | 文件/位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| CR-001 | 中 | `legacy_reader.py` 状态白名单 | 全来源筛选 `no_candidate` 会被旧账本提前拒绝，无法看到自动发布无候选记录 | 旧账本接受该统一状态并自然返回 0 条，增加跨账本回归 | 已修复 |
| CR-002 | 低 | 跨账本时间排序 | 旧账本使用 `Z`、新账本使用 `+00:00`，直接字符串排序不应承担格式兼容 | 解析为 UTC 时间戳后全局排序，非法历史值稳定沉底 | 已修复 |
| CR-003 | 低 | 页面顶部刷新 | 共用顶栏已绑定整页刷新，新页面重复绑定日志加载 | 移除重复监听，仅保留面板内“刷新日志” | 已修复 |

## 编译 / 验证结果

- Python 编译、两个 JavaScript 语法检查、`git diff --check` 通过。
- 统一日志专项 12 项通过。
- TT Post、TT 自动发布及应用契约合计 339 项回归通过。
- Playwright 桌面、来源筛选、自动任务详情和 390×844 窄屏验收通过，控制台 0 错误。

## 2026-08-07 4 位码增量复核

独立复核未发现遗留 P0/P1。复核提出的加固项均已关闭：历史回填只接受冻结的 `AIpost` 归因；分配前检查全局 4 位码容量并禁止回收旧 route；页面和共享路由读取只接受原样合法的大写 `[A-Z0-9]{4}`；备份文件使用独占创建且从创建时即为 `0600`。

回填继续保持明确 queue ID、expected count/hash、`BEGIN IMMEDIATE`、守卫更新和整批回滚；默认 dry-run 不写数据库，`tt_post_direct_test` 不在范围内。
