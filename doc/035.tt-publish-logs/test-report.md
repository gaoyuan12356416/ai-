# 测试报告

## 测试结论

通过，已部署生产；本次未执行真实 TikTok 发布。

## 测试范围

统一日志服务、双账本只读查询、来源/触发/状态/日期筛选、全局分页、安全脱敏、主 API 代理、导航、新页面、旧发布池瘦身，以及 TT Post/TT 自动发布全回归。

## 执行统计

| 类型 | 数量 | 通过 | 失败 | 阻塞 |
| --- | --- | --- | --- | --- |
| 统一日志专项自动化 | 13 | 13 | 0 | 0 |
| TT 相关完整回归（含专项） | 531 | 531 | 0 | 0 |
| 编译/语法/差异检查 | 4 | 4 | 0 | 0 |
| 生产浏览器/接口场景验收 | 6 | 6 | 0 | 0 |

## 缺陷情况

开发期发现并关闭 1 个统一状态兼容缺陷，见 `bugs/BUG-001.md`；代码终审另修正时间格式排序和重复刷新监听，无遗留阻塞缺陷。

## 验证证据

- 最终 Windows 全量：`Ran 531 tests ... OK`；统一日志专项：`Ran 13 tests ... OK`。
- Linux runtime 兼容回归 529/529；最终 `74ad563` 与该 runtime 逐文件一致，新增 Nginx/UI 专项 6/6。
- 页面实际显示两类来源、四种触发方式、统计、筛选、详情和来源查询参数。
- 自动任务详情成功读取原运行事件；390×844 页面可访问，表格保留横向滚动；浏览器控制台 0 error / 0 warning。
- 旧发布池静态契约确认无日志 DOM、无 `/tasks` 请求，素材池与立即测试业务控件仍在。
- 生产登录态页面显示总计 62、素材池 61、自动发布 1，首屏 20 行；旧发布池登录态确认日志 DOM 为 0，新入口正确。
- 新页面公网返回 200 和 `no-cache, no-store`，主 API、自动 sidecar、code broker、runner/scheduler/metric 与旧 TT 服务均为 active，近 5 分钟 warning 为 0。

## 遗留风险

- 两账本默认聚合采用失败关闭；任一账本不可读时“全部来源”页面会明确失败，需修复账本后恢复。
- 移动端沿用后台全局导航布局，长导航会出现在内容之前；未在本需求中调整公共导航。
- 未在生产数据规模上做压测，因此发布日志 offset 限制为 10,000，单页最多 200。

## 发布结果

已按 `deploy.md` 从 GitHub 不可变提交 `74ad5639bb90cd0c1d6777a1fc3862e0f063ecec` 发布，备份与回滚点已记录。数据库无迁移，旧 TT 发布后端未重启，自动发布三重门禁保持关闭。
