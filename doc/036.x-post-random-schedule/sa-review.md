# SA 需求评审

结论：通过。

- “次数”定义为每日批次数，一批对每个所选账号各发布一条，页面明确展示总条数。
- 随机结果必须入库，避免重启和轮询导致同一日重新抽取。
- 修改随机配置次日生效，避免当天补发和编辑时碰撞正在冻结的分钟。
- 两池共用账号时按相同分钟避碰；不同账号允许各自执行。
- 冻结模板中允许 `{{url}}`，实际短链仍在日志预留后生成，不提前伪造。
- 迁移保持旧配置为 `fixed`，不扩大本次变更到真实发布验证。
