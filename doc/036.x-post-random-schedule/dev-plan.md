# 开发计划

1. 扩展 SQLite 配置、运行批次和每日随机计划表，保证可重复迁移。
2. 实现随机时间生成、次日生效、持久化、跨池避碰和冻结快照。
3. 扩展主后端转发、错误映射和脱敏审计。
4. 更新素材池和短剧池配置 UI、摘要及计划预览。
5. 增加存储、接口、UI、随机上限和 `{{url}}` 渲染回归。
6. 浏览器验证后提交 GitHub，备份并按精确提交部署；不触发真实发帖。
