# BUG-001 单个Video汇总异常中断整批

状态：已定位、修复并上线。运行提交8798f8b098c3cd5f2e0b881c6a196399f704bff6；本地/服务器293项回归及生产只读验收通过。

根因：Service用set <= tuple判断成功状态，抛TypeError，外层标记ledger_or_worker_interrupted；首条Video已记录账户结果，之后对象没有机会执行。旧单Video测试未核对run状态，漏过该异常。

修复：使用issubset；增加3个独立Video测试和真实Graph/SQLite整链路回归。补充明确invalid video ID后的完整账户缺失确认，只有证据完整才already_deleted，失败保留并继续。

更改Graph、Service、对应tests和窄部署脚本。未改历史台账结果，未执行真实Meta DELETE。验证命令见test-report.md。
