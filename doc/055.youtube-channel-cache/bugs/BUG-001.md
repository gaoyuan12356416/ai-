# BUG-001 频道列表只有静态scope判断

根因：production channels闭包过滤掉不可用条目，仅检查DB字段，不校验实时Token和频道功能。CoffeeShorts可选但封面403。修复：ChannelDirectory全部条目快照与实时预检，缺权限/未知禁选；创建和入队再次校验。API无法保证封面接受，独立保存已知拒绝与人工核验。

另修复素材单槽20秒缓存反复被搜索替换，改SQL+搜索词有界SWR缓存。验证见test-report。
