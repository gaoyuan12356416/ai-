# 用例审查

2026-09-22，实施者自审。

除三个字段的正向覆盖外，必须覆盖“空值不等于清空发布字段”、A切B保留A值、先后请求倒序、失败重试、普通发布人编辑授权异常频道、跨租户读取与并发首次写入。使用隔离 API，不创建真实视频、评论、审核通知或短链。发布提交仍由原后端校验宏、首评资格和预约；回滚只能恢复代码，保留数据库。

结论：这些场景均纳入自动化检查；生产只读核验静态/接口/服务/历史文案，不做平台 canary。
