# 需求与架构审查

2026-09-22，实施者自审。

- 用户确认的非空覆盖、切换保留当前值、允许手改、同租户全体发布人维护均有明确落点。
- 不在 create_task 或 worker 中再次查模板，否则会覆盖手改及破坏任务冻结；保持原提交契约。
- 不能调用发布 validate 作为编辑模板的入口，否则已停用频道无法维护；单独只读解析真实频道身份。
- GET 模板允许两个导航入口任一授权；列表/保存独立导航，旧发布路由保留原鉴权。导航上线继承现有发布入口权限元数据。
- 新列表绕开素材 SQL configured 门控。版本冲突保留输入，纯空白统一为空，清空不删除版本历史行。

结论：按上述约束实施。
