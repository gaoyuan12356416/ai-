# 测试报告

## 本地
- Python与JavaScript语法校验通过。
- YouTube后端完整发现运行508项，506通过、2跳过（当前系统不执行Linux进程组测试、缺少历史生产损坏图片fixture）。含新增模板7项、HTTP新增2项。
- 随后新增部署与回滚专项3项通过，覆盖权限元数据/线上导航补丁保留、数据保留和后续漂移拒绝。
- 新增浏览器20个断言通过，API全部隔离模拟；没有真实平台调用。桌面1440×1000、手机390×844截图已查看；页面无横向溢出。
- 预约浏览器38个断言通过，涵盖北京时间转换、可选预约、取消/改期、CAS和unknown保护。
- 旧轮询竞态用例的封面URL为空，审核按钮按当前规则禁用。补齐隔离封面fixture后10个断言通过；没有修改生产审核逻辑。新增模板20项再次通过。
- Linux最终版本511项运行，510通过、1个历史图片fixture跳过。生产真实会话与静态只读验证通过，详见 deployment-evidence.md。

## 限制
没有将模板配置写入生产频道，也没有创建真实视频/首评验证；生产验收使用既有会话只读GET和历史数据对照。未把模拟浏览器结果表述为真实平台发布验证。
