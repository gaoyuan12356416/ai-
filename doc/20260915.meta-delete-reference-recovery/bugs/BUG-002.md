# BUG-002 历史视频字段截断与零占位

2026-09-15生产只读全量扫描发现6条不能完整解析的源记录。

- 5条字段恰为512字符并以逗号结束，属于账户395917922152974，源行4101413、4222580、4238623、4261429、4261430。不能去尾逗号后当作完整列表；末尾纯数字也可能被截断。
- 1条源行24313476为0,0,0,0,0,0，是无法指向Meta对象的全零占位。

修复：视频索引单独识别全零占位；达到512字符或其他不可完整解析的记录保存为异常，必须用Meta GET确认历史Ad已删除或读取完整Creative关系。引用必须保留原产品和账户；任一记录权限不足使当前引用核验保持阻止。FORMAT_VERSION=2避免复用旧解析器代次。

当前外部限制：账户395917922152974的候选用户160、212、631、709均无法读取，212凭证返回190/464，其他返回200。未变更任何权限、未修改源行、未执行DELETE。恢复账户读取后可在原任务重新核验。

回归：新增7项异常解析/缓存版本/Meta读取测试，本地151项通过。最终生产异常数量以最新完整快照为准，不使用旧快照放行。
