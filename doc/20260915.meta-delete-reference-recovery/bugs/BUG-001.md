# BUG-001 Windows fsync只读句柄失败

状态：已修复。
根因：os.fsync用于open('rb')的文件句柄，在Windows返回errno9 Bad file descriptor，导致完整索引无法发布。
修复：同步前以r+b打开已写完且关闭SQLite连接的stage文件，再fsync、校验TTL并原子发布。
验证：最小文件复现确认r+b成功；VideoIndex22项测试通过，包括真实fsync与失败注入。
