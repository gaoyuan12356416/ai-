# SA 设计审查

2026-09-15 独立 reference_design_review 审查：有条件通过完整流式快照方案，否决仅依赖 updated_at 增量放行。

P0 已纳入：单个一致性 SELECT、EOF/退出码/完成标记、原子发布；TTL 从开始计算；执行专属新代次及逐对象过期检查；不完整解析保持阻止；固定任务对象；成功记录不改。
P1 已纳入：后台异步202、单连接 FIFO Gate、SQLite singleflight、磁盘容量上限与读取时限、进程中断不复用部分索引。服务停止会由 Gate 清理其子进程。
上线条件：完成真实全量读取性能验证，不以小样本或仅 mock 测试宣称线上视频核验可用。

性能条件已验证：两轮完整生产读取分别288.53秒、361.88秒，均小于600秒窗口。新增风险处理：512字符字段可能截断；只能使用当前Meta实际关系消歧，缺权限不清空引用，旧解析代次失效。生产历史账户权限问题保留为明确外部阻止项。
