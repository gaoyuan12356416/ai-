# 测试报告

## D10 当前结论（2026-08-07）

D7/D10 生产切换尚未执行。用户已批准看板起点改为 `2026-08-01`，后端、前端和测试边界已同步；本地自动化通过，但候选缓存、真实数据对账、原子切换、已授权页面和自然 timer 仍待验收，不把历史 D30 验收结果表述为 D10 已上线。

## D10 已验证与阻塞项

| 项目 | 结果 | 状态 |
| --- | --- | --- |
| `ads_app_revenues_10d` schema/index 与 D30 对照 | 结构与索引一致，可复用映射和缓存设计 | 通过 |
| D10 日期覆盖 | 当前最早日期 `2026-08-01`；逐日覆盖需在 bootstrap 前复核 | 起点已确认；生产门禁待执行 |
| D10 日期边界 | 用户批准从 `2026-08-01` 开始；7/31 必须拒绝 | 本地通过；生产待验收 |
| D10 真实点样本 | 尚未在最终批准范围内重选 | 待执行 |
| D10 本地自动化 | 边界与 60 天裁剪回归后 `64/64` 通过 | 通过（仅本地） |
| D10 浏览器/生产回归 | 候选数据、已授权页面和生产网络请求尚未验证 | 待执行 |
| 全新 D10 SQLite、语义标记和 bootstrap | 未执行 | 阻塞 |
| 原子切换、已授权页面和自然 timer | 未执行 | 阻塞 |

D10 本地自动化已确认代码级 API/CSV 字段、`2026-08-01` 边界、60 天裁剪、源表合同和缓存拒绝路径，共 `64/64` 通过；其中 checkpointed 历史 D30 缓存先用 `mode=ro&immutable=1` 拒绝，确认 bytes/mtime 不变且不创建 `-wal` / `-shm`；另以 writer 保持打开的已提交、未 checkpoint WAL 篡改 D10 语义，确认普通只读、WAL-aware 二次门禁会拒绝，refresh 不进入 writable/MySQL，Web 不启动 HTTP Server。这不是候选库或生产验收。生产验收仍必须确认：真实候选缓存含 `comparison_window=D10` 和 `new_attribution_source=kunlunads_dev.ads_app_revenues_10d`；Web/刷新在目标机拒绝历史 D30 SQLite；真实点样本及汇总守恒通过；候选库按 8/1 bootstrap 后再原子提升；失败可原子回滚到历史 D30 数据库。

## 2026-08-06 历史 D30 基线

以下结果是 2026-08-06 已发布 D7/D30 版本的不可变历史证据，仅用于容量、性能和回滚对照，不是 D10 验收。

### 历史执行统计

| 类型 | 数量 | 通过 | 失败 | 阻塞 |
| --- | --- | --- | --- | --- |
| Python 自动化 | 51 | 51 | 0 | 0 |
| 前端契约（包含于自动化） | 14 | 14 | 0 | 0 |
| 本地浏览器场景 | 6 | 6 | 0 | 0 |
| 生产只读 schema/样本/规模核验 | 4 | 4 | 0 | 0 |
| 生产发布验收 | 4 | 3 | 0 | 1 |

当时唯一阻塞项为缺少已授权飞书浏览器会话，无法自动完成登录后的生产视觉补验；这不改变当前 D10 需要重新验收的事实。

### 历史验证证据

- `python -m unittest discover -s ops\dramawave-attribution-comparison -p 'test_*.py' -v`：历史 D30 合同 `51/51 OK`；编译、Python 3.9 AST、最终 HTTP fixture 和 Playwright fixture 按当时记录通过。
- 10 万 custom + 10 万 D7 + 10 万 D30 长 ID 合成刷新：`23.776s`，`tracemalloc` 峰值 `229.6MiB`，长 ID、重复 revenue 键与全指标守恒通过。
- 历史真实点样本 `2026-07-29 / ad_id=120250136876120737`：D30 D0 `225.54`、D30 D7 累计 `245.23`、旧 D7 D0 `225.73`。这些值不得复制为 D10 预期结果。
- 历史真实规模：2026-07-29 facts `75,283`，2026-08-05 facts `131,011`；custom 九天 `1,903,748` 行。
- 历史单日 canary（2026-08-05）：`131,010` facts，约 93 秒，峰值 `809,607,168` bytes；`quick_check=ok`，汇总最大误差 `<5e-7`。
- 历史全量 bootstrap：`2026-07-29`～`2026-08-06` 共 `920,751` facts，约 11 分 12 秒；SQLite 约 `1.34 GiB`、WAL 为 0，1 GiB cgroup 下 `oom_kill=0`。
- 历史第一轮自然 timer：`2026-08-06 21:22:00` 启动，`21:26:12` 推进至 `20260806T132547Z-d44a154d`，`21:27:01` 完成预热；日志、stage 清理、版本和 `quick_check` 均按当时记录通过。
- 历史缓存：`920,753` facts、`70,729` filter rollups、`898,655` campaign rollups；三层最大误差 `<4.74e-7`。预热后 Campaign、Ad Set、排行约 `2.295/1.787/1.543 ms`，未预热 optimizer×country 约 `248 ms`。
- 历史目标机：Python 3.9、`systemd-analyze verify`、`nginx -t`、Web service、refresh timer 和 `127.0.0.1:8832` 健康检查通过；公网未登录页面/API 均 `302` 到飞书登录。

## D10 遗留风险

- 日期边界已批准为 8/1；不得让代码、前端或部署参数重新接受 7/31 及更早日期，也不得把历史 D30 数据混入 D10 候选库。
- D10 必须构建全新 SQLite；旧 D30 数据库只能作为在线基线与回滚点，不能原地迁移后继续使用。
- 当前真实 D10 历史较短，60 天容量和延迟回流仍需在切换后持续观察；历史 D30 性能仅是参考。
- 两张 revenue 表没有产品字段；未命中 Dramawave custom 候选的键只能日期级审计，不能安全归属国家/优化师等筛选。
- timer 与 TT 重刷新共用互斥锁；锁忙状态 75 会保留上一成功版本，因此“每 30 分钟调度尝试”不等于“每 30 分钟必完成”。

## 发布建议

维持历史 D30 生产基线，直至候选门禁通过。按已批准的 8/1 边界和 `deploy.md` 构建、验证全新 D10 SQLite，完成真实点样本、自动化/浏览器回归、原子提升、已授权页面和一轮自然 timer，再形成新的 D10 生产验收报告。

## 变更记录

- 2026-08-07：测试对象由 D7/D30 改为 D7/D10；用户批准起点为 8/1，边界与 60 天裁剪回归后本地自动化 `64/64` 通过，原 D30 生产通过记录整体降为历史基线。真实点样本、候选数据库、原子切换与回滚仍待生产验收。
