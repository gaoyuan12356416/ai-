# SA 测试用例评审

## 结论

D7/D10 测试设计及生产验收通过，覆盖数据口径、防放大、缓存原子性、API 白名单、浏览器性能、缓存语义与部署安全。日期边界固定为 `2026-08-01`；目标机自动化、旧 D30 拒绝门禁、真实数据对账、独立 D10 SQLite、已授权页面和自然 timer 均已有 D10 证据，不沿用历史 D30 结果。历史 D30 数据库与备份已保留，但本次未执行完整实际回滚演练。

## 覆盖性问题

| 编号 | 场景/用例 | 问题 | 补充要求 | 状态 |
| --- | --- | --- | --- | --- |
| TR-001 | revenue Join | 只测总额不能发现 OS 多行放大 | 构造同一 Ad 的 Android/iOS 两行并断言只合并一次 | 已纳入 |
| TR-002 | fallback | 只测 Ad 无法覆盖 Google/UAC | 覆盖 Ad Set、Campaign 和 ambiguous 分支 | 已纳入 |
| TR-003 | 比率 | 行级 ROAS 平均可能偶然通过 | 使用不同 spend 的两组数据断言汇总后相除 | 已纳入 |
| TR-004 | 缓存失败 | 删除后异常可能丢失上一版 | 故障注入并断言旧日期和 data_version 保持 | 已纳入 |
| TR-005 | 性能 | 静态测试不能证明浏览器不下载明细 | 真实浏览器检查网络请求、首屏和切换筛选 | 已授权 Chrome 验收通过；页面加载、Meta 筛选与 D7 累计切换无功能错误 |
| TR-006 | 安全 | API 动态列可能被注入 | 非白名单维度/排序/日期必须 400 | 已纳入 |
| TR-007 | 定时器 | 手动刷新不等于自然调度成功 | D10 上线后观察一轮自然 timer 和版本推进 | D10 生产通过；`18:34:10`～`18:38:33` 自然运行刷新 8/7、8/6、8/1，版本成功推进并预热 |
| TR-008 | 汇总性能 | 最细事实每日超过 7 万行，单纯 LRU 无法保证新版本首个请求 | 校验 D10 汇总路由与事实守恒，新版本发布后预热真实全范围常用路径，并记录未预热组合 | D10 生产通过；重复暖查询 `0.0027s`。D30 历史基线仍保留 |
| TR-009 | 排除量语义 | 候选内未匹配为 0 不代表全源没有被排除行 | 单列日期级全源排除量；有业务筛选时验证“不可归属筛选”标记 | 已纳入 |
| TR-010 | 日期边界 | D10 最早日期为 8/1，必须防止前后端或部署参数仍使用 7/29 | 固定 `MIN_DATE`、前端 fallback、bootstrap 和测试为 8/1；验证 7/31 拒绝、8/1 可查 | 本地及生产通过；活动 D10 缓存起点为 `2026-08-01` |
| TR-011 | 缓存语义 | 历史 D30 SQLite 可能被误用于 D10 | 结构或 `comparison_window` / `new_attribution_source` 不匹配时，Web 与刷新均拒绝打开 | D10 生产通过；旧 D30 immutable 拒绝门禁通过，活动库为独立 D10 SQLite |

## QA 修订确认

- 2026-08-06：历史 D30 用例已回填自动化、浏览器 fixture、生产 canary/bootstrap、自然 timer 与性能证据。
- 2026-08-07：测试对象改为 D10，新增日期覆盖、60 天裁剪与缓存语义用例；用户批准起点为 8/1 后，目标机自动化 `64/64` 通过。release `ec6fba1b7e155be904c4c54791fd491c76e09a9a` 已使用独立 `dashboard-d10-6c323f5ff35e-20260807-175911.sqlite3` 上线；真实唯一 Ad 的 custom/D7/D10 精确对账、旧 D30 immutable 拒绝门禁、已授权 Chrome 页面加载与 Meta 筛选+D7 累计切换均通过。自然 timer `*:04,34` 在 `18:34:10`～`18:38:33` 成功刷新 8/7、8/6、8/1，版本由 `20260807T101302Z-28d37de4` 推进到 `20260807T103755Z-96c22ea1` 且预热成功；页面无功能错误，浏览器扩展启动时仅有 3 条 `message-channel` 噪声。历史 D30 库/备份继续保留，但未执行完整实际回滚演练。
