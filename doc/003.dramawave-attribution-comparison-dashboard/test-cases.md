# 测试用例

## 测试范围与当前状态

覆盖 D7/D10 数据口径、分层映射、SQLite 刷新原子性与语义门禁、60 天保留、服务端聚合 API、前端交互、Feishu 鉴权、候选库原子切换和服务器定时调度。业务源库验证仅使用 63350 只读连接。

D10 边界调整后的本地自动化已 `63/63` 通过；生产测试当前仍为“待切换”。用户已批准看板起点为 `2026-08-01`，后端/前端边界和测试已同步；接下来执行真实点样本、候选 bootstrap、生产页面和自然 timer 验收。

## 测试数据

- D10 已核验事实：表结构及索引与 D30 相同；当前最早数据日期为 `2026-08-01`。
- 批准边界：北京时间 `2026-08-01`（含当天）；`2026-07-31` 及更早日期必须拒绝，`MIN_DATE`、前端 fallback、测试和部署参数必须一致。
- D10 真实点样本：待在最终批准范围内重新选择并只读核验；不得复用历史 D30 样本数值。
- 单元测试 fixture：同一实体 Android/iOS 多行、Ad/Ad Set/Campaign fallback、一对多歧义、零分母、刷新故障注入、旧缓存语义不匹配。
- 浏览器测试：全新 D10 临时 SQLite fixture + 本地只读 Web 服务；生产缓存/API、已授权页面和自然 timer 均在切换后重新验证。

## D10 用例列表

| 编号 | 场景 | 步骤 | 预期结果 | 优先级 | D10 状态 |
| --- | --- | --- | --- | --- | --- |
| TC-001 | 日期边界 | 查询 7/31 与 8/1，并核验 `/api/meta.minimum_date` | 7/31 被拒绝、8/1 可查，meta 为 `2026-08-01` | P0 | 本地自动化通过；生产待验收 |
| TC-002 | 真实点样本 | 在 8/1 起范围内选择一个有 custom、D7、D10 数据的 Ad，比对缓存与两张 revenue 表 | D7/D10 金额和计数分别一致；结果来自 D10 实表，不引用历史 D30 数值 | P0 | 待候选数据选样本 |
| TC-003 | OS 行合并 | 同一 Ad 构造 Android/iOS 两行 | users、purchase、IAA/IAP 先加总且 revenue 只计一次 | P0 | 本地自动化通过 |
| TC-004 | Ad 映射 | 构造唯一 `dt+ad_id` | 命中 `matched_grain=ad`，维度来自 custom | P0 | 本地自动化通过 |
| TC-005 | Ad Set fallback | Ad ID 为空、Ad Set 唯一 | 命中 `matched_grain=adset`，不重复计入 Campaign | P0 | 本地自动化通过 |
| TC-006 | Campaign fallback | Ad 与 Ad Set 不可用、Campaign 唯一 | 命中 `matched_grain=campaign` 且只计一次 | P0 | 本地自动化通过 |
| TC-007 | 映射歧义 | fallback 键对应多个维度签名 | 标记 `ambiguous`，不随机或按花费比例分摊 | P0 | 本地自动化通过 |
| TC-008 | 渠道来源 | revenue.platform 为 android/ios，custom.platform 为 0/1/3/4 | 渠道只取 custom；OS 不出现在渠道筛选 | P0 | 本地自动化通过 |
| TC-009 | 优化师 | 相同/缺失名称、不同 optimizer ID | 按 ID 聚合；有名称显示名称，无名称保留 ID | P1 | 本地自动化通过 |
| TC-010 | 聚合 ROAS | 两组 spend/收入不同后合并查询 | ROAS=汇总收入/汇总花费，不平均行级 ROAS | P0 | 本地自动化通过 |
| TC-011 | 提升率零分母 | D7=0、D10>0；两者均为 0 | 前者显示“新增”，后者显示“—” | P1 | 本地自动化通过 |
| TC-012 | 刷新回滚 | 多日期覆盖中注入异常 | 全部目标日期旧事实保留，`data_version` 不推进 | P0 | 本地自动化通过 |
| TC-013 | 版本提交 | 所有目标日期成功 | 单次提交新版本，页面不见半版数据 | P0 | 本地自动化通过 |
| TC-014 | 60 天保留 | 插入早于保留边界的事实和刷新日志后刷新 | 删除 `max(2026-08-01, 今天-59天)` 之前的数据 | P1 | 本地自动化通过；生产待验收 |
| TC-015 | 刷新选日 | 执行多个常规定时轮次 | 每轮含今天、昨天及一个历史 cursor 日期 | P0 | 本地自动化通过 |
| TC-016 | 页面查询不回源 | 停用 MySQL/清空其环境变量后请求 API | 页面、meta/options/query/export 均只读 SQLite 并可工作 | P0 | 本地自动化通过 |
| TC-017 | API 白名单 | 传入非法维度、排序键及 SQL 片段 | HTTP 400，无任意 SQL 标识符拼接 | P0 | 本地自动化通过 |
| TC-018 | 多维查询 | 组合日期、渠道、产品、优化师、国家组、账户、Campaign、Ad Set | totals/trend/rankings/rows 使用同一筛选口径，字段仅为 `d7_*` / `d10_*` | P0 | 本地自动化通过 |
| TC-019 | CSV 一致性 | 用同一筛选请求 query 与 export | 导出完整聚合；共同字段和总计与页面一致，列名为 D7/D10 | P1 | 本地自动化通过 |
| TC-020 | HTTP 缓存与压缩 | 重复请求并带 If-None-Match/Accept-Encoding | 有 ETag、命中返回 304、文本响应可 gzip、Cache-Control 为 private | P1 | 本地自动化通过 |
| TC-021 | 前端加载架构 | 检查代码与浏览器 Network | 不下载 60 天广告明细，仅调用聚合 API | P0 | 本地契约通过；生产 Network 待验收 |
| TC-022 | 页面交互 | 宽屏/移动宽度切换筛选、分组、D0/D7 累计、分页 | 首屏和交互正常，界面明确显示“旧 D7 / 新 D10”，无控制台错误或失控横向溢出 | P1 | 本地契约通过；浏览器待验收 |
| TC-023 | Feishu 鉴权 | 未登录和已授权分别访问页面/API | 未登录跳转登录；已授权可用；缓存不公开 | P0 | 待 D10 生产验证 |
| TC-024 | 自然定时 | D10 切换后等待下一 22/52 分错峰触发 | oneshot 成功、版本推进、下次触发正确；与 TT 共享锁且不并发 | P0 | 待 D10 生产验证 |
| TC-025 | 性能 | D10 新版本预热后验证全范围常用路径，并测未预热低基数组合 | Campaign/Ad Set/排行常用路径 <=300ms；未预热低基数组合 <=1s；首屏 <=2s | P1 | 待 D10 生产验证 |
| TC-026 | 汇总路由守恒 | 对同一筛选分别强制最细事实和日级汇总查询 | totals/trend/Campaign/筛选维度结果与所有可加指标完全一致，浮点误差 <=0.01 | P0 | 本地自动化通过 |
| TC-027 | 范围外排除透明度 | 构造无 Dramawave custom 候选的全源 revenue，并叠加业务筛选 | 看板金额不纳入该行；质量元数据分别返回 `d7_*` / `d10_*` 日期级排除量，并标明不可归属业务筛选 | P0 | 本地自动化通过 |
| TC-028 | 旧 SQLite / WAL 篡改拒绝 | 让 D10 Web 和 refresh 打开 checkpointed 历史 D30 SQLite；另保持 D10 writer 打开，在未 checkpoint 的已提交 WAL 中篡改语义标记 | 历史 D30 库由 `mode=ro&immutable=1` 粗门禁拒绝，bytes/mtime 不变且不创建 `-wal` / `-shm`；WAL 篡改由普通只读、WAL-aware 二次门禁拒绝，refresh 不进入 writable/MySQL，Web 不启动 | P0 | 本地自动化通过；目标机待验收 |
| TC-029 | 候选库原子切换与回滚 | 用全新 D10 库 bootstrap、核验语义后原子替换 live DB 指向，再原子切回旧 D30 库 | 用户只看到完整 D10 或完整历史 D30 版本；无混合字段、半版数据或打开错误库；回滚点可验证 | P0 | 待 D10 生产验证 |

## 2026-08-06 历史 D30 基线

- 当时固定边界为 `2026-07-29`，点样本 `ad_id=120250136876120737` 的 D30 D0 收入为 `225.54`、D30 D7 累计收入为 `245.23`；这些数值只证明历史 D30 版本，不能作为 D10 预期值。
- 当时自动化、浏览器 fixture、生产 canary/bootstrap、未登录飞书跳转和自然 timer 的通过记录保留在 `test-report.md`，仅用于容量、性能与回滚对照。

## 回归范围

- 主 AI 后台页面与既有报告路由不被覆盖。
- TT 小程序统计服务、缓存、端口和调度不做改动。
- 业务 MySQL 无 DDL/DML；只读连接校验仍然生效。
- Nginx 现有 Feishu auth 子请求和其他 `/reports/` 路由保持可用。

## 变更记录

- 2026-08-07：测试合同从 D7/D30 改为 D7/D10，新增 D10 日期覆盖、真实点样本重选、旧 D30 SQLite 拒绝、候选库原子切换与回滚用例。用户批准 8/1 起点后，边界用例和代码已统一；本地自动化 `63/63` 通过，D10 候选库和生产状态保持待执行。
