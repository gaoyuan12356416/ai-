# BUG-002：旧正式长片的内存持续增长

- 状态：观察成立，根因与修复待长片实测；不得以 5 秒样例关闭。
- 进程：香港旧 FFmpeg PID 1708285，模板渲染 14:36:48 开始。
- 15:29 约 9 GB RSS；16:03:16 RSS 15,565,072 kB；16:16:09 RSS 19,217,568 kB，96 线程，CPU 199%，GPU encoder 8%。
- 16:45:37 实测：RSS `27,445,544 kB`、VmSwap `122,164 kB`、MemAvailable `4,080,848 kB`、CPU `199%`、线程 `96`。该时点有非零VmSwap且可用内存明显下降，须作为资源保护风险处理；这些是带时间的快照，不能据此声称已经OOM或进程已经停止。
- 节点内存约32 GB；16:16 MemAvailable `11,998 MiB` 是较早快照，不能沿用为当前余量。线程数未增长也不能证明RSS安全或确认泄漏位置。
- 17:21:35：RSS `31,154,056 kB`、VmSwap `4,964,204 kB`、MemAvailable `495,332 kB`，96线程。17:25:39输出仍在增长（`2,298,216,496`字节，mtime同秒），RSS `31,261,148 kB`、VmSwap `5,410,900 kB`；不能把“内存持续增长”误写成“完全没有制作进展”。仍没有完成manifest，未启动节点渲染验收。
- 授权状态：主任务已询问用户是否允许保存证据后终止旧渲染，**尚未收到回复，未获终止授权**。实施新代码的批准不等于中断当前正式任务的许可。
- 后续结局：约17:36:51旧渲染自行退出，17:41核对GPU服务未重启、COS仅封面/无视频、无完成manifest；journal/dmesg无OOM，cgroup oom_kill=0。退出时间与旧10800秒限时吻合，但旧worker吞掉原异常，不能将推断写成已捕获超时。我们没有终止或重启，先前的停止询问已无操作对象。详见 [结局证据](../evidence/original-case-outcome-20260828.json)。
- 当前门禁：内存恢复不能证明缺陷修复。TT迁移任务释放HK媒体窗口、正式制作排空以及v1内存/swap保护确认前，不启动短/长样；代码实际运行时回归通过也不关闭此缺陷。
- 约束：未获明确授权不重启/中断正式任务；不修改同机FB优先级，不以增加并发或临时放大资源上限掩盖风险。旧任务自然完成，或另获授权终止且确认PID/进程组停止并核查未完成状态后，才满足隔离制作的排空前提；发出停止请求本身不算停止完成。
- 代码边界：render start guard/prepared、12小时有界渲染时限与异步COS分片改善的是恢复与超时合同，**不是内存增长修复证明**，不据此关闭本缺陷。
- 后续：按 [媒体验收手册](../media-acceptance-runbook.md)，从GitHub精确候选进行固定输入/配方短样、约90分钟长片，记录每秒RSS/线程/媒体进度，完整解码并核对滤镜帧同步与循环资产行为。当前真实长片验收未执行；无证据不声称增长根因已确认。
