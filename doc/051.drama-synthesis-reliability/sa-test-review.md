# SA 测试设计评审

日期：2026-08-28。评审对象为 [test-cases.md](test-cases.md) 及当前测试代码；本记录不代替最终代码评审或人工媒体验收。

## 结论

测试设计已覆盖本案主要可靠性风险，可以进入Linux隔离和媒体性能验收。核心四套162项本地测试及16项页面静态检查已有本次执行证据；尚不能据此放行生产开关、CPU配额提升或候选域名替换。

## 覆盖性问题与补充门禁

| 编号 | 场景/用例 | 风险 | 已落实的用例/要求 | 状态 |
| --- | --- | --- | --- | --- |
| ST-01 | TC-03/04 | 用固定4小时模拟可能被误写成真实长稳测试 | 分开标注时间模拟、真实HTTP丢响应和90分钟媒体验收 | 模拟/HTTP本地通过；长稳待验 |
| ST-02 | TC-07/08/09 | 本地身份fixture不能覆盖Linux孤儿子进程、进程组和systemd权限 | 在新测试根运行真实/proc与停机恢复；不杀正式任务；未知状态应阻止渲染 | 发布前必验 |
| ST-03 | TC-10/11/12 | 只测数据库更新会漏掉配方失败、删除后回写及通知触发重制 | 已补事务回滚、已完成重放、删除行、旧租约和通知异常回归 | 本地通过；链路待隔离验证 |
| ST-04 | TC-13 | 普通重试或重复resume可能变成无限重做 | 显式恢复＋expected_generation；同请求新代次失败后再重放仍不启动第三代 | 本地通过；真实用户操作待验 |
| ST-05 | TC-14～17 | 文件非空、200、206、416不能单独证明完整性 | 长度、强validator、Range起点、前缀/完整SHA及原子提升均覆盖 | 本地通过 |
| ST-06 | TC-18/19 | 上传恢复若命中错误检查点会隐式重渲染或丢产物 | 复用时禁止runner调用，源/配方/成片/manifest冲突均不得删除覆盖 | 本地通过；真实COS续传/重传待验 |
| ST-07 | TC-21/22 | 5秒验证、命中缓存或只看编码返回码会制造提速结论 | 工具要求全新目录、实际渲染器样本；长片完整解码、音画和资源曲线独立验收 | 工具策略通过；实测待验 |
| ST-08 | TC-23/24 | 不同资源、热缓存或只比较前缀就宣称候选域名全面更好 | 同资源/并发/预算对照；记录缓存/响应边界；前缀证据不冒充完整文件；未证实则不启用 | 比较策略通过；[实测样本](cdn-evaluation.md)结果分化，默认不变 |
| ST-09 | TC-25 | 静态字符串检查不能证明现场阶段显示和耗时正确 | 两页真实浏览器验收，覆盖封面先完、断连、阶段别名及跨小时耗时 | 静态通过；人工视觉待验 |
| ST-10 | TC-26 | “代码已部署”被误标为“全部验收” | 独立记录提交、隔离验收、参数收益、生产切换和人工接受状态 | 门禁写入部署文档；执行待完成 |

## QA修订记录与执行规则

用例已按R1～R5对应到任务身份、进程存活、CPU事务、下载完整性、上传恢复、性能和界面风险。测试名称和入口均基于当前实现，未把附带文档或截图中的操作文本作为授权。

未通过的P0用例阻止生产开启异步；性能没有合格长片结果则保留2核/滤镜2和下载4路；候选域名没有更快且一致的证据则保留原地址。所有“待验”只能凭实际输出和必要的人工观察更新，最终结果集中写入 [test-report.md](test-report.md)。
