# BUG-008 unified sync worker 永久未配置

- 级别：P0
- 旧候选：`f05e10f` 永久 HOLD。
- 现象：worker 固定构造 `UnifiedYouTubeWriter(None)`，即使外部 owner 就绪也永远不能同步。
- 修复：从 env factory 构造受控 RPC executor；只允许三表、SELECT/INSERT/UPDATE、受限 payload、并发 1 与外部 ID 幂等；凭据从 server-only 0600 文件读取；HTTPS/loopback、no redirect、timeout；缺配置/认证/表/未知响应 fail closed。
- 状态：Wave8 exact SHA `85c0b3cda58aeab50765a9ecb09e79a1bbf7e883` 独立 QA PASS，候选缺陷已关闭；外部 schema/owner 仍是 production release gate。
