# BUG-029：三项新操作仅在详情抽屉，未直接展示到任务列表

类型 P2，需求入口遗漏。附件第三节明确为“任务列表操作”。59f95e6 的已完成行只有查看详情、下载全部、删除；复制素材 URL、生成短链、发布到 YouTube 仅在详情“结果与日志”出现，与 live/sync 开关无关。

最小修复在两个同源 HTML 的 buildJobRow 增加现有三处理器按钮，复用原 mini 按钮、actions-inline 换行布局与事件传播保护；只有 done 展示，封面可复制、无视频禁用短链/发布。保留详情入口、原权限/CSRF、异步发布和正式 live 服务端门禁，不自动发起任何写操作。同时把模板读取提示从香港 GPU 改为 CPU，匹配实际职责。

新增纯 Node VM 行渲染测试，覆盖两页三种视频、封面、三个非完成状态和提示/详情兼容；不控制浏览器、不联网。状态：CLOSED / PRODUCTION VERIFIED，两页16检查及独立10场景/20处理器调用通过。ee6e00c上线后郜远会话验证20行均有直接入口；同一原任务的短链复用和发布弹窗已从列表实际打开，未误触详情或正式public入队。
