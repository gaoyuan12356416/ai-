# BUG-007 随机模板 outputs 权威键错误

- 级别：P0
- 旧候选：`f05e10f` 永久 HOLD。
- 现象：实现错误使用 `random_template`，与唯一附件的 `random_template_video` 不一致。
- 修复：app/UI/GPU payload/SQLite migration/test/docs 全链路使用 `random_template_video`；旧错误键仅作为历史输入 fallback，迁移后不存旧键；结果仍为 `output_random_template_url`。
- 状态：实现者 focused 回归 PASS；独立 QA 待复验。
