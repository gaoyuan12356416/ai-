# BUG-026 MySQL 5.7 的精确库名授权显示未兼容

CPU 隔离 MySQL 5.7.44 的 SHOW GRANTS 将库名显示为反引号包裹的 `ads\_ai`。候选 f3d754e2d4d0912d0cb7bf63b98d01c5f6f554bb 的 bootstrap 仅接受未转义 ads_ai，因而在管理员身份预检处停止；只读确认隔离库仍为 0 张表，生产库未执行 DDL。

修复只增加精确的单次下划线转义形式，不做通用反转义。其他库、通配符、双重转义、错配引号和错误账号仍拒绝。运行时 writer 授权解析和固定 DDL 不变。

独立定向复验：23/23 bootstrap 用例、另 6/6 内存接受/拒绝对抗通过，真实网络/DB 调用为 0；两文件 compile、Python 3.9 AST、diff-check 通过，测试前后 SHA 一致，其余 16 个冻结源码/配置不变。原 262 项完整回归仍绑定 f3d754e 候选，不重跑、不改绑；新候选须重新取得 dry-run 与 fresh-rehearsal 实机证据。
