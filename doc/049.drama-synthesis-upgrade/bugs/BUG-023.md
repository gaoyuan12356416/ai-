# BUG-023 演练镜像参数与精确 digest 不兼容

本轮 root 在实机准备中发现 bootstrap 只接受 `mysql:5.7.44` 标签，而实际容器按更明确的固定 digest 启动，导致合法演练环境被拒绝。未进行生产 DDL。

已改为允许冻结标签或精确 digest，并同时强制实际 Image ID。错误 digest、错误/缺失 Image ID 和标签漂移均拒绝；专项通过。容器无需重建，历史数据未删除。最终独立回归见 [测试报告](../test-report.md)。
