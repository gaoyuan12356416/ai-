# BUG-001 YouTube 频道查询使用错误线上列名

- 级别：P0
- 发现：独立 QA
- 现象：候选使用 `ch.status`，线上 `ads_youtube_channels` 实际列为 `channel_status`，查询报 MySQL 1054。
- 修复：查询改为 `ch.channel_status`，资格条件仍严格等于 1；fake runner 强制校验 SQL schema contract。
- 状态：已修复，待独立 QA 回归。
