# BUG-009 rollback 使用不存在的 live flag

- 级别：P0
- 旧候选：`f05e10f` 永久 HOLD。
- 现象：rollback 文档曾写成不存在的带 `DRAMA_` 前缀名称，实际代码读取 `YOUTUBE_LIVE_ENABLED`。
- 修复：全仓统一使用 `YOUTUBE_LIVE_ENABLED=0`，并加入错误名称扫描。
- 状态：实现者全仓错误名称扫描 PASS；独立 QA 待复验。
