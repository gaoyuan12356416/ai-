# BUG-006 YouTube 弹窗 Promise.all TDZ

- 级别：P0
- 旧候选：`f05e10f` 永久 HOLD。
- 现象：channels URL 在同一 `Promise.all` 初始化表达式引用尚未完成初始化的 `job`，弹窗无法打开。
- 修复：先 await job，验证存在视频产物，再以冻结 `app_id` 请求 channels。
- 状态：Wave8 exact SHA `85c0b3cda58aeab50765a9ecb09e79a1bbf7e883` 独立 QA PASS，已关闭。
