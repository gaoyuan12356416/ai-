# BUG-002 随机模板 YouTube 来源对 legacy row 不稳健

> 2026-08-26：本文保留为历史缺陷证据；旧字段 `random_template_video_url` 已被权威结果字段 `output_random_template_url` 取代，旧合同不可用于本轮验收。

- 级别：P1/P0 路径
- 发现：CEO 集成评审
- 现象：若来源 job 为未装饰的 legacy row，`random_template_video_url` 不在 row 中，随机模板成片无法进入 YouTube 队列。
- 修复：random source 缺失时从该 job 的已完成冻结 recipe/output ledger 解析；未完成仍失败关闭。
- 状态：已关闭；独立 targeted bug 回归 PASS（8/8 证据集）。
