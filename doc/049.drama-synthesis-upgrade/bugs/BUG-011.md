# BUG-011 recipe XSS、writer/outbox 合同与 Playwright harness

- 级别：P0/P1 batch
- 旧候选：`f05e10f` 永久 HOLD。
- 现象：recipe layer 名称仍可未经可信边界进入 `innerHTML`；writer 会丢弃未知 payload 后继续，未绑定 external identity；坏 outbox JSON 可能离开 syncing；Playwright 需手工 `NODE_PATH`。
- 修复：两 UI 以 DOM textContent 展示全部审计值；三实体 exact keys/types/identity；publish_log external ID 使用十进制 publish ID；所有 claimed payload 错误 fenced fail 且安全消息；spec 使用 CLI-relative resolver，普通 npx 命令可运行。
- 状态：实现者 focused 与实际 Chrome hostile-payload 回归 PASS；独立 QA 待复验，不构成 production release PASS。
