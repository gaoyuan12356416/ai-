# 测试用例

## 测试范围

宏校验、队列冻结、W2A 参数、短链文件、发布链路换行、循环池流程、兼容性和失败关闭。

## 测试数据

- content ID：`ABCD1234`
- TT 短链：`https://gy.g2flow.com/s2l/8000000000000000009.html`
- W2A：`https://www.dramawavew2a.com/ads/101/2250/view`
- 描述：三段文本，以两个 `\n\n` 分隔。

## 用例列表

| 编号 | 场景 | 前置条件 | 步骤 | 预期结果 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-001 | `{url}` 正常渲染 | 元数据完整 | 创建队列 | 宏替换为固定 gy 短链，空行不变 | P0 | 通过 |
| TC-002 | 未知单花括号宏 | 模板含 `{bad}` | 校验模板 | `caption_placeholder_invalid` | P0 | 通过 |
| TC-003 | 缺少 URL 绑定 | 非延迟渲染 `{url}` | 渲染模板 | `caption_url_required` | P0 | 通过 |
| TC-004 | X 同款参数 | 完整归因元数据 | 构造并解析长链 | 八个字段顺序和值精确一致 | P0 | 通过 |
| TC-005 | 短链不可变 | 相同编号不同目标 | 二次写跳转页 | 冲突且原文件不变 | P0 | 通过 |
| TC-006 | CPU 到 GPU 换行 | 描述含双空行 | 捕获 HTTP JSON | `title` 字符串完全一致 | P0 | 通过 |
| TC-007 | GPU 到 TikTok 换行 | 准备产物有效 | 捕获 `post_info` | `title` 字符串完全一致 | P0 | 通过 |
| TC-008 | 循环池/手动发布 | 入池模板含 `{url}` | 预制作并创建手动队列 | 元数据及短链在队列冻结 | P0 | 通过 |
| TC-009 | 无 `{url}` 兼容 | 历史模板 | 执行原回归 | 不创建短链且行为不变 | P1 | 通过 |
| TC-010 | 前端长度预览 | 模板含 `{url}` | 页面校验 | 用最长短链样例计算 UTF-16 长度 | P1 | 通过 |

## 回归范围

TT 核心存储、CPU sidecar、GPU worker、发布池页面、每日/手动调度、合规门禁和历史幂等重放。
