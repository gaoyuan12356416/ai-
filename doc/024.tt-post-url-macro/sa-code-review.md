# SA 代码评审

## 结论

通过离线评审。实现保持失败关闭，未改变无 `{url}` 队列的远端发布路径。

## 评审范围

宏解析、SQLite 增量迁移、循环池元数据、W2A 构造、跳转页原子性、Nginx 路由、CPU/GPU 请求和前端校验。

## 问题清单

| 编号 | 严重级别 | 文件/位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| CR-001 | 高 | `links.py` 文件发布 | `os.replace` 在竞态下可能覆盖不同目标 | 改用同文件系统 hard-link 的无覆盖原子发布 | 已关闭 |
| CR-002 | 高 | Nginx 路由 | 通用 X 正则可能先接管 TT 编号 | 明确要求 TT location 前置 | 已关闭 |
| CR-003 | 中 | 队列幂等 | 重放未比较短链及归因字段 | 纳入全部冻结字段比较 | 已关闭 |
| CR-004 | 中 | 前端预览 | `{url}` 实际长度晚于素材入池 | 使用最大合法 TT 短链样例测量 | 已关闭 |

## 编译 / 验证结果

Python 编译通过；链接、核心、CPU 服务、页面和 GPU worker 测试全部通过。详细数量见 `test-report.md`。
