# SA 测试用例评审

## 结论

通过。关键发布边界、换行字节值、归因参数和不可变文件均有离线覆盖。

## 覆盖性问题

| 编号 | 场景/用例 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- |
| TR-001 | 换行 | 只测数据库不足以证明 TikTok 请求 | 增加 CPU JSON 和 GPU `post_info.title` 精确断言 | 已关闭 |
| TR-002 | 实际 UI 流程 | 仅测旧 queue API 不覆盖发布池 | 增加素材入池、预制作和 `run_now` 用例 | 已关闭 |
| TR-003 | 文件竞态 | 重放可能覆盖文件 | 使用无覆盖原子发布并验证冲突 | 已关闭 |
| TR-004 | 上线验证 | 离线测试不能证明 Nginx 生效 | 部署文档加入 `nginx -t`、curl 和目录检查 | 已关闭 |

## QA 修订确认

用例已补齐；真实 TikTok 客户端的展示只作为上线观察项，不作为服务端单测结论。
