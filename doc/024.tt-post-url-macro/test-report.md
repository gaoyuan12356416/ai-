# 测试报告

## 测试结论

通过。共执行 237 个离线测试，237 个通过，0 个失败，0 个阻塞。

## 测试范围

TT 短链、核心存储、CPU sidecar、页面静态契约、GPU worker，以及无 `{url}` 的历史发布回归。

## 执行统计

| 类型 | 数量 | 通过 | 失败 | 阻塞 |
| --- | --- | --- | --- | --- |
| 短链与 W2A | 5 | 5 | 0 | 0 |
| TT 核心 | 60 | 60 | 0 | 0 |
| CPU sidecar/runner | 89 | 89 | 0 | 0 |
| 发布池页面 | 25 | 25 | 0 | 0 |
| GPU worker | 58 | 58 | 0 | 0 |
| 合计 | 237 | 237 | 0 | 0 |

## 缺陷情况

- `BUG-001`：服务端换行未丢失，TikTok 客户端紧凑视图折叠展示；已补完整回归证据，平台展示限制保留。
- 代码评审发现的竞态覆盖风险已改为无覆盖原子 hard-link 发布。

## 验证证据

- W2A 长链八个参数的顺序和值精确断言。
- 描述中的两个 `\n\n` 在模板、CPU JSON、GPU 校验及 TikTok `post_info.title` 中完全一致。
- 发布池入池、预制作、手动队列创建可冻结 `{url}` 和素材元数据。
- 同一短链同目标可重放、不同目标冲突且原文件不变。
- Python 编译及前端内联 JavaScript 语法检查通过。

## 遗留风险

- 未在生产 Nginx、真实域名路径或真实 TikTok Post 上执行验证。
- TikTok App 最终排版仍可能折叠换行。

## 发布建议

代码可进入审核与合并；生产部署应按 `deploy.md` 分步执行并保留现有门禁，真实 Post 必须另行获得用户授权。
