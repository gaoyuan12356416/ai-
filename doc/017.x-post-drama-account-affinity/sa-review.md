# SA 评审意见

## 结论

通过。采用“持久绑定 + 事务内重算 + 数据库触发器 + 发布前断言”四层保护，不能仅修改 runner 的候选顺序。

## 问题清单

| 编号 | 严重级别 | 位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-01 | P1 | 旧选择器 | 同一剧连续集与多个账号直接配对 | 每账号只选择一部剧的下一集 | 已修复 |
| SA-02 | P1 | 数据库 | 没有账号—短剧持久归属 | 增加字段、部分唯一索引和不可换绑触发器 | 已修复 |
| SA-03 | P1 | 冻结计划恢复 | 旧跨账号队列可能绕过新选择器 | 发布日志预留前校验归属 | 已修复 |
| SA-04 | P1 | 历史数据 | 生产已有多账号混发，不能删除或改写历史 | 以最早确认发布账号作为后续归属，历史原样保留 | 已确认 |
| SA-05 | P2 | 页面 | `last_account_*` 只是最近账号，不代表归属 | 显示持久化绑定字段 | 已修复 |

## 决策记录

- 绑定不自动转移；完成后通过部分唯一索引释放账号的未完结占用。
- `needs_review` 延续现有全局暂停逻辑，避免结果未知时换剧。
- 生产现有 `bURak9Oyn7` 归属账号 ID 10。
- 关闭任务允许保存；启用状态删除未完结归属账号必须拒绝。

## PM 修订确认

规则已转换为可测试的验收标准；无需新增人工换绑交互。
