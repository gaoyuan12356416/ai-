# SA 代码评审

## 结论

通过。BUG-003 最新 diff 评审未发现阻塞部署的 P0/P1 问题；失败批次恢复能力不纳入本次发布。

## 评审范围

SQLite 加法迁移与触发器、账号—短剧选择、未绑定坏剧 FIFO 顺延、事务内建计划、冻结队列恢复、发布状态推进、sidecar 契约和页面展示。

## 问题清单

| 编号 | 严重级别 | 文件/位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| CR-01 | P1 | 旧 selector/runner | 同剧多集与多账号 `zip` | 每账号只取一剧下一集 | 已修复 |
| CR-02 | P1 | 旧 service | 无归属字段与数据库防线 | 增加持久绑定、唯一索引、触发器 | 已修复 |
| CR-03 | P1 | 冻结恢复 | 旧队列可绕过新建计划校验 | `reserve_log` 和 `mark_publishing` 校验归属 | 已修复 |
| CR-04 | P2 | query_drama_pool | 新用户名 join 假设账号表已初始化 | 从 `assigned_source_queue_id` 读取证据用户名 | 已修复 |
| CR-05 | P1 | 页面账号状态 | “不可发布请移除”与后端未完结绑定保护冲突 | 改为校验/重新授权；关闭任务时允许保存失效账号 | 已修复 |
| CR-06 | P2 | 页面归属列 | 不可用/已完成/待核查都显示待分配或当前绑定 | 按状态显示不可分配、历史账号和暂停 | 已修复 |
| CR-07 | P1 | 失败批次恢复 | 直接重建 `failed_preflight` 计划缺少管理员审批、不可变审计，并可绕过已停用配置 | 删除恢复路径；10:06 保持失败，修复仅影响后续自然批次 | 已修复 |
| CR-08 | P1 | 坏剧顺延 | 若确定性失败也跳过已绑定/有历史短剧，会破坏账号粘性和审计证据 | 内部校验入口仅允许未绑定且无历史；其余进入 `needs_review` 并停止整批 | 已修复 |
| CR-09 | P2 | 生产遗留状态 | 旧版本已把池 53 的确定性预检失败误记为 `needs_review`，会阻断后续候选查询 | 部署门禁中精确核对无绑定/无历史后，通过新校验入口改为 `validation_failed`；不恢复 10:06 批次 | 待部署验证 |

## 编译 / 验证结果

- 3 个本轮修改 Python 模块 `py_compile` 通过；发布前补跑含 `drama_selector.py` 的 4 模块检查。
- 335 个完整 X 回归测试通过。
- `git diff --check` 通过。
- 后端第二轮定向评审 101 项测试通过，无 P0/P1。
- TC-15–TC-18 全部通过，包括“绑定字段为 0 但已有旧队列”的独立历史夹具。
