# SA 评审意见

## 结论

有条件通过；以下 P0/P1 决策已纳入需求，可进入开发。

## 问题清单

| 编号 | 严重级别 | 位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-001 | P0 | 时间存储 | 直接把数据库时间改成 UTC+8 会破坏 preview 过期、幂等和历史比较 | 保持 UTC 存储，仅在 API/展示/自然日边界转换 | 已采纳 |
| SA-002 | P0 | Meta 复制 | 只改 created_data 名称会造成站内记录与 Meta 实际名称不一致 | PAUSED 阶段逐对象重命名并回读，失败隔离 | 已采纳 |
| SA-003 | P1 | 名称长度 | 长来源名称可能导致后缀被平台拒绝或截断 | 固定上限并优先完整保留后缀 | 已采纳 |
| SA-004 | P1 | 日期筛选 | 仅改前端格式无法修正 UTC 自然日查询偏移 | 服务端将 UTC+8 日期边界换算为 UTC | 已采纳 |
| SA-005 | P1 | 并行发布 | 线上 app.py 已包含 playable 并行变更 | 以 GitHub 提交 `4ecaa75` 为线上 app 底座，合并 V3 `63f1d1c` 后开发 | 已完成 |

## 决策记录

- 固定展示时区为 `Asia/Shanghai` / `UTC+8`，不采用浏览器本地时区。
- 同一复制 intent 的全部对象共享一个后缀；重放已完成 intent 不产生新名称或新 Meta 写。
- Meta 写次数增加属于预期审计数据，不调整配额定义。

## PM 修订确认

需求、验收标准和回滚边界已按 SA-001～005 修订。
