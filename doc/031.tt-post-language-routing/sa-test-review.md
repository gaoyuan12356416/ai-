# SA 测试用例评审

## 结论

通过。用例覆盖数据迁移、语言合同、无匹配等待、按语言 FIFO、并发、后改与恢复、目标账号设置、手动边界和无真实发布要求。

## 覆盖性问题

| 编号 | 场景/用例 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- |
| TR-01 | T11 | 只测拒绝误发不足以证明素材仍可入池 | 明确验证 intake、预制作、available 和 queue=0 | 已纳入 |
| TR-02 | T13/T14 | 全局 FIFO 与语言 FIFO 容易混淆 | 同时覆盖语言内顺序和语言间互不阻塞 | 已纳入 |
| TR-03 | T17/T18 | 单线程测试不能证明原子领取 | 使用两个 store/连接并发执行 | 已纳入 |
| TR-04 | T19/T21/T22 | 账号语言后改的冻结边界不完整 | 分别覆盖未领取、已有 run、已有 queue | 已纳入 |
| TR-05 | T20 | 动态账号可能继承错误的 AIGC 值 | 断言 pool/queue 使用实际目标账号当前值 | 已纳入 |
| TR-06 | T24/T25 | 手动/canary 可能被自动语言路由影响 | 增加精确账号和精确 pool 回归 | 已纳入 |
| TR-07 | T16 | 仅验证账号默认 en，遗漏旧空素材 | 单独增加旧空素材仅匹配 en | 已纳入 |
| TR-08 | T32 | 自动化可能误触真实发布 | 全部使用 fake，断言 Create Post 调用为 0 | 已纳入 |
| TR-09 | T26/T27 | 新字段可能破坏 100% 布局 | 增加页面结构和浏览器布局检查 | 已纳入 |

## QA 修订确认

测试数据、P0 并发/冻结边界、UI 列和无真实发布断言均已补入 `test-cases.md`。
