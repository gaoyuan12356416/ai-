# SA 评审意见

## 结论

有条件通过。仅允许在候选审计、全量自动化测试、生产副本迁移演练、短链公网验证和账号动态刷新全部通过后执行一条真实 Post。

## 问题清单

| 编号 | 严重级别 | 位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-001 | P0 | 发布边界 | Create Post 无业务幂等键，网络超时可能导致重复帖 | 持久化幂等键；`publishing/published/unknown` 禁止盲重试 | 已纳入设计 |
| SA-002 | P0 | Token 生命周期 | 现有发布凭证上下文不主动刷新过期 Token | 在账号互斥锁内刷新、原子写 Token 并校验 `/users/me` 身份 | 已纳入设计 |
| SA-003 | P0 | 短链 | 通用开放重定向会成为安全风险 | 只生成静态 ID 页面，目标限 Dramawave W2A HTTPS 固定路径 | 已纳入设计 |
| SA-004 | P0 | 素材合规 | 仅查一张违规表或展示标签会漏检 | 同时查四类违规数据及实际标签映射，未知即跳过 | 已纳入验收 |
| SA-005 | P1 | 部署 | 生产目录为复合工作目录，直接覆盖风险高 | GitHub 精确 commit 独立 release，仅重启 X sidecar | 已纳入部署 |

## 决策记录

- 灰度账号默认优先 `ShortsDramhx`，但实时状态不合格时可改用另外两个目标账号；只允许成功发布一条。
- 素材按最高消耗排序后“逐个合规筛选”，不是先挑第一名再放宽门禁。
- 短链使用日志 ID，不接受客户端传任意目标 URL。
- 生产日志不输出候选下载鉴权信息、OAuth Token、Refresh Token 或完整上游响应头。

## PM 修订确认

2026-07-23：范围与用户确认一致，真实灰度 1 条；调度和批量账号明确排除。
