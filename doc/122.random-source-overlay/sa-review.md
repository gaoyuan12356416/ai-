# 技术方案评审

通过。复用现有解码源帧，无额外下载或解码管线；以新可选字段区分新旧配方，保持既有 version/profile 和无字段配方语义。Drama 保留原内核文件，新增叠层内核并按场景选择，避免旧缓存/恢复身份漂移。

TT/FB 在上传之前写持久收据是必要条件：仅在成功 manifest 冻结会导致失败重试被新默认目录影响。收据应绑定请求、源 SHA、原资产目录和 manifest SHA，清理仅处理生成缓存。

部署以在线 CPU core/UI 和三路现行发布基线分别合并，避免用较旧 GPU 分支覆盖 CPU 其他功能。CPU 仅同步 core.py 和 index.html；资产目录、数据库、发布状态不变。
