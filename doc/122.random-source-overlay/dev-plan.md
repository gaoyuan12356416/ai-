# 开发计划

1. 三个独立基线工作树并行实现 Drama、TT、FB；共享 OpenCL 由 TT 负责，FB 同步相同字节。
2. 主任务负责 CPU UI、runtime 预检/健康信息、整合 legacy adapter、文档、独立验证与发布。
3. 先单元与契约测试，再 GitHub 精确提交发布到独立目录，真实 GPU 离线测试。
4. 备份；短暂停止新领取并等待已有任务；切换三路 GPU，验证反向隧道；CPU allowlist 切换；恢复领取和原定时器状态。
5. 从新进程读取能力与配方，记录实测结果与回滚路径。
