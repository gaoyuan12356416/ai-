# BUG-001 旧删除流程与范围风险

优先级P1，状态：V2已修复，生产验收记录见deploy.md。

旧版问题：只支持Post/Ad，默认产品826误用；用名称模糊子串匹配；缺权限/Unsupported get request误判已删；启动Ad阶段重建summary覆盖Post完成状态，导致posts_not_confirmed。

修复：移除旧执行器/源UPDATE和旧默认产品，旧路由关闭；按显式content_id/series_code+短剧产品目录解析；三种Meta节点独立删除；GET错误绝不作为删除凭据；对象/阶段使用独立SQLite记录，支持幂等与人工恢复。

回归：source、graph、store、service、bridge测试及本机Mock UI验收，真实删除未用于测试。
