# SA 代码评审

## 结论

通过本地评审，无未解决 P0/P1。

## 发现与修复

| 严重级别 | 发现 | 修复 | 状态 |
| --- | --- | --- | --- |
| P0 | 原日志仅有 `published`，会提前推进短剧 | 新增 `source_published/repost_creating`，仅 Repost 成功终态 | 已修复 |
| P0 | Repost transport unknown 可能重试 | 持久化 unknown + runner 停止，调用计数测试 | 已修复 |
| P1 | 计划候选中的 relay ID 不可信 | 服务端当前会员列表 + 事务内再分配 | 已修复 |
| P1 | 会员失效可能换号造成双发 | 仅 `source_attempt_count=0` 可重选，触发器再栅栏 | 已修复 |
| P1 | 日负载只看进程内数据 | ledger 聚合排序，重启稳定 | 已修复 |
| P1 | 历史 140s 阻断仍会停池 | 仅零写入迁移并追加审计 | 已修复 |
| P0 | 原帖确认后的提交失败分支可能造成双账本不一致 | 原帖证据与未知栅栏改为同事务写入，并补故障注入测试 | 已修复 |

## 验证

- 专项：28/28 通过。
- 全量 X：598 tests，597 通过，1 个环境相关 skip。
- `py_compile` 和 `git diff --check` 通过。
