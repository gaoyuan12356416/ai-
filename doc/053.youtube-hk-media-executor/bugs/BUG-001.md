# BUG-001 release 身份门禁拒绝短 SHA 目录

根因：首次建立香港 release 时目录名使用 `f6bef3f`，而运行时门禁要求 `/data/drama-synthesis-gpu/releases/<40位SHA>` 与 `DRAMA_GPU_RELEASE_SHA` 精确一致。

修复：在 CPU 流量切换前将 release 移入完整 SHA 目录、更新受控 systemd drop-in，并重启验证。预检 `issues=[]`，服务 active。此阶段没有活动合成/YouTube 任务，也没有外部发布。
