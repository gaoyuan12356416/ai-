# 部署与回滚

记录 CPU/GPU release、unit/env 哈希并在线备份 SQLite；确认无活动任务。由 GitHub 精确 commit 建香港 immutable release，验证 health；再更新 CPU YouTube 文件并启用 `DRAMA_YOUTUBE_MEDIA_EXECUTOR_URL=http://127.0.0.1:18788`。回滚先移除 CPU 开关并恢复 CPU 文件，再恢复 GPU `current`；保留当前 SQLite/ledger，绝不覆盖发布事实。
