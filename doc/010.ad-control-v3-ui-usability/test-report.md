# 测试报告

## 测试结论

通过并已完成生产验收。R3 主版本 190 项本地自动化、162 项服务器精确提交回归，以及下拉热修 163 项本地和 163 项服务器精确提交回归、语法/静态检查、默认/矮视口真实浏览器验证全部通过；七项用户反馈均已关闭。

## 测试范围

V3 核心、UI 合同、具体产品目录/查询、元数据缓存、脚本语法和生产浏览器。

## 执行统计

| 类型 | 数量 | 通过 | 失败 | 阻塞 |
| --- | --- | --- | --- | --- |
| UI / 可用性 | 64 | 64 | 0 | 0 |
| 核心 / 路由 / Repository / Live | 98 | 98 | 0 | 0 |
| 部署与导航 | 28 | 28 | 0 | 0 |
| 语法/静态检查 | 4 | 4 | 0 | 0 |
| 本地真实浏览器完整流程 | 1 | 1 | 0 | 0 |
| 服务器精确提交 unittest | 162 | 162 | 0 | 0 |
| 收尾热修服务器专项 | 36 | 36 | 0 | 0 |
| 生产浏览器完整流程 | 1 | 1 | 0 | 0 |
| 下拉热修 V3 精确回归（本地） | 163 | 163 | 0 | 0 |
| 下拉热修 V3 精确回归（服务器） | 163 | 163 | 0 | 0 |
| 下拉热修共享 UI / 执行日志回归 | 120 | 120 | 0 | 0 |
| 下拉热修生产浏览器双视口 | 2 | 2 | 0 | 0 |

## 缺陷情况

- BUG-001～006 均已完成自动化、生产浏览器和数据回读，状态关闭。
- BUG-007 已关闭：菜单原高度 300px，但父卡片 `overflow:hidden` 只允许显示约 111px；修复后默认视口和 1582×430 矮视口均完成生产浏览器复测。

## 验证证据

- 生产复现：范围估算在未选择对象的第 1 步提示“请先选择…调控对象”。
- 生产元数据：管理员有效优化师 394 条。
- 源库索引等值实查：FB App ID `1031273318485141` 对应 129 条 `ads_apps_setting`。
- 新具体 W2A 单测证明 SQL 保留 `dpdo` 和绑定 `w2a_page_id`。
- 本地浏览器使用 394 名优化师：关闭菜单时只有 10 个其他原生 select option；搜索 `qa-393` 后优化师结果为 1 条。
- 本地浏览器选择 `drama-double · W2A 1723`，Campaign 范围估算返回 7；页面无“当前层级能力”。
- 本地浏览器“保存并立即试算”创建规则、命中 7 个对象并自动返回规则组列表。
- GitHub-first runtime 提交 `774785c07d37be4aea784015ce2609dd52982bad` 已精确发布；重复 overlay 检查为 `unchanged`。
- 同步 dry-run/apply 均为 129 条，计划哈希 `01d1c0519aa2f5009256a678cac9d08241149e4241da99fd248a4ac419463eae`；63350 回读为 144 条 FB 产品，其中 129 条具体产品且 selector 唯一。
- 错误 plan hash 在写前被拒绝，目录总数前后均为 144。
- 生产优化师目录 394 条；关闭菜单时页面只有 10 个其他原生 option，按邮箱搜索后只剩 1 条。
- 生产选择 `w2a:1723`、优化师 717、最近 1 天 Campaign，范围估算成功：9 个广告账户、98 个对象。
- 生产 observe 规则保存、Top 1 试算并返回列表；两次执行 `meta_write_count=0`，验收规则随后软删除，审计执行保留。
- 收尾热修后再次手动试算，按钮从“试算中…”恢复为“试算”，无需刷新。
- 真实浏览器到规则列表墙钟为冷启动 7166 ms、热缓存 3599 ms；该数值包含浏览器控制与 DOM 快照开销，用于同一验收链路对照。首屏不再构造 394 条优化师节点。
- 下拉布局回归：V3 精确 163 项通过，共享 UI/执行日志组合回归 120 项通过；菜单打开态卡片层级、视口高度变量、向上展开与滚动/resize 重算均有静态合同覆盖。
- 默认 2560×1215 生产视口：菜单高 284px、完整位于视口内；菜单越过卡片底边仍可见，打开态卡片 `overflow:visible; z-index:30`，底部操作栏 `z-index:9`。
- 1582×430 生产视口：滚动至业务范围后菜单自动向上展开为 204px，边界 `12.375～216.375px`；选项区可视高 144px、内容高 6778px、`overflow-y:auto`，未被卡片或底部操作栏遮挡。
- 下拉热修 runtime `3b2e2ca02b161de16c4aa6cdf6bf5f5893ee79a4` 已精确发布；重复 overlay 检查为 `unchanged`，浏览器复测未保存规则或产生业务写入。

## 遗留风险

- TikTok 和更多平台 App ID 批次不在本期；同步脚本已参数化，后续仍必须逐 App ID 做 count/hash 审核。
- 本次未重复制造真实 Meta 暂停/复制对象；既有 live pause/copy 开关仍为 `1`，能力 banner 与 V2 页面回归通过。

## 发布建议

发布完成，可进入用户测试。保留两个代码检查点和一份完整数据盘基线，可按部署文档逐级回滚。
