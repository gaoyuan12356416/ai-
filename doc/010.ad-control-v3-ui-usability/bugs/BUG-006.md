# BUG-006 保存并立即试算不返回列表

## 现象

按钮完成保存和试算后仍停留第 5 步，用户不确定是否完成。

## 根因

`previewGroup(..., true)` 成功分支只重新渲染编辑器。

## 修复

成功后清空 editor/open menu 状态并重新加载规则列表；结束 in-flight 状态后再次重绘，避免已完成的行残留“试算中…”；试算失败仍保留编辑器和已保存版本。

## 回归

自动化、本地和生产通过：保存后命中 Top 1、返回列表；收尾热修后按钮也从“试算中…”恢复为“试算”。验收规则已软删除，审计保留。状态关闭。
