# BUG-006 保存并立即试算不返回列表

## 现象

按钮完成保存和试算后仍停留第 5 步，用户不确定是否完成。

## 根因

`previewGroup(..., true)` 成功分支只重新渲染编辑器。

## 修复

成功后清空 editor/open menu 状态并重新加载规则列表；试算失败仍保留编辑器和已保存版本。

## 回归

自动化已覆盖成功分支状态转换；本地真实浏览器完成创建、命中 7 个对象的试算并返回列表，生产端到端待验。
