# SA 评审意见

## 结论

有条件通过。必须保持旧宽口径产品兼容、具体产品查询有界、同步先 dry-run/hash、保存试算仅在完整成功后退出编辑器。

## 问题清单

| 编号 | 严重级别 | 位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-001 | P0 | 产品目录 | 直接把 129 个平台名当 `insight.product` 会筛不到或扫大表 | 使用目录证据解析到审核的 product，并叠加 App/W2A 身份 | 已采纳 |
| SA-002 | P0 | 产品兼容 | 替换原 15 枚举会使历史规则失效 | 只追加具体产品，旧枚举保留 | 已采纳 |
| SA-003 | P0 | 同步写入 | 源库随时可能新增/删除产品 | apply 强制 expected-count + plan-hash，事务写 | 已采纳 |
| SA-004 | P1 | 查询 | 宽口径与具体范围重复会双重聚合 | 保存/估算时拒绝重叠范围 | 已采纳 |
| SA-005 | P1 | 缓存 | 缓存授权或可变对象会越权/污染 | 只缓存共享选项并深拷贝，TTL 上限 300 秒 | 已采纳 |
| SA-006 | P1 | 保存试算 | 保存成功、试算失败时直接退出会隐藏失败 | 仅试算成功退出；失败保留编辑器 | 已采纳 |

## 决策记录

- 不新建表，复用 catalog JSON 证据字段。
- 具体选择值使用稳定 ID，不使用可变名称。
- 第 2 步承载估算，第 3 步继续按层级提供真实筛选字段。

## PM 修订确认

六项用户反馈全部纳入，未扩大到 TT 或 V2。
