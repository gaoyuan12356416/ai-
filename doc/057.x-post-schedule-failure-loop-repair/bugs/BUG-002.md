# BUG-002 候选查询异常不透明且无有限重试

## 现象

Run 335 仅显示 `OperationalError`，历史底层原因不可恢复，批次直接失败。

## 根因与修复

selector 将全部数据库异常压成类型名，runner 不重连。现改为稳定 `x_post_source_query_failed`、安全阶段和中文提示；保持冻结 pool snapshot，只重连重试当前只读页一次，持续失败终态化且零队列/X。

## 验证

覆盖 material/drama/deploy_time 脱敏与首次成功/持续失败路径。
