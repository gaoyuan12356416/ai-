# BUG-003 跨午夜 stale claim 与活跃 planner 竞态

## 现象

Run 338 23:44:10 开始预检，00:00:01 被停止，原进程 00:04:53 因冻结状态冲突退出。

## 根因与修复

旧逻辑仅以 `run_date < today` 判断 stale。新增独立 `lease_heartbeat_at`，每个候选前续租；只有超过两小时无心跳才停止。独立字段避免推进素材 FIFO 的 validation cutoff。

## 验证

23:44 claim 跨午夜保持 claimed，续租后两小时内不停止，租约过期才进入 stale。
