# BUG-004 已绑定短剧媒体错误导致库存表面充足但可用为零

## 现象

Run 274 的 14 条队列在 X 请求前因 `invalid_media_dimensions` 失败，attempt=0、unknown=0；14 部短剧仍绑定但全部不可用，后续短剧批次报 shortage。

## 根因与修复

现有 backfill 只允许未绑定无历史池项，material recovery 又拒绝 drama。新增精确 manifest、GPU 修复证据、append-only audit、全量验证后单事务 rearm 原队列；不新建队列、不换绑定，测试不触发 X。

## 验证

离线覆盖 validate-only、apply、重复、部分清单、绑定/episode 漂移、attempt/unknown 冲突；生产发布前再次只读核对 14 条。
