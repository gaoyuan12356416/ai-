# 验收用例

- 每类新增恰好5个；原20条资产元数据逐项完全相同。
- 10个PNG像素与已批准设计一致；10个WebM为720x1280/30fps/120帧/4秒、无音轨、alpha有效、循环端点一致。YUV转换允许alpha一档舍入。
- 1000个确定性配方覆盖38个有效资产，新旧选层均可达。
- 新默认目录下，Drama旧SHA在legacy和native入口选回旧目录，未知SHA在渲染前拒绝。
- TT旧ready成片准备复用与发布校验继续使用原SHA；业务身份或配方被篡改仍拒绝。
- 两套缓存各38个有效资产全覆盖，文件/帧/时间戳校验通过。
- 私有GPU样片使用新增的所有四类，每条链路保留原尺寸/编码/时长约束；不调用上传发布。
- CPU目录四组数量为8/10/8/12；TT/FB/Drama真实进程配置一致，健康200，原触发器恢复。
