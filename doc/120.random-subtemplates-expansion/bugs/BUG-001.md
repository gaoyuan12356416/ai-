# BUG-001 目录扩展会拒绝历史冻结配方

状态：已修复；本地回归及生产旧SHA真实渲染验证通过。
原实现把recipe.asset_set_sha256与当前唯一默认目录比较。直接切新目录会拒绝Drama历史任务以及TT已有ready成片的发布校验。
修复：保留不可变旧目录；部署可信SHA→root映射，按冻结SHA选回原目录后继续严格校验。TT在生成新配方前先查旧ready manifest，并保留原业务身份完整比对。
验证：test_drama_catalog_versions.py及TT新增目录兼容回归。
