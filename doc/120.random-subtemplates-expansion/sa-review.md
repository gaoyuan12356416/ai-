# 设计评审

通过，前提如下：
- 不在旧路径覆盖manifest，否则TT已有ready成片发布重试及Drama冻结配方会失败。采用不可变新目录和可信SHA映射。
- TT线上基线16ae57a，Drama线上a93b541，FB线上e3bef98含新并发保护。分别保留各自基线；FB仅改目录配置。
- 保留light两项，满足现有严格manifest五类约束，light仍不参与选层。
- 两套缓存按源文件SHA增量建立；缓存验证通过才切默认。
- 回滚默认目录不能删除新目录或退回不认识新SHA的旧代码，除非证明无新冻结任务。
