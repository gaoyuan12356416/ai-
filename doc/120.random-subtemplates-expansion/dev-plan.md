# 实施计划

1. 从已批准的程序化设计抽取纯图层源码，生成10个PNG和10个透明WebM，附原manifest及追加后的manifest。
2. 独立TT工作树增加可信目录helper和旧ready成片兼容；Drama统一渲染入口使用相同helper。
3. 验证旧条目完全一致、38个有效资产可被选中、尺寸/透明度/循环/视频时间轴与预览一致。
4. 运行目录兼容、Drama目录与渲染路径相关测试及py_compile；GitHub提交后由服务器fetch精确提交。
5. 离线准备共享素材目录、TT/FB v1缓存、Drama demux-v2缓存，真实GPU短样片验证。
6. 保存CPU/HK配置和指针、暂停TT触发器与FB准备timer、排空制作子进程、窄范围切换。最后更新CPU目录并恢复触发器。
7. 校验所有入口目录数量/旧配方兼容/健康和回滚记录。

不覆盖主仓库或其他任务的脏文件；不进行数据库迁移。
