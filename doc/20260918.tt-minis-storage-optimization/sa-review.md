# 设计复核

P1 已处理：文件 mtime 不能代表最后引用时间。旧 manifest 在退出时存档，24 小时内的引用全部保留。
P1 已处理：按固定最近两天跳过历史会遗漏回填。使用每个日期/层级的刷新台账版本；刷新时间改为微秒精度。
P1 已处理：回收不得影响硬编码历史分析或正在打开的文件。扫描源码/配置、租约、pin、FD，并核对文件身份；旧文件只允许显式审计清单回收。
评审方式：同一执行者逐项复核，未声称独立人员评审。
