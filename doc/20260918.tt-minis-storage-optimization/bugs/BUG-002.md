# BUG-002 引用预检将无扩展名的 ELF 工具误当源码

状态：修复并补充回归。
服务器预检在 ffmpeg 二进制文件触发 4 MiB 源码扫描上限，安全停止，未删除文件。
根因：无扩展名 cron/shell 文件与编译后程序共用扫描规则。
修复：只对无扩展名文件识别 ELF/PE 文件头并跳过二进制；大型源码仍拒绝清理且报告路径。没有通过放宽扫描上限绕过保护。
验证：增加二进制跳过与大型文本拒绝两例；本地/服务器 unittest。
