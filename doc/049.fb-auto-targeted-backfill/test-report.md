# 测试报告

## 测试结论

本地功能、边界和 FB 全量回归通过；生产 dry-run、建单和外部发布终态尚待 GitHub-first 部署后执行，因此当前结论为“准予受控部署，不代表回补已完成”。

## 测试范围

定向白名单、来源校验、模板竞态、Page 范围漂移、Token 计数指纹、操作号幂等、重复来源拒绝、报告路径、普通自动计划/预制/发布/对账/API/部署契约。

## 执行统计

| 类型 | 数量 | 通过 | 失败 | 阻塞 |
| --- | --- | --- | --- | --- |
| 恢复专项 + store | 52 | 52 | 0 | 0 |
| FB 自动发布全量回归 | 141 | 141 | 0 | 0 |
| 生产 dry-run | 1 | 0 | 0 | 1（待部署） |
| 生产 Page 终态 | 5 | 0 | 0 | 5（待建单） |

## 缺陷情况

开发中发现 1 个本地兼容问题（BUG-001），已修复并回归通过；无未关闭缺陷。

## 验证证据

- 52 个专项/store 测试耗时约 8.4 秒，PASS。
- 141 个 `test_fb_auto*.py` 全量测试耗时约 16 秒，PASS。
- `py_compile` 与 `git diff --check` PASS。
- 生产证据（备份、commit、dry-run 指纹、run/task/ledger/post id）部署后补录。

## 遗留风险

GPU 制作时间、Graph 网络状态和 Token 在建单后的可用性属于生产动态因素；由现有重试/切换/reconcile 处理，必须观察到无 unknown 的终态。

## 发布建议

允许按部署文档执行单次受控生产恢复；apply 前必须再次核对固定 5 个 Page、1 个实时阻塞 Page、来源 run 20 和报告路径。
