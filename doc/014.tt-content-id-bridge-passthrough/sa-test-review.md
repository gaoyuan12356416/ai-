# SA 测试用例评审

## 结论

通过。用例覆盖正常透传、编码/重复语义、核心覆盖、边界输入、移动端与生产访问。

## 覆盖性问题

| 编号 | 场景/用例 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- |
| TR-01 | 核心参数 | 需要覆盖大小写变体 | 契约测试加入 `AF_DP` 等变体 | 已采纳 |
| TR-02 | 跳转安全 | 需要证明目标域名不可改变 | 检查生成 URL 的 origin 与 pathname | 已采纳 |
| TR-03 | 外部剧集解析 | HTTP 200 不能证明剧集有效 | 只把最终 URL 结构和实际导航作为本需求证据 | 已采纳 |

## QA 修订确认

2026-07-24 已补充大小写保护和固定目标断言。
