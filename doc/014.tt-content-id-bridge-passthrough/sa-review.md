# SA 评审意见

## 结论

通过，可进入开发。方案保持固定目标域名与核心参数，透传逻辑完全在浏览器内完成，不引入后端状态。

## 问题清单

| 编号 | 严重级别 | 位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-01 | 高 | 参数透传 | 外部参数可能覆盖 `af_dp/c/af_c_id` | 核心键大小写不敏感地加入保留集合 | 已采纳 |
| SA-02 | 中 | 参数透传 | 无边界会放大超长 URL 与滥用风险 | 限制参数数量、键和值长度 | 已采纳 |
| SA-03 | 中 | 跳转 | 可配置目标会形成开放重定向 | 目标域名和路径写死，不接受目标参数 | 已采纳 |
| SA-04 | 低 | 页面展示 | 回显追踪值可能暴露信息 | 只展示透传数量，不展示值 | 已采纳 |

## 决策记录

- 使用纯静态 HTML + JavaScript；生产短路径为 `/tt`，由精确 Nginx location 直接映射。
- 不新增数据库或剧库查询；`content_id` 的解析责任继续由 W2A 承担。
- 保留合法重复参数，避免改变追踪系统的原始语义。

## PM 修订确认

2026-07-24 已将以上安全边界写入需求与验收标准。
