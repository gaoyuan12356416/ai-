# 测试用例

## 测试范围

参数过滤、URL 生成、移动端交互、生产静态访问和实际 W2A 导航。

## 测试数据

- `content_id=l9rP6ey2CB`
- `af_adset_id=XXX`
- 核心参数覆盖尝试：`af_dp=evil&c=evil&af_c_id=evil`
- 编码值与重复参数：`utm_term=hello world&tag=a&tag=b`

## 用例列表

| 编号 | 场景 | 前置条件 | 步骤 | 预期结果 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-01 | 单个附加参数 | 入口带 `af_adset_id=XXX` | 输入 `l9rP6ey2CB` 并搜索 | 最终 URL 与需求示例完全一致 | P0 | 通过 |
| TC-02 | 多参数与编码 | 入口带空格、Unicode 等编码值 | 搜索并解析最终 URL | 参数语义保持一致 | P0 | 通过 |
| TC-03 | 重复参数 | 入口带 `tag=a&tag=b` | 搜索并检查目标 | 两个值按顺序保留 | P1 | 通过 |
| TC-04 | 核心覆盖保护 | 入口传入三个核心键 | 搜索并检查目标 | 外部值被忽略，固定值不变 | P0 | 通过 |
| TC-05 | 页面控制键 | 入口传入 `content_id/cid/auto/preview` | 搜索并检查目标 | 控制键不透传 | P1 | 通过 |
| TC-06 | 非法或超限参数 | 非法键、超长值或超过 40 个参数 | 搜索并检查提示 | 非法项忽略，页面提示跳过数量 | P1 | 通过 |
| TC-07 | 非法 Content ID | 输入不足 10 位或含非法字符 | 点击搜索 | 不生成结果链接并提示输入完整 ID | P0 | 通过 |
| TC-08 | 移动端布局 | 390×844 真实浏览器 | 打开、输入、搜索、截图 | 页面无横向溢出，按钮可点击 | P1 | 通过 |
| TC-09 | 生产访问 | 已部署 | 打开公开 URL | 无登录跳转，HTTP 200 | P0 | 通过 |
| TC-10 | 实际 W2A 导航 | 有效示例 ID | 点击结果按钮 | 新 URL 包含完整固定和附加参数 | P0 | 通过 |

## 回归范围

- AI 后台首页与现有静态页仍可访问。
- `drama-material-api.service` 状态不受纯静态发布影响。

2026-07-27 回归复测：10/10 通过。
