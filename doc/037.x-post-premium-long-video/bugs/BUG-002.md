# BUG-002 候选文案预检调用参数落后于模板函数签名

## 状态

已修复。

## 现象

新计划候选在 `_plan_candidate` 中调用 `build_post_text` 时只传短链和描述，而当前函数还要求 `drama_name`，导致新鲜候选在本地文案门禁全部失败。

## 根因

文案模板函数升级后，runner 的预检调用未同步。

## 修复

传入短链、剧名、描述和可选冻结模板，保持与正式发布渲染入口一致。

## 回归证据

每日发布/路由套件 55 项通过；完整 X 回归 381 项通过。
