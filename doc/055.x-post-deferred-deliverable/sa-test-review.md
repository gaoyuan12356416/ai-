# SA 测试用例评审

## 结论

通过。P0 用例覆盖“仍未来”“正好到时”“历史旧行”“本次证据”“已有队列”五个最易
造成提前发布或永久冻结的边界。

## 覆盖性问题

| 编号 | 场景/用例 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- |
| STR-001 | TC-003/004 | 只测候选查询无法证明事务层不会误放过旧证据 | 同时覆盖本次刷新与旧时间戳两种 FIFO 结果 | 已关闭 |
| STR-002 | TC-005 | 单纯清错测试可能绕过队列原子性 | 断言清错和队列绑定在同一次 reserve 中完成 | 已关闭 |
| STR-003 | TC-006/007 | UI 标签与 API 枚举可能各自通过但不一致 | 增加静态页面合同测试和主 API 参数测试 | 已关闭 |
| STR-004 | TC-010 | 真实 Post 不是必要验收 | 使用自然 timer、只读账本和计数不变量 | 已关闭 |

## QA 修订确认

已按评审补齐用例，允许进入实现。
