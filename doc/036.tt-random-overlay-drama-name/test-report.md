# 测试报告

## 测试结论

通过。文案宏、配方冻结、资产防篡改、五层堆叠、黑底转透明、旧 profile 回归均符合要求。测试没有调用 TikTok 发布接口，也没有生成新的 `publish_id`。

## 测试范围

- TT 素材池、立即测试、排期队列和自动发布的 `{{drama_name}}` 渲染与缺失阻断。
- `random_overlay` 的五类随机选择、旋转/缩放/透明度边界、同任务重试配方复用。
- 20 个附件模板的完整转换、SHA 清单、720x1280 与动态 Alpha 标记。
- 一次真实五层 FFmpeg 离线合成，包含原音频和完整 4 秒时长。
- 全部 `scripts/test_tt_*.py` 回归。

## 执行统计

| 类型 | 数量 | 通过 | 失败 | 阻塞 |
| --- | ---: | ---: | ---: | ---: |
| TT 自动化测试 | 574 | 574 | 0 | 0 |
| 真实附件资产构建 | 20 | 20 | 0 | 0 |
| 离线完整合成 | 1 | 1 | 0 | 0 |

## 验证证据

- 资产 manifest SHA-256：`028326ab211418934b026c227f2e3707553cce7560551dca3c0bfddc681d566f`。
- 离线样例配方：旋转 `-1.686°`、缩放 `98.08%`、透明底 `16.53%`，五类各选一个文件。
- 离线输出：H.264 `720x1280 yuv420p`、AAC 48kHz 双声道、时长 `4.000000s`。
- 抽帧人工检查：光效和透明度视频均未产生不透明黑色矩形。

## 遗留风险

- 真实长视频的 GPU 编码耗时和输出码率仍需在生产 GPU 上用只 prepare、不 publish 的受控样本确认。
- 附件动效本身较明显，这是模板设计效果；代码未额外降低边框、光效和四角动效的不透明度。

## 发布建议

按 `deploy.md` 进行三方配对切换。先完成 GPU 离线 prepare 和健康检查，再恢复自然调度；不得为了验收主动创建真实 TikTok 帖子。
