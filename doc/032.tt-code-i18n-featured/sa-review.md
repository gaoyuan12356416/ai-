# SA 评审意见

## 结论

有条件通过。采用“新增静态分语言快照 + 新 `/tt-code` 客户端选桶”的兼容设计；不得修改旧 `/tt` 或旧 Featured 快照合同。

## 问题清单

| 编号 | 严重级别 | 位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-001 | 高 | Featured API | 旧 `/tt` 依赖固定 v1 快照，直接改 schema 会破坏旧页面 | 新增独立 `featured-by-language` 文件和 Nginx exact location | 已采纳 |
| SA-002 | 高 | 数据刷新 | 多语言文件逐个写会让一次请求读到不同代快照 | 所有桶合并成一个文件并原子替换 | 已采纳 |
| SA-003 | 高 | 跳转 | 语言参数若进入 resolver/目标 URL 会破坏冻结参数合同 | 语言仅用于 UI 和本地榜单选桶 | 已采纳 |
| SA-004 | 中 | 语言兼容 | 浏览器地区标签与数据源语言值可能不同 | 建立固定别名和 `exact -> base -> en` 回退 | 已采纳 |
| SA-005 | 中 | 可用性 | 某个小语种不足 5 条可能拖垮整个榜单 | 非英文不完整桶跳过；英文失败则保留上次完整快照 | 已采纳 |
| SA-006 | 中 | 隐私 | 排名来源含消耗，不应传给浏览器 | 公网 schema 明确拒绝 `spend` / `spend_n` | 已采纳 |

## 决策记录

- 标题下“副标题”定义为截图红框内的说明段落；上方 eyebrow 保留并翻译。
- 页面翻译使用内置静态字典，禁止在线翻译和 `innerHTML` 注入。
- 新接口静态读取数据盘文件，不把查询流量打到 MySQL、Redis或应用进程。
- 旧全局榜单刷新与文件继续保留，作为旧 `/tt` 兼容合同。

## PM 修订确认

2026-08-05：用户已经给出明确变更要求，无新增业务阻塞问题；按上述兼容边界执行。
