# 测试用例

## 测试范围

CPU 服务、GPU worker、管理页静态契约、现有正式发布/每日排期回归，以及生产部署后的只读状态。

## 测试数据

- 单元测试：临时 SQLite、伪账号、伪 GPU、伪 TikTok API。
- 生产白名单：账号 640、素材 5391678、对应 GPU Job 与 Drama ID。
- 测试不得由部署脚本或验证脚本触发真实发布。

## 用例列表

| 编号 | 场景 | 前置条件 | 步骤 | 预期结果 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-001 | 默认关闭 | 正式门禁关闭、白名单关闭 | 人工运行 | 门禁失败，素材不消耗 | P0 | 待执行 |
| TC-002 | 自动任务隔离 | 白名单开启、正式门禁关闭 | 运行自动 due | 不创建可发布队列 | P0 | 待执行 |
| TC-003 | 账号不匹配 | 白名单开启 | 用其他账号人工运行 | 拒绝且不调用 GPU 发布 | P0 | 待执行 |
| TC-004 | 素材或 Job 不匹配 | 白名单开启 | 下一条素材不匹配 | 预检失败，素材释放 | P0 | 待执行 |
| TC-005 | CPU 正确人工路径 | 精确目标已就绪 | 人工运行 | 创建 direct_post 队列并强制 SELF_ONLY/关闭互动与商业开关 | P0 | 待执行 |
| TC-006 | UI 按钮 | 服务返回 canary ready | 加载排期 | 正式门禁关闭时按钮仍可用并显示私密测试提示 | P0 | 待执行 |
| TC-007 | 其他队列仍阻断 | 白名单开启 | runner 领取非白名单队列 | 队列 blocked_compliance | P0 | 待执行 |
| TC-008 | GPU 请求校验 | 白名单开启 | 错账号/Job/Drama ID/隐私/互动 | 网络调用前拒绝且无账本 | P0 | 待执行 |
| TC-009 | GPU 来源校验 | 精确目标但 URL origin 错误 | 发布 | 网络调用前拒绝 | P0 | 待执行 |
| TC-010 | GPU 精确白名单 | 精确目标且旧 manifest 不可直发 | 发布 | 允许进入 TikTok init，request 为 SELF_ONLY | P0 | 待执行 |
| TC-011 | 明确拒绝不重试 | TikTok 返回明确错误 | 再次发布同一 Job | 首次写拒绝账本，第二次被本地阻断 | P0 | 待执行 |
| TC-012 | 未知结果不重试 | TikTok 超时/5xx | 再次发布同一 Job | 首次写 unknown 账本，第二次被本地阻断 | P0 | 待执行 |
| TC-013 | 正式路径回归 | 三重门禁开启 | 普通 direct_post | 行为不变 | P1 | 待执行 |
| TC-014 | 生产部署只读验收 | 两端部署完成 | 查 health、排期、队列 | canary ready；排期 0；队列和 publish_id 未增加 | P0 | 待执行 |

## 回归范围

- 素材校验和后台预制作。
- 账号设置与 creator info。
- 每日排期保存、due 执行与恢复。
- 队列 claim/publish/reconcile。
- GPU prepare、storage、publish ledger 和 HTTP 契约。
