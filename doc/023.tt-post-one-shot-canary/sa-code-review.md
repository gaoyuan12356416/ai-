# SA 代码评审

## 结论

通过，允许进入部署阶段。正式门禁未被改写，一次性通道为独立、短时、精确目标、服务端强制私密的例外。

## 评审范围

- CPU 配置、人工运行、队列冻结、claim/publish 边界。
- GPU 配置、独立路由、凭据绑定、manifest 校验和发布账本。
- TikTok 错误字段安全保留。
- 管理页按钮、每日排期锁定和确认提示。
- 自动化测试与部署边界。

## 问题清单

| 编号 | 严重级别 | 文件/位置 | 问题 | 修复 | 状态 |
| --- | --- | --- | --- | --- | --- |
| CR-001 | 阻断 | `credentials.py` | 新 operation 未加入凭据白名单 | 增加 `canary_publish` 并测试不能重放为普通 publish | 已修复 |
| CR-002 | 阻断 | `service.py` | 初版把 canary 条件误放到素材预制作，导致未定义变量 | 恢复预制作原逻辑，只在人工发布预检跳过旧公开设置 | 已修复 |
| CR-003 | 高 | CPU/GPU 配置 | 过期许可若导致服务重启失败，会影响正常业务 | 允许格式正确的过期许可启动为 inactive | 已修复 |
| CR-004 | 高 | CPU 队列 | 仅按账号/人工类型可能误认历史普通运行 | run key、pool、完整身份与无启用排期共同校验 | 已修复 |
| CR-005 | 高 | GPU | 仅绕过门禁会接受错误成片 | 增加 Job、素材、Drama ID、SHA、大小、profile、origin、path 校验 | 已修复 |
| CR-006 | 高 | TikTok API 客户端 | 丢失原始 code/message/HTTP status | 安全清洗后写入 GPU ledger，并在 CPU 错误信息展示 | 已修复 |

## 编译 / 验证结果

- Python 语法编译通过。
- 目标单元测试 220 项通过。
- `git diff --check` 通过。
- 生产发布动作未由测试或部署流程触发。
