# SA 评审意见

## 结论

通过。采用服务端统一资格字段和最终凭据闸门，避免只在页面隐藏造成绕过。

## 问题清单

| 编号 | 严重级别 | 位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-01 | 高 | 发布链路 | 仅过滤页面候选无法覆盖旧排期或直接队列发布 | 在 `publish_credentials` 再次校验许可 | 已解决 |
| SA-02 | 中 | 数据迁移 | 历史账号默认值若为开启会扩大授权范围 | 默认 0，管理员显式勾选 | 已解决 |
| SA-03 | 中 | 并发 | 取消许可可能与发布竞态 | 与发布共用账号锁 | 已解决 |

## 决策记录

- 对外保留 `publish_eligible` 字段，但语义收紧为“凭据有效且管理员允许”。
- 新增 `credential_publish_eligible` 用于页面解释账号自身状态，不供发布决策。
- 取消许可不清理排期，避免不可逆配置变化。

## PM 修订确认

需求与验收标准已补充默认关闭、历史配置保留和最终发布闸门。
