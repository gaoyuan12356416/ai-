# 测试用例

## 测试范围

迁移、权限、资格计算、内部/外部 API、UI 契约、最终发布闸门及既有素材/短剧排期回归。

## 测试数据

临时 SQLite、伪造 OAuth Token 和本地 sidecar HTTP 服务；不调用真实 X 发布接口。

## 用例列表

| 编号 | 场景 | 前置条件 | 步骤 | 预期结果 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-01 | 历史库迁移 | 无许可列 | 连续执行两次初始化 | 加列成功且旧行值为 0 | P0 | 通过 |
| TC-02 | 新账号默认值 | 完成 OAuth | 查询账号 DTO | 凭据有效但不可发布 | P0 | 通过 |
| TC-03 | 管理员启停 | 管理员登录 | 勾选后再取消 | 资格随许可变化且记录事件 | P0 | 通过 |
| TC-04 | 普通用户修改 | 普通用户登录 | 调用许可接口 | 返回 `x_admin_required` | P0 | 通过 |
| TC-05 | 最终发布闸门 | 账号有效但未许可 | 获取发布凭据 | 返回 409，不产生发布 | P0 | 通过 |
| TC-06 | 页面交互 | 管理员列表 | 勾选/取消 | 调用管理员 API 并刷新状态 | P1 | 通过 |
| TC-07 | 发布池和排期回归 | 混合账号 | 运行既有测试 | 仅有效许可账号进入候选 | P0 | 通过 |

## 回归范围

执行 `scripts/test_x*.py` 全量回归，共 354 项，通过 354 项，跳过 1 项环境相关用例。
