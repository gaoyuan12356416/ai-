# 测试用例

## 测试范围

语言规范化、语言候选隔离、模板兼容、临时 SQLite 运行规划、冻结与幂等。用 `scripts/test_fb_auto_v2.py` 的时钟和既有模板 fixture 构造独立测试；没有 Graph、GPU、真实 MySQL 或发布请求。

## 测试数据与用例

Page 使用 `en`、`English`、`ES`、空字符串、非法格式；英语素材 `101`，西语素材 `201`，独立剧、描述、媒体 URL 和指标代次；旧模板语言设为 `fr`。

| 编号 | 场景与步骤 | 预期结果 | 优先级 | 状态 |
| --- | --- | --- | --- | --- |
| TC-01 | 别名、大小写、空白和区域下划线 | 规范化为标准格式 | P1 | 通过 |
| TC-02 | 空或非法语言 | 空结果，无默认语言 | P1 | 通过 |
| TC-03 | 多个 Page 共享语言别名 | 每种语言只查一次，配置不被改写 | P1 | 通过 |
| TC-04 | 所有 Page 语言为空但模板有语言 | 不读取模板语言候选 | P1 | 通过 |
| TC-05 | 适配器返回混杂语言 | 删除非 Page 语言候选 | P1 | 通过 |
| TC-06 | 候选查询异常 | 向上传递，不改用模板语言 | P1 | 通过 |
| TC-07 | 新建请求省略语言 | 通过且保存不含语言 | P1 | 通过 |
| TC-08 | 旧请求携带语言 | 接收但新保存配置不含语言 | P1 | 通过 |
| TC-09 | 英西语混合 Page 规划 | 剧、素材、描述和语言快照各自匹配；指标代次保留 | P1 | 通过 |
| TC-10 | 已存旧模板 `fr`、Page `es` | Page 选择西语，旧版本可读取 | P1 | 通过 |
| TC-11 | 有效、空、非法语言混合 | 只跳过无效 Page，并保留明确原因 | P1 | 通过 |
| TC-12 | 全部语言无效 | 全部 skipped，零素材查询 | P1 | 通过 |
| TC-13 | 本语言素材为空 | `fb_auto_no_eligible_video`，不借用其他语言 | P1 | 通过 |
| TC-14 | Page 语言有效但无 Token | 保持原 Token 跳过原因，不调用选材 | P1 | 通过 |
| TC-15 | 两次 Page 读取间出现新语言 | `fb_auto_page_language_changed`，零运行/任务落账 | P1 | 通过 |
| TC-16 | 两次读取间清空语言 | 跳过，不使用第一次语言素材 | P1 | 通过 |
| TC-17 | 后续时隙修改 Page 语言 | 新时隙按当前语言；之前所有任务/快照字段不变 | P1 | 通过 |
| TC-18 | 原时隙重复提交并修改语言 | 返回原运行，不读候选，不改冻结选择 | P1 | 通过 |
| TC-19 | 本语言素材已进入冷却 | 跳过，不跨语言兜底 | P1 | 通过 |
| TC-20 | 已建立未来运行扩充缺失 Page | 仅追加准确 Page，原任务、媒体和账本完整保留 | P1 | 独立测试通过 |
| TC-21 | 模板浏览器编辑与保存 | 无语言控件，省略语言成功保存；旧模板正常加载 | P1 | 正式 URL 未登录 DOM 通过；登录与保存未执行 |
| TC-22 | 部署后的计划、制作和发布 | 精确发布版本、运行范围、实际语言与平台结果可核对 | P1 | 最终90faff9已上线，732条追加与历史不变读回通过；制作中，实际发布完成待验收 |
| TC-23 | 公开模板传入 `_candidate_limit` | 拒绝计划器内部参数覆盖 | P1 | 通过 |

## 未来运行扩充独立套件

`scripts/test_fb_pool_extension.py` 包含 19 项：范围指纹稳定性、仅追加缺失 Page、原媒体/任务/快照/发布尝试不变、相同操作幂等、回执恢复不依赖仓库、历史运行拒绝、小于十分钟拒绝、手动运行拒绝、第二个运行冷却耗尽整批回滚、范围变化、重复 Page、版本变化、停用模板、unknown/running/submitted 阻断、跨运行同 Page 同时间阻断、原 Page 移出范围、缺语言/授权、缺本语言素材、容量上限与新增任务发布下限。

首次执行确认回执恢复存在 BUG-001；修复后 19 项全部通过，与语言路由 20 项合并回归为 39/39。

## 回归范围

既有 FB 校验、仓库筛选、运行状态、计划租约、容量、素材冷却、预制作、短链、发布及未知结果保护，由主任务执行完整适用套件并报告 186 项通过（22.331 秒）。独立套件额外包含公开请求拒绝内部候选上限的用例。

后续容量 `284f06e` 的 4 项验证通过（0.111 秒）；最新完整套件 192 项通过（23.191 秒）。最终生产 7 种语言各 500 候选均已审核冻结，732 条已追加，6 个运行各 145 唯一 Page、原历史三表逐行不变。操作 CLI 已在维护窗口执行，详见 `deploy.md`。

## 服务器巡检用例

`scripts/test_fb_readiness_watch.py` 17/17 通过（0.014 秒）：145 Page 覆盖与重复、失败/unknown/终止失败/跳过、submitted 与 published 区分、跨时隙与具体输入时长累计 ETA、制作完成不占 ETA、30 分钟未领取与 2 小时未确认边界、语言不符、健康接口错误/超时。仅运行本套件，未扩大回归或写入生产。
