# SA 测试用例评审

## 结论

2026-09-18：语言路由 20 项、未来运行扩充 19 项独立测试覆盖主要边界且全部通过。主任务已提供 732 条生产追加、每运行 145 唯一 Page 和原历史三表逐行不变证据；生产制作中，登录保存未执行，实际发布完成单独验收。

| 编号 | 覆盖点 | 处理 | 状态 |
| --- | --- | --- | --- |
| STR-01 | 只测 helper 无法证明任务正确 | 增加 SQLite 混合语言规划及剧/描述/媒体断言 | 已完成 |
| STR-02 | 候选读取后的 Page 语言变化 | 新语言导致整次延后；清空语言只跳过 Page | 已完成 |
| STR-03 | 模板兼容与历史不变 | 注入真实旧版本配置；比较之前任务和快照全部字段 | 已完成 |
| STR-04 | 空候选与冷却后的错误回退 | 独立测试无素材和本语言冷却耗尽 | 已完成 |
| STR-05 | 未来运行扩充范围 | 增加 19 项独立测试，覆盖追加不变性及多运行事务回滚 | 全部通过，BUG-001 已关闭 |

增加公开模板拒绝 `_candidate_limit` 的用例，防止内部性能参数成为外部可调的选材边界。

未使用真实发布作为单元测试手段；生产发布仅由主任务按用户授权范围执行并记录结果。

主任务使用既有 10 组真实发布耗时校准容量，并报告后续 4 项容量测试通过；QA 本轮未执行真实发布或新增测试。操作 CLI 的并发隔离依赖维护窗口，现有 39 项单元/集成测试不涵盖不停机在线 apply。

后续按用户要求新增服务器巡检独立套件 17 项并全部通过（0.014 秒），覆盖跨时隙/视频时长 ETA、145 Page 覆盖、失败与未知结果以及未领取/未确认边界。Codex heartbeat 已暂停，server timer 有限运行；生产 unit 报告不等同于发布成功证明。
