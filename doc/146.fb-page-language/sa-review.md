# SA 评审意见

## 结论

2026-09-18：Page 语言作为唯一选材依据的设计通过；独立验证和主任务完整 FB 回归通过。最终应用 `90faff9` 已部署，未来运行 `119..124` 的 732 条追加完成，6 个运行各 145 个唯一 Page、原历史三表逐行不变；生产制作继续运行。

| 编号 | 级别 | 位置 | 问题与建议 | 负责人 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-01 | P1 | 计划器第二次 Page 读取 | 新语言可能未有候选快照；整次延后并零落账 | 实现负责人 | 已落实，独立测试通过 |
| SA-02 | P1 | 旧模板配置 | 读取旧字段不能恢复模板语言兜底 | 实现负责人 | 已落实，旧配置测试通过 |
| SA-03 | P1 | 历史与未来运行 | 语言修改不得重写已冻结任务；扩充只能追加缺失 Page | 实现负责人 | 历史冻结、追加不变性、BUG-001 修复均已回归通过 |
| SA-04 | P2 | 候选适配器 | 防止查询适配器错误返回其他语言素材 | 实现负责人 | helper 再次过滤，测试通过 |

## 决策记录

语言格式复用既有 `LANGUAGE_RE`，不另建支持语种白名单。无素材与缺语言区分记录；容量中的可发布 Page 数仍沿用 Token 资格口径，不能当成实际发布成功数。

候选数量按最大占用动态限定，以减少多语言重复全量扫描；精确排序、排除占用后的前 50 选择和事务内增长复查共同保证选择语义。该参数属于计划器内部输入，必须继续拒绝公开模板请求覆盖。

扩充 CLI 采用维护窗口执行：五类自动 timer 暂停且活动/未确认发布归零，候选与 metadata 完成后冻结并记录 SHA，从副本预演到 apply 后读回保持暂停。脚本不具备无需暂停的在线扩充保证。
