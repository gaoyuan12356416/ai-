# 测试报告

## 测试结论

2026-09-18：Page 语言与未来运行扩充独立套件 39/39 通过，原 186 项回归证据保留；主任务最新完整套件 192 项通过（23.191 秒），最终应用 `90faff9` 已上线。只读巡检新增独立套件 17/17 通过（0.014 秒）。17:19:48 主任务已读回 732 条追加完成，6 个运行各 145 个唯一 Page、原历史三表逐行不变；生产制作正在运行，尚不代表全部发布完成。

## 范围与结果

| 类型 | 数量 | 通过 | 失败 | 待执行 |
| --- | --- | --- | --- | --- |
| 语言 helper 单元测试 | 6 | 6 | 0 | 0 |
| 模板兼容与内部参数保护 | 3 | 3 | 0 | 0 |
| SQLite 计划器集成测试 | 11 | 11 | 0 | 0 |
| 未来运行扩充测试 | 19 | 19 | 0 | 0 |
| 服务器只读巡检准确性测试 | 17 | 17 | 0 | 0 |
| 未登录浏览器 DOM 读回（主任务） | 1 个场景 | 1 | 0 | 0 |
| 语言版本部署读回（主任务） | 1 个场景 | 1 | 0 | 0 |
| 生产追加与历史不变读回（主任务） | 1 个场景 | 1 | 0 | 0 |
| 登录后保存、实际发布完成验收 | 2 个场景 | 0 | 0 | 2 |

覆盖混合语言、别名去重、错误候选隔离、空语言/空素材/Token 原因、二次读取语言变化、剧/媒体/描述/短链冻结、旧模板兼容、冷却、重复时隙、追加不变性、跨运行事务回滚、未来/版本/范围约束、未知结果阻断和持久回执恢复。QA 无真实网络发布。

## 验证证据

工作目录：`worktrees/fb-145-capacity-20260918`。

```text
python -m unittest scripts.test_fb_page_languages scripts.test_fb_pool_extension -v
Ran 39 tests in 5.694s
OK
```

模板 HTML/JS 与输入校验差异静态审阅完成；主任务在正式 URL 的未登录浏览器中确认语言控件为 0、帮助文字与脚本版本正确，未登录或保存模板。主任务提供的完整 FB 回归结果为 186 项、22.331 秒；独立套件还包含其后新增的公开模板拒绝 `_candidate_limit` 用例。两组测试有重叠，不累计为独立总数。

主任务提供的语言上线证据：CPU `4201910`，公开 HTML/JS HTTP 200 且 hash 一致，sidecar `/health` 200，原有 1511 tasks / 2245 attempts / 1205 ledger 均未变。

容量阶段由主任务报告已部署 `e3bef98` 至 CPU/GPU，随机排重 8 项通过，性能输出哈希完全一致、加速比 1.169。后续 `284f06e` 已部署，依据 10 组每组 4 条真实发布的 22.93–57.72 秒耗时，调整为 8 路持续发布、30 分钟领取窗口/预算、service 60 分钟，保留全局 Graph 0.5 秒间隔；预制作 lookahead 2 天，GPU 2 并发/CPUQuota 400%/MemoryMax 8G 不变。新增容量 4 项测试通过（0.111 秒）；它们不改变本报告已记录的 39 与 186 项证据。

## 缺陷与风险

已发现并关闭 BUG-001：已成功扩充的相同操作重试不再依赖实时素材查询。原 39 项独立套件无未解决失败。

后续确认 BUG-002：TL 完整扫描候选为 0，根因是历史自有 COS 素材使用 HTTP 被 `_https` 过滤，并非缺视频或描述。五个抽查素材的同对象 HTTPS HEAD 均为 200/video/mp4，TL 描述唯一非空且剧已上线。精准 bucket HTTP→HTTPS 修复 `f7cb58f` 已部署，TL 已重建 500 条候选，缺陷关闭。

191 项首次的两项旧预期（保留的新 quick-nav 资源版本、publisher 8 路）已更新；主任务最新 192 项全套通过，23.191 秒。该结果与原 39/186 项证据分开保留，不累计重叠测试数量。

最终用于追加的 7 种语言 en/pl/es/id/th/zh-tw/tl 各 500 条候选均已审核，缓存 `0440` 冻结，SHA 为 `f6ee45c6e294eae76a493ff06e83dd753ae36e23b91d05dde4afc71801c12b1c`。副本和真实 apply 均证明旧 1511 tasks / 2245 attempts / 1205 ledger 逐行不变，6 个运行各 145 个唯一 Page，732 条追加完成。今天新增 122 个 Page 为北京时间 21:30，旧 23 个仍为 18:54。

只读 CLI 审查确认：live 绑定与历史事实核对位于提交后，因此依赖全部自动写入暂停的维护窗口，不能在线无暂停执行。主任务已在候选冻结、预演、apply 与读回完成后恢复五类 timer 为 active，任务 1512/1513 自然进入 preparing。今天新选片平均时长为 307.04 秒，时长修正的模型估算 3.892 小时、约 17 分钟余量，并非实际完成保证。登录后的保存操作尚未验证。

## 发布建议

语言与 HTTPS 修复均已上线，732 条未来运行追加及历史不变读回已通过，生产制作已自然启动。服务器巡检代替已暂停的 Codex heartbeat，持续区分制作、领取和实际发布状态，不自动补发。不得重放历史时隙来补测试证据。

## 服务器巡检独立验收

`python -m unittest scripts.test_fb_readiness_watch -v`：17 项通过，0.014 秒。覆盖 145 Page 每运行覆盖、缺页与重复 Page、unknown/failed/failed_without_retry/skipped 不报健康完成、submitted 不计 published、累计跨时隙 ETA、输入视频时长变化影响后续时隙余量、已制作任务不占制作 ETA、30 分钟领取宽限、2 小时未确认边界、语言匹配及健康接口失败/超时。未确认告警不改变任务状态、不触发重试。

审阅期间发现 `failed_without_retry` 初始未纳入异常，主任务已修复且测试通过；scope 读取后也增加 145 行、145 唯一 Page、语言非空校验。QA 未执行生产部署。heartbeat `dramawave-145-page` 已由主任务暂停并读回，后续不再使用 Codex 定时任务。服务器 unit 显式 Asia/Shanghai 的三条日历已通过 CPU systemd 239 验证。主任务已部署 `20eb3a8`，timer enabled/active，三个文件哈希匹配，首次及 17:35:46 报告正常；完整记录见 production-verification.md。

17:23 制作进度由主任务报告 ready 3、preparing 2、新增错误 0，GPU 约 4 核/1.5 GiB RAM/1.4 GiB VRAM。巡检 unit 与脚本均要求数据盘已挂载；本轮只读审阅该保护，没有模拟生产卸载。

17:35 主任务抽查自然成片 1512、1513、1514：公网 TLS HEAD 均 200/video/mp4，大小、SHA256、制作 profile 与数据库相符。17:35:46 新增任务 ready 13；全部未来任务 ready 151、preparing 2、planned 717，无当前巡检异常。此证据验证制作及公开拉取，不替代到期后的 Graph 发布结果。
