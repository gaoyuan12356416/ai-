# BUG-002 TL 历史 COS HTTP 地址被错误排除

## 状态与影响

2026-09-18：已关闭。P1。TL 候选完整扫描曾返回 0，但不能据此判定没有 TL 视频、没有描述或剧未上线；根因为历史 URL 过滤。修复 `f7cb58f` 已部署，TL 候选已重建为 500 条。

## 根因与证据

历史素材保存为 `http://advertising-1306474899.cos.ap-hongkong.myqcloud.com/...`，原 `features/fb_auto_posts/repositories.py::_https` 仅接受 HTTPS，因而在视频候选校验阶段将它们过滤。

主任务只读抽查素材 `1916498`、`1918582`、`1918961`、`1925790`、`1931309`：相同对象改用 HTTPS 后 HEAD 均返回 200、`Content-Type: video/mp4`；对应 TL 描述唯一且非空，剧已上线。此处记录主任务提供的抽查证据，QA 未重新请求这些对象。

## 精准修复契约

- 仅允许解析后 hostname 精确等于 `advertising-1306474899.cos.ap-hongkong.myqcloud.com` 的历史 HTTP URL 规范化为 HTTPS。
- 转换要求没有 userinfo、端口或 fragment；其他 HTTP 主机、伪装后缀主机、带凭证或端口的地址仍拒绝。
- 转换仅更换 scheme，保留完整对象路径、百分号编码和 query 内容；后续获取使用 HTTPS，不先请求 HTTP。
- 现有合法 HTTPS 输入的校验契约保持不变；不修改源 MySQL 历史地址，不扩大语言、描述、上线状态或其他选材规则。

## 影响文件与测试

主任务修改 `features/fb_auto_posts/repositories.py`，新增 `scripts/test_fb_material_urls.py` 四项测试：同对象路径/query 保留、其他 HTTP 主机与歧义 authority 拒绝、fragment 拒绝、原 HTTPS 契约保持。

主任务的 191 项全套首次出现两项旧预期失败，分别为 quick-nav 应保留 `20260915retired`、publisher 已改为 8 路。预期更新后，最新 192 项全套通过（23.191 秒）；最终应用版本 `90faff9` 已上线。既有 39 与 186 项历史测试证据继续保留。

## 待验收

修复部署、全套回归和 TL 500 候选恢复已由主任务提供证据；最终 7 种语言各 500 条审核并冻结。732 条未来运行 apply 属于独立执行验收，不因本缺陷关闭而视为已完成。
