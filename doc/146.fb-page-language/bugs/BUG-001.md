# BUG-001 已完成扩充重试仍依赖素材仓库

## 发现与影响

2026-09-18 独立 QA，P2。扩充已经提交并存在持久化回执后，相同操作重试仍先读取素材仓库；若仓库不可用或候选耗尽，会返回失败而无法读取已成功结果。原任务不会因此重复，但丢失响应后的恢复不可靠。

## 复现

1. 对未来自动运行成功执行 `extend_future_runs`。
2. 保留相同 `operation_id`、运行和 Page 范围，令素材仓库读取抛错。
3. 再次执行；应从回执返回幂等成功，实际在 `candidate_snapshots_for_pages` 抛错。

## 根因与修复建议

`features/fb_auto_posts/pool_extension.py` 在候选快照读取后才查询回执。建议在完成操作/运行范围核验后，对全部已有且范围一致的回执直接返回；事务中继续检查回执防止并发重复。负责人：主任务。

## 测试证据

`python -m unittest scripts.test_fb_pool_extension -v` 的 `test_receipt_retry_does_not_depend_on_catalog_remaining_available` 失败，提示 `completed extension must use its durable receipt`。

## 修复与回归状态

已关闭。主任务在素材仓库读取前检查全部匹配持久化回执并返回幂等成功；事务内仍逐运行复查回执，防止并发重复。

修改文件：`features/fb_auto_posts/pool_extension.py`。独立回归命令：`python -m unittest scripts.test_fb_page_languages scripts.test_fb_pool_extension -v`，39/39 通过，5.694 秒；本缺陷复现用例通过。
