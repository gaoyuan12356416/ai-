# SA 代码评审

## 结论

通过，可进入 GitHub-first 发布。P0/P1 开放问题为 0。

## 评审范围

- 配置保存：Creator Info 已移除；可信账号、本地设置、版本锁和 canary 锁保留。
- 素材入池：配置版本一致时账号可省略；SHA-256 稳定分配且响应返回实际账号。
- 预制作：不读取账号源或 Creator Info，只要求成片时长为正。
- 发布执行：自动发布与立即测试的 Creator Info、Token、设置和最大时长校验仍保留。
- 前端：入池门禁不再读取本次测试账号；立即测试门禁未改。
- 兼容性：legacy 显式账号路径保留；无 schema 变化。

## 问题与处理

| 编号 | 级别 | 问题 | 处理 | 状态 |
| --- | --- | --- | --- | --- |
| CR-01 | P1 | 初次补丁误放宽立即测试预制作的最大时长校验 | 已恢复立即测试原门禁并跑全量回归 | 已关闭 |
| CR-02 | P1 | 非 ASCII 描述模板直接传给 `compare_digest` 会抛 `TypeError` | 改为 UTF-8 bytes 常量时间比较 | 已关闭 |

## 验证结果

- 9 个 Python 测试脚本：344/344 通过。
- Node drama bridge：53/53 断言通过。
- 页面内联 JavaScript 语法通过。
- Python 编译和 `git diff --check` 通过。
