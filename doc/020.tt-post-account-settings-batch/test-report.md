# 测试报告

## 测试结论

本地自动化与静态检查全部通过，未发现阻断发布的问题。生产只读浏览器验收待部署后执行。

## 测试范围

- TT Core、CPU Service/HTTP/Runner、App contract、账号管理 UI、发布池 UI 和 GPU 协议。
- X 发布、多排期存储与 UI。
- 素材 webhook 与播报。
- Python 编译、页面 JavaScript 解析和差异格式检查。

## 执行统计

| 类型 | 数量 | 通过 | 失败 | 阻塞 |
| --- | --- | --- | --- | --- |
| TT 自动化 | 138 | 138 | 0 | 0 |
| X 自动化 | 72 | 72 | 0 | 0 |
| 素材自动化 | 28 | 28 | 0 | 0 |
| 合计 | 238 | 238 | 0 | 0 |

## 缺陷情况

无未关闭缺陷。代码评审发现的前端 50 个上限问题已修复并补充契约测试。

## 验证证据

- 末尾版本冲突时，前序账号设置和版本均不变化。
- 任一账号实时能力失败或能力不兼容时，批量保存 0 写入。
- 批量检测只返回共同隐私项，评论、Duet、Stitch 按全部账号能力取交集。
- 单账号接口、发布池、GPU 和三重门禁契约未改变。

## 遗留风险

- TikTok 实时能力接口的延迟和可用性由上游决定；批量检测限制并发 4、单批最多 50，任一失败整批停止。
- 生产浏览器仅允许检测，不保存真实账号设置。

## 发布建议

建议按 GitHub 精确提交发布到 CPU 服务，保持 GPU 与 Direct Post 门禁不变；部署后完成 BS-15。
