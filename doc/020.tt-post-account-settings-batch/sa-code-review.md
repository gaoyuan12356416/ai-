# SA 代码评审

## 结论

通过，可进入发布。实现符合统一策略、能力交集、全成全败和不触碰 Token 的设计。

## 评审范围

- Core 批量事务及乐观版本控制。
- Service 目标校验、并发受限能力检测、能力交集和保存前复检。
- AI 后台路由权限、同源约束和审计摘要。
- 页面选择状态、检测失效、共享表单和批量结果处理。
- 单账号、发布池、GPU、X 和素材相关回归。

## 问题清单

| 编号 | 严重级别 | 文件/位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| CR-001 | P1 | 批量 UI | “全选”可能超过服务端 50 个上限 | 前端按账号顺序最多选择 50 个并提示，服务端继续硬校验 | 已修复 |
| CR-002 | P0 | Core 事务 | 若边写边校验版本，末尾冲突可能留下前序修改 | 同一 `BEGIN IMMEDIATE` 中先预检全部版本，再统一写入 | 已确认 |
| CR-003 | P1 | Service 能力检测 | 单账号能力差异可能被首个账号覆盖 | 返回所有账号隐私交集，互动仅在全部允许时开启 | 已确认 |
| CR-004 | P1 | App 审计 | 直接记录请求体会扩大敏感信息风险 | 只记录账号 ID、数量、共享设置和保存数量 | 已确认 |

## 编译 / 验证结果

- Python 编译通过。
- TT 相关 138/138 通过。
- X 相关 72/72 通过。
- 素材相关 28/28 通过。
- 总计 238/238 通过。
- 页面内联 JavaScript 解析、`node --check` 与 `git diff --check` 通过。
