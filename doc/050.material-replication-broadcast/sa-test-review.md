# SA 测试用例评审

## 结论

2026-08-27：36 项业务场景的测试设计通过；独立 QA 已执行影响范围内 11 个模块、244 项测试，全部通过，外网尝试为 0。本地实现签署与发布负责人回传的 CPU / 无消息上线验证分别记录；均不等同于真实送达或用户最终验收。

发布阻断条件：任一 P0 用例失败或缺少可核验执行证据；未知结果可能转兜底；安全窗口可能重置；新旧表混用；超限被静默截断 / 自动拆分；回归发生真实飞书调用。

## 覆盖性问题

| 编号 | 场景/用例 | 问题 | 建议 / 负责人 | 状态 |
| --- | --- | --- | --- | --- |
| STR-01 | MR-20 至 MR-23、MR-29 至 MR-30 | 仅模拟 sender 抛异常，无法证明发送前预置和崩溃恢复安全 | 服务 / 运行时测试注入“预置提交后、POST 前”与“成功返回后、落库前”两个断点，独立 QA 复核冻结字段 | 本地验证通过 |
| STR-02 | MR-21、MR-22 | 后续明确失败易覆盖历史未知；HTTP 与 JSON 结果可能互相矛盾 | 参数化未知后再明确失败，以及 HTTP 5xx + code=0；断言零兜底，不只断言 status | 本地验证通过 |
| STR-03 | MR-27 至 MR-28 | 只测普通重试无法证明 55 分钟安全窗口 | 假时钟覆盖 3299 / 3300 / 3301 秒；重开 SQLite、续租和鉴权重试均验证初始时间不变 | 本地验证通过 |
| STR-04 | MR-05 至 MR-06 | 字符数测试不能代表实际 HTTP JSON 字节数，兜底正文可能更大 | 比较 requests 最终序列化请求体；覆盖中文、emoji、转义及兜底附加文案；超限零写入 / 零发送 | 本地验证通过 |
| STR-05 | MR-09 至 MR-16、MR-34 | 批次重放、新旧表隔离、收件人身份和旧行为易混淆 | 用同数值 ID 的新旧事件、不同制作人 / 优化师、映射变更和并发请求做反例；完整执行旧专项 | 本地验证通过 |
| STR-06 | MR-24 至 MR-25、MR-31 至 MR-32 | 阶段预算与租约检查只测 happy path 不足 | 验证兜底预算重置 5 次、阶段不回退；过期 / 被接管 lease 在实际外部调用前零发送 | 本地验证通过 |
| STR-07 | MR-12、MR-35 至 MR-36 | 接收端测试不能证明上游去重或真实厂商送达 | 将上游跨批次去重列为对接责任；部署 / 生产验收单列，不以 Mock 成功宣称真实送达；负责人：主负责人、独立 QA | 本地与无消息上线通过；上游 / 真实送达另验 |

## QA 修订确认

已将上述覆盖性要求写入 `test-cases.md`。未实现业务代码的独立全回归负责人已统一执行 244 项测试，并补充 24 项独立安全 / 部署 / 文档测试；开发者自测仅作为补充，未替代最终回归。

执行使用临时存储和虚构凭据，进程级阻止非 loopback DNS / connect / connect_ex，实际外网尝试为 0。发送前落盘、成功后记账失败、Token 内部两次请求的历史未知、3300 秒边界及失效租约均已独立核验；BUG-001 至 BUG-005 修复复验通过。原始命令、退出码、统计及源码指纹见 `test-report.md`。发布负责人已回传最终提交的 CPU Python 3.9.6 244/244 PASS（16.792 秒、外网 0），以及 apply、备份 / 副本建表和八个线上拒绝探针通过的证据；新表存在且为 0 条。startup / 配置权限附加核对也已通过：worker configured=True、启动失败 0，哈希匹配、备份 quick_check=ok、目录 0700 / Token 配置 0600。独立 QA 已读取 `evidence/deployment-verification.json` 并与报告核对一致；本轮部署检查封板，首个获准业务批次与真实消息送达仍未验收。
