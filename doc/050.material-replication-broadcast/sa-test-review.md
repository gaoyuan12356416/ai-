# SA 测试用例评审

## 结论

2026-08-27：测试设计有条件通过，36 项用例已覆盖已确认范围及关键失败窗口；全部待执行。该结论不等同于实现通过、生产验证通过或用户最终验收。

发布阻断条件：任一 P0 用例失败或缺少可核验执行证据；未知结果可能转兜底；安全窗口可能重置；新旧表混用；超限被静默截断 / 自动拆分；回归发生真实飞书调用。

## 覆盖性问题

| 编号 | 场景/用例 | 问题 | 建议 / 负责人 | 状态 |
| --- | --- | --- | --- | --- |
| STR-01 | MR-20 至 MR-23、MR-29 至 MR-30 | 仅模拟 sender 抛异常，无法证明发送前预置和崩溃恢复安全 | 服务 / 运行时测试注入“预置提交后、POST 前”与“成功返回后、落库前”两个断点，独立 QA 复核冻结字段 | 已纳入计划；待执行 |
| STR-02 | MR-21、MR-22 | 后续明确失败易覆盖历史未知；HTTP 与 JSON 结果可能互相矛盾 | 参数化未知后再明确失败，以及 HTTP 5xx + code=0；断言零兜底，不只断言 status | 已纳入计划；待执行 |
| STR-03 | MR-27 至 MR-28 | 只测普通重试无法证明 55 分钟安全窗口 | 假时钟覆盖 3299 / 3300 / 3301 秒；重开 SQLite、续租和鉴权重试均验证初始时间不变 | 已纳入计划；待执行 |
| STR-04 | MR-05 至 MR-06 | 字符数测试不能代表实际 HTTP JSON 字节数，兜底正文可能更大 | 比较 requests 最终序列化请求体；覆盖中文、emoji、转义及兜底附加文案；超限零写入 / 零发送 | 已纳入计划；待执行 |
| STR-05 | MR-09 至 MR-16、MR-34 | 批次重放、新旧表隔离、收件人身份和旧行为易混淆 | 用同数值 ID 的新旧事件、不同制作人 / 优化师、映射变更和并发请求做反例；完整执行旧专项 | 已纳入计划；待执行 |
| STR-06 | MR-24 至 MR-25、MR-31 至 MR-32 | 阶段预算与租约检查只测 happy path 不足 | 验证兜底预算重置 5 次、阶段不回退；过期 / 被接管 lease 在实际外部调用前零发送 | 已纳入计划；待执行 |
| STR-07 | MR-12、MR-35 至 MR-36 | 接收端测试不能证明上游去重或真实厂商送达 | 将上游跨批次去重列为对接责任；部署 / 生产验收单列，不以 Mock 成功宣称真实送达；负责人：主负责人、独立 QA | 已纳入计划；待执行 |

## QA 修订确认

已将上述覆盖性要求写入 `test-cases.md`。测试执行由未实现业务代码的独立全回归负责人统一完成；开发者自测可作为补充，不能替代最终回归。

执行前确认测试只使用临时存储和虚构凭据，阻止非 loopback 网络；逐项记录原始命令、退出码、用例数及失败原因。未运行、环境阻塞和仅文档检查必须分开统计。当前没有执行结果，不能关闭 SA 设计问题或给出发布通过结论。
