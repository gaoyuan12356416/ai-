# 验收用例

| 用例 | 通过条件 |
|---|---|
| 总数与命名 | 四组各20，01-20连续，light原2条 |
| 追加兼容 | 旧40条metadata逐字段不变，旧目录与文件完整SHA一致 |
| 静态素材 | 新20张PNG为720x1280 RGBA；边框中心透明；色彩图层保持原低强度合成语义 |
| 动态素材 | 新22段透明VP9、30fps、120帧、4秒、无音频，动态有效、周期端点一致 |
| 像素保真 | 解码alpha最大误差<=1；合成平均误差<1.5/255；PNG等于生成设计 |
| 可选择 | 确定性抽样覆盖80个活动项；相同身份重复得到同配方 |
| 元数据 | CPU严格解析，Git blob/本机/两GPU根/CPU文件SHA相同 |
| 缓存 | 两套80项有效，原像素及V2时间戳收据通过，不超过原容量上限 |
| 实际制作 | 12个五秒组合覆盖42新样式并跨4秒循环；两个旧目录配方可在新默认下制作；TT/FB输出格式与时长保留 |
| 上线 | 三GPU+两CPU进程配置一致，三个隧道健康；TT七trigger/FB timer恢复 |
| 回滚 | 仅回退默认根/SHA，所有旧新配方目录仍被认可，禁止恢复旧DB |
