# 代码与素材审查

2026-09-20 审查通过。媒体解码和服务器隔离样片验证已完成，生产结果见部署记录。

- 本次只追加素材/生成验证脚本，三条业务制作路径保持线上版本。
- 已检查42个程序化样式定义：形状与构图各异；动态使用整数周期和边缘消隐；中央人物及字幕区域透明，角标限于四角，色彩保留1%-10%运行时强度。
- 已人工查看42格预览，未见中心遮挡、缺失或重复构图。色彩图层在10%示例强度下保持轻量。
- 构建器最多2路编码，完整文件需SHA、源身份及容器/解码检查才能复用；最终manifest按定义顺序构建。
- 验证器逐帧检查22段视频，记录循环末帧到首帧相对正常帧间变化；不只依赖t=0/t=1恒等。
- staging只允许受控绝对目录名，旧文件保持并验证SHA，不覆盖既有目录。light2条保持但不参与80个活动项。
- JSON文件和被收据引用的artwork源字节使用Git -text，防止跨平台换行变更指纹。
- 配置切换脚本要求已通过真实GPU样片、原目录/代码指针未漂移、媒体子进程和HTTP请求排空；备份后只替换默认目录与新增受信版本映射。

已执行19项目录/冻结配方回归，全部通过；新脚本语法、42项完整媒体检查、2000种子覆盖和16段真实GPU样片均通过。

独立部署脚本复核已完成：SIGSTOP意图先持久化及精确身份恢复问题在执行前修复（BUG-002），二次审查通过。最终运行脚本归档operations/，无生产密钥。
