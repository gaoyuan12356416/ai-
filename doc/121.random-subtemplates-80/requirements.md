# 随机模板四组各20个

2026-09-20 用户明确要求边框、透明视频、角标、色彩均扩充至20个，总计80个有效子模板。现网分别8/10/8/12，因此追加12/10/12/8，共42个。四组组合数由7,680变为160,000，不包括连续旋转、缩放和色彩强度参数。

保留已有素材、名称、文件SHA和严格manifest字段。历史light2条保持但不参加随机选择。新任务使用新目录；已冻结、制作中和完成任务沿用其目录、配方、成片身份。TT、FB和Drama三条随机制作链路统一默认目录，CPU只读元数据。原旋转、缩放、色彩强度范围及图层顺序不变。

技术方案：追加只读版本目录；继续沿用线上目录版本兼容代码；追加第三个SHA受信映射，两个旧目录都保留。四类标准命名连续补齐01至20。PNG为720x1280 RGBA；动态层为720x1280、30fps、120帧、4秒透明VP9、无音频。动态保持循环、主体与字幕区域低遮挡。两个RGBA缓存预先构建，保留旧项。部署只更新素材及默认配置，不替换线上应用代码。

验收：四组精确20；旧元数据原样；42素材透明/动画/尺寸/时长验证；全部80可选且可复现；三链路真实GPU私有样片；新默认仍能执行两个旧目录配方；CPU/GPU新SHA一致；服务、隧道及原触发器恢复。无业务数据库迁移，无对外测试发布。
