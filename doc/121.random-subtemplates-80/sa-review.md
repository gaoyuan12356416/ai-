# 设计审查

2026-09-20：通过方案，发布前必须满足以下条件。

- 已确认“各20个”为总数，不是各新增20个。
- 历史目录不可覆盖；旧配方必须同时保留原始028326目录及9月18日24d6fa目录映射。
- CPU仅换元数据配置，三条GPU制作路径沿用实际线上代码，避免覆盖9月18日以后的无关变更。
- Drama素材位于其隔离根，不能直接指向共享根；按真实服务UID验证。
- Git -text保留manifest精确字节；以Git blob和服务器完整SHA复核。
- 缓存容量上限96GiB/套及32GiB空闲保留；在切默认池前离线完成两套缓存。
- 保存原触发器状态，排空已受理媒体和HTTP请求后再窄重启。回滚只换默认目录，保留三版本映射和所有业务状态。
