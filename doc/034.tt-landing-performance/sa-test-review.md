# SA 测试用例评审

## 结论

通过。用例覆盖首屏正确性、缓存压缩、安全边界、LKG、图片回退和完整跳转回归。

## 覆盖性问题

| 编号 | 场景/用例 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- |
| STR-001 | TC-006 | 只看最终 DOM 无法证明没有语言闪屏 | 使用 init script MutationObserver + FCP 记录标题 | 已纳入 |
| STR-002 | TC-008 | 只检查配置不能证明 gzip 生效 | 对公网和 loopback 都发送 Accept-Encoding 验证 | 已纳入 |
| STR-003 | TC-015~017 | 只测成功图片不足 | 增加超时、字节上限、像素上限、解码失败回退 | 已纳入 |
| STR-004 | TC-019~021 | 性能改动可能破坏跳转 | 拦截但不真实访问 W2A，核对完整 URL | 已纳入 |

## QA 修订确认

已补充 DOM/FCP、双侧 gzip、图片失败和跳转合同测试。
