# 设计审阅

2026-09-15，由本任务执行静态审阅。

- 接受：观察线程与 GPU 合成名额独立，OpenCL 合成仍为 1。
- 接受：预取只在当前任务完成来源准备后开始；不会争抢当前下载。
- 已处理：冻结路由和完整校验不可绕过，不跨 CDN 拼接断点。
- 已处理：同任务文件锁保护交接；预取未退出不能释放实例锁。
- 已处理：预取是可选准备，其异常不进入整个任务的失败/重试流程。
- 已处理：小范围采样不作为整部剧提速比例；补充完整文件及自然任务观察。
- 发布要求：CPU 定点覆盖并核对基线；HK 完整发布目录与 release SHA 一致。
