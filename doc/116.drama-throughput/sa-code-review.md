# 代码审阅

- SQL 在 LIMIT 前排序，恢复任务不会被前 50 条排队任务遮挡。
- worker_id/attempt 校验和独立心跳保持；观察线程最多 2。
- 队列读取偶发失败保留活跃观察者，下一轮仅重试读取。
- 预取持有同任务文件锁，合成 semaphore 不变。
- 关停/交接发送下载取消事件，已有强 ETag、SHA 和断点继续有效。
- 页面仅投影白名单数字，可选预取失败不变成业务失败。
- 网络前及文件大小返回后核查预算；预取默认关闭，生产显式开启。
- CPU 仅更新核对一致的模块，保留后台其他已部署变更。
