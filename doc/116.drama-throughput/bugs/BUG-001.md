# BUG-001 重启后原制作进度不更新

状态：代码及离线回归通过，生产验收见 test-report.md。

原因：领取 SQL 将 queued 优先。GPU 制作继续运行，CPU 重启后唯一观察线程去等待下一任务，原任务没有观察者。

修复：中断租约优先，两个独立观察者跟踪当前及下一任务；保持媒体 ID 和失败重试契约。

验证：ObserverTests 覆盖 60 条排队记录、有效租约、双槽及退出；remote_client 回归验证断连和副作用校验。
