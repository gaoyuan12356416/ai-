# 测试报告

## 测试结论

本地与生产门禁均通过，未执行任何真实 X 发布。生产数据库副本迁移两次可重复，部署前后账本计数零增量。

## 测试范围

- 高优资格、最近点击排序、取消、校验失效清理、原绑定优先和冻结计划不变。
- 手动请求幂等、素材池隔离、全局去重、全批预检、原子队列、恢复与中断终态。
- 账号身份重读、长视频 token 会员资格、worker bearer、公开 DTO 白名单和 method/CSRF 合同。
- daily/catchup/schedule/canary、素材选择、媒体修复、短剧回放与历史账本回归。
- 三个变更页面的脚本语法和 DOM 合同。

## 执行统计

| 类型 | 数量 | 通过 | 失败 | 阻塞 |
| --- | --- | --- | --- | --- |
| X Python 离线测试 | 408 | 407 | 0 | 0 |
| Python 编译 | 关键模块/测试 | 全部 | 0 | 0 |
| 前端语法/DOM 合同 | 3 页 | 全部 | 0 | 0 |
| 生产副本迁移/部署验收 | 1 组 | 1 | 0 | 0 |

另有 1 项既有条件测试按设计跳过，不计为失败或阻塞。

## 缺陷情况

代码评审发现 2 个 P0、3 个 P1 和 1 个 P2，均在提交前修复并加入回归。没有未关闭的本地 P0/P1 缺陷。

## 验证证据

- `python -m unittest discover -s scripts -p "test_x*.py"`
- `python -m py_compile ...`
- Node 解析变更页面全部 inline script。
- `git diff --check`
- 生产提交 `5f9084b…`：72 项定向测试、真实 EnvironmentFile 配置校验和 systemd unit verify 通过。
- 生产备份副本迁移两次：旧表原字段哈希一致，完整性均 `ok`，schema 可重复。
- 部署结果：queue/log/Post ID `150/150/149`，unknown/active queue 均为 `0`，manual timer 自然 `no_pending`。
- 公网静态入口修正后，登录态 Chrome 验证手动按钮顺序、弹窗警告、12 个短剧高优控件及 7/5 可用状态；未点击最终确认，manual run/queue 均为 `0`。

## 遗留风险

- X 写入不是数据库事务；页面已明确提示可能部分成功且不能回滚。
- 队列一旦建立即永久占用素材；普通明确失败不会自动重试。
- 真实 token、MySQL、GPU repair 和 X API 只在自然生产执行时验证，本次部署验收禁止造 Post。
- 当前生产页面中的 15 个 X 账号均显示“无会员/不可发布”，因此现阶段手动弹窗没有可选目标账号；需先恢复相应账号的当前 token 授权/订阅能力，系统不会绕过此门禁。

## 发布建议

功能发布门禁已通过；实际创建手动批次前须先恢复至少一个账号的当前发布资格。保留上述备份和前一 release 作为回滚点。
