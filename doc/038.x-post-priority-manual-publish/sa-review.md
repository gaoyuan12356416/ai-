# SA 评审意见

## 结论

有条件通过。需求必须保持“一剧一号”、全局去重、全批预检、未知结果停批和 GitHub-first 部署；下列问题已在需求正文中关闭。

## 问题清单

| 编号 | 严重级别 | 位置 | 问题 | 建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SA-001 | P0 | 短剧高优 | 直接修改 `created_at` 会破坏入池审计与历史选择证据 | 使用独立 `priority_at`，只改变未分配候选排序 | 已采纳 |
| SA-002 | P0 | 短剧高优 | 高优若能覆盖已绑定短剧会破坏一剧一号和连续剧集 | 仅允许未分配且仍可发布的短剧高优 | 已采纳 |
| SA-003 | P0 | 手动发布 | 仅加按钮并直接调用 X 会绕过队列、去重和未知结果保护 | 增加持久化 manual run，预检后原子建队列 | 已采纳 |
| SA-004 | P0 | 手动发布 | 浏览器同步等待下载/GPU/X 请求不可靠 | 独立 timer/oneshot 异步执行并支持恢复 | 已采纳 |
| SA-005 | P0 | 批次语义 | 素材数与账号数不一致时映射含糊 | 强制 N=N，后台按会员资格匹配 | 已采纳 |
| SA-006 | P0 | 幂等 | 响应丢失或重复点击可能创建第二批 | 客户端稳定幂等键 + 数据库唯一约束 | 已采纳 |
| SA-007 | P1 | 调度并发 | 手动与自动 worker 可同时发布 | 共用生产发布锁 | 已采纳 |
| SA-008 | P1 | 回滚 | 删除新增表/列会破坏历史账本 | 代码回滚保留 additive schema 和发布历史 | 已采纳 |

## 决策记录

- 手动发布账号在独立弹窗选择，默认使用已保存素材排期账号，不读取未保存排期草稿。
- 明确普通失败继续处理后续账号；限流或任何未知结果停止剩余账号。
- 手动素材不入池，但队列建立后仍执行永久全局去重。
- 部署验收不得调用真实 X 写接口。

## PM 修订确认

2026-08-11：所有 P0/P1 建议已写入 `requirements.md`，可以进入开发。
